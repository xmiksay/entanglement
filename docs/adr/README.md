# Architecture Decision Records

An **ADR** is a short, immutable record of *why* an architecture decision was
made. The [architecture doc](../architecture.md) describes the current state
(*what is*); ADRs are the decision log (*how we got here, and what else we
considered*). The two run in parallel: a decision lands here first, then the
arch doc is updated to reflect it.

## When to write one

Write an ADR for any decision that is **hard to reverse** or that a reader would
reasonably ask *"why?"* about — protocol shapes, crate boundaries, a chosen
pattern over an obvious alternative, security/permission models. Don't write one
for trivial refactors or local naming.

## Format

File name: `NNNN-kebab-case-title.md` (zero-padded, monotonically numbered).
Each record:

```
# NNNN. Title
- Status: Proposed | Accepted | Superseded by ADR-XXXX | Deprecated
- Date: YYYY-MM-DD

## Context
Why this came up — forces, constraints, what the reference projects do.

## Decision
What we chose, precisely.

## Consequences
Positive / negative / neutral effects.

## Alternatives considered
The options rejected and why. (This is the part the arch doc can't carry.)
```

## Status lifecycle

`Proposed` → `Accepted` → (`Superseded by ADR-XXXX` | `Deprecated`). Never edit
an accepted ADR in place — supersede it with a new one that links back.

## Index

| # | Title | Status |
| --- | --- | --- |
| [0001](0001-actor-model-abi.md) | Actor model is the integration ABI | Accepted |
| [0002](0002-session-multiplexed-protocol.md) | Session-multiplexed wire protocol | Accepted |
| [0003](0003-agent-and-permission-profiles.md) | Agent + permission profiles (opencode-style) | Accepted |
| [0004](0004-structured-plan-and-task-events.md) | Structured Plan & TaskList events (profiles + events, both) | Accepted (`TaskList` half superseded by [0039](0039-markdown-task-list.md)) |
| [0005](0005-ndjson-stdio-head.md) | NDJSON stdio head (`run` + `pipe`) | Accepted |
| [0006](0006-core-dependency-hygiene-gate.md) | Layering: core / provider / runtime + core hygiene gate | Accepted (direction superseded by [0053](0053-invert-core-provider-seam.md)) |
| [0007](0007-streaming-llm-and-provider-crate.md) | `entanglement-provider`: streaming `Llm` trait, pooling, retry, rate-limit, reasoning | Accepted (trait placement superseded by [0053](0053-invert-core-provider-seam.md)) |
| [0008](0008-host-tools-workdir-and-bounded-output.md) | Host tools: working-directory root + bounded output | Accepted (lexical-only containment superseded-by-addition by [0054](0054-canonicalizing-symlink-safe-root-containment.md)) |
| [0009](0009-edit-and-bash-host-tools.md) | Host tools: `edit` (search/replace) and `bash` (subprocess + timeout) | Accepted |
| [0010](0010-single-head-crate-and-bash-opt-in.md) | `entanglement-runtime`: the head crate — tools, execution, permissions, sessions | Accepted |
| [0011](0011-tui-head-ratatui-crossterm.md) | TUI head: ratatui + crossterm in `entanglement-runtime` | Accepted |
| [0012](0012-tui-event-buffering-rendering.md) | TUI event-buffering & rendering model | Accepted |
| [0013](0013-keybinding-leader-which-key.md) | Keybinding scheme: leader key + which-key | Accepted |
| [0014](0014-tool-approval-inline-modal.md) | Tool approval UX: inline card vs modal | Accepted |
| [0015](0015-rich-text-pipeline-syntect.md) | Rich-text pipeline: pulldown-cmark → ratatui Text, syntect for code blocks | Accepted |
| [0016](0016-host-tool-empty-result-contract.md) | Host tools: empty-result contract (no silent zero-output) | Accepted |
| [0017](0017-stop-cancels-turn-not-session.md) | `InMsg::Stop` cancels the turn, not the session | Accepted |
| [0018](0018-turn-loop-stash-discipline.md) | Turn-loop command stash discipline | Accepted |
| 0019 | — _(number skipped; no ADR-0019)_ | — |
| [0020](0020-event-sourced-session-persistence.md) | Event-sourced session persistence | Accepted |
| [0021](0021-hierarchical-session-model.md) | Hierarchical session data model | Accepted |
| [0022](0022-subagent-spawn.md) | Sub-agent spawn and parent→child answer relay | Accepted |
| [0023](0023-subagent-spawn-limits.md) | Sub-agent spawn recursion / fan-out limits | Accepted |
| [0024](0024-subagent-permission-gating.md) | Sub-agent spawn permission gating and privilege ceiling | Accepted |
| [0025](0025-runtime-cargo-feature-gates.md) | `entanglement-runtime` cargo feature gates (`cli`/`tui`) for lean library embedding | Accepted (lean-transport claim amended by [0053](0053-invert-core-provider-seam.md)) |
| [0026](0026-async-subagent-spawn-and-poll.md) | Non-blocking sub-agent spawn with handle + `agent_poll` | Accepted |
| [0027](0027-ask-user-interactive-prompt.md) | `ask_user` tool — model-driven user decision prompt | Accepted |
| [0028](0028-session-lifecycle-enumeration-and-backpressure.md) | Session lifecycle: enumeration, explicit close, non-blocking routing | Accepted |
| [0029](0029-external-editor-and-markdown-export.md) | External `$EDITOR` compose + Markdown transcript export | Accepted |
| [0030](0030-tui-file-mentions-and-bash-passthrough.md) | TUI `@file` mention completion + `!bash` passthrough | Accepted |
| [0031](0031-write-host-tool-whole-file.md) | Host tool `write`: whole-file create/overwrite (quartet → quintet) | Accepted |
| [0032](0032-yaml-provider-model-catalog.md) | YAML provider/model catalog: embedded defaults + user override | Accepted |
| [0033](0033-agent-tool-family-and-blocking-agent.md) | `agent_*` tool family: rename `spawn_agent` → `agent_spawn`, add blocking `agent` | Accepted |
| [0034](0034-file-based-agent-definitions.md) | File-based agent definitions: discovery, frontmatter, registry | Accepted |
| [0035](0035-deterministic-system-prompt-assembly.md) | Deterministic system-prompt assembly: preamble + body + brief + env + skills | Accepted |
| [0036](0036-skill-discovery-and-registry.md) | Skill discovery + registry: SKILL.md frontmatter, tier-1 disclosure | Accepted |
| [0037](0037-load-skill-tool-deterministic-resolution.md) | `load_skill` tool: deterministic resolution + path substitution | Accepted |
| [0038](0038-physical-per-agent-tool-restriction.md) | Physical per-agent tool restriction: allowlist/denylist masks specs + dispatch | Accepted |
| [0039](0039-markdown-task-list.md) | Markdown task list: structured `Vec<TaskItem>` → plain snapshot | Accepted |
| [0040](0040-per-profile-spawn-control.md) | Per-profile spawn control: `can_spawn` + spawnable-agents allowlist + target-mode gate | Accepted |
| [0041](0041-update-plan-ownership-default-closed.md) | `update_plan` ownership: `owns_plan` default-closed plan authority + physical read-only `plan` | Superseded by [0049](0049-plan-task-tools-as-runtime-state-tools.md) |
| [0042](0042-plan-acceptance-via-propose-plan-approval-roundtrip.md) | Plan acceptance via `propose_plan` approval round-trip: accept → fresh root `build` session (head-policy handoff) | Accepted |
| [0043](0043-skill-preload-vs-access-independent-mechanisms.md) | Skill preload (`skills:` frontmatter) vs access (`load_skill` tool mask) as two independent agent-definition mechanisms | Accepted |
| [0044](0044-agents-skills-system-prompt-epic-synthesis.md) | Agents, skills & system prompt — epic synthesis: six principles → enforcement map, disclosure tiers, enforcement-locus split, deferred follow-ups | Accepted |
| [0045](0045-call-host-tool-argv-exec-tailed-output.md) | Host tool `call`: argv exec (no shell) with auto-tailed output (`tail=30`, `tail=0` = full) | Accepted |
| [0046](0046-rhai-sandboxed-script-tool.md) | `rhai` host tool: embedded capability-sandboxed script engine; quintet bindings permission-checked per call via a sync/async bridge | Accepted |
| [0047](0047-local-trust-boundary.md) | Local trust boundary: repo is trusted, config precedence system < user < repo, inspection over enforcement | Accepted |
| [0048](0048-serve-head-local-trust-model.md) | `serve` head: local-only WebSocket protocol interface (Vue SPA primary/non-exclusive; browser surface out of scope; loopback + opt-in handshake) | Accepted |
| [0049](0049-plan-task-tools-as-runtime-state-tools.md) | `update_plan`/`update_tasks` as runtime state tools: out of core, gated by the ordinary permission path; plan authorship default-closed via explicit allowlist membership (supersedes 0041) | Accepted |
| [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md) | Provider resilience keyed **per `(endpoint, api-key)`**: RPM budget + `Retry-After` window per base URL + key hash (isolates throttling; multiple keys each get their own limit); retry classifies the response status inside the loop (fixes dead-code #193); `LlmSession` references per-endpoint state (#195) | Accepted |
| [0051](0051-argument-scoped-permission-rules.md) | Argument-scoped permission rule keys `tool(pattern)`: `PermissionProfile::resolve(name, arg)` + dependency-free `*`/`?` glob in core; runtime extracts the argument (command for `bash`/`call`, path for `edit`/`write`/`read`) and threads it through the ancestor clamp, config ceiling, and rhai bindings (#173) | Accepted |
| [0052](0052-approval-scope-and-persisted-grants.md) | Approval scope `Once \| Session \| Always` on `InMsg::Approve` (default `Once`, wire-additive) + a runtime `GrantStore` that upgrades a resolved `Ask` → `Allow` for an exact `(tool, arg)` already granted (never overrides `Deny`, not sub-agent-inherited); `Always` persists to a managed `grants.yml` sibling of `config.yml`, not its ceiling `permissions` section (#174) | Accepted |
| [0053](0053-invert-core-provider-seam.md) | Invert the core↔provider seam: `entanglement-provider` becomes a leaf owning the `Llm` trait + DTOs + `Message` (usable standalone for raw LLM); `entanglement-core` depends on provider and is no longer transport-free; `make tree`/`make check-lean` gates narrowed to UI/web-server + CLI/TUI (supersedes direction of 0006/0007, amends 0025) | Accepted |
| [0054](0054-canonicalizing-symlink-safe-root-containment.md) | Canonicalizing, symlink-safe host-tool root containment: canonicalize `root` once at startup; `resolve_under_root` canonicalizes the resolved target's deepest existing ancestor and requires it under canonical root (blocks final- and middle-component symlink escape for `read`/`edit`/`write` while keeping the create path); `list_files` drops any `glob`/`grep` match whose canonical path escapes — supersedes-by-addition the lexical-only containment of 0008 point 3 (#163) | Accepted |
| [0055](0055-usage-cost-and-stop-reason-surfacing.md) | Usage/cost surfacing: normalize `LlmEvent::Finish` to `{ stop_reason: StopReason, usage: Usage }` (cache dimensions split so each prices once), price it via `ModelPricing::cost_usd`, fold into `SessionUsage`, and emit per-round-trip `OutEvent::Usage { …, cost_usd }`; `MaxTokens` surfaces as a warning; `OutEvent`/`InMsg` drop `Eq` (float cost) (#192) | Accepted |
| [0056](0056-closesession-cascades-over-spawn-subtree.md) | `CloseSession` cascades over the spawn sub-tree: the supervisor breadth-first walks `parent_links` (child→parent) and retires the target plus every transitive descendant — dropping each command channel (→ `SessionEnded`) and tombstoning the id — so an orphaned sub-agent can't keep burning provider tokens with no consumer; explicit-destroy path only, parent `Stop` still does not cascade ([0026](0026-async-subagent-spawn-and-poll.md)) (#180) | Accepted |
| [0057](0057-mid-stream-error-partial-commit-and-retry.md) | Mid-stream LLM error: keep committed context aligned with the display — re-request the turn once if the stream fails before any `TextDelta`/`ReasoningDelta` (`STREAM_RETRIES = 1`, covering the first-byte drop the provider's connect-level retry [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md) can't); if a delta was already shown, commit the partial assistant message with an appended `\n\n[interrupted]` marker (streamed as a final `TextDelta`) before `Error` + `Done`, dropping any half-assembled tool calls — no new wire variant (#181) | Accepted |
| [0058](0058-mid-turn-prompt-folds-into-live-turn.md) | Mid-turn `Prompt` folds into the live turn: drain stashed `Prompt`s into `Context` (`push_user`) at the top of each inner-loop iteration, before the next `LlmRequest`, so mid-turn guidance steers the running turn on the next round-trip instead of replaying as a fresh turn after `Done`; fold site reached only when the prior round called tools, so a post-answer prompt still starts a new turn via the stash; non-`Prompt` commands keep ADR-0018's replay-after-turn discipline (refines [0018](0018-turn-loop-stash-discipline.md), #182) | Accepted |
| [0059](0059-tool-trait-and-registry-live-in-the-runtime.md) | `Tool` trait + `ToolRegistry` move from core to `entanglement-runtime` (re-exported at its crate root): core holds no executable tools, only advertises schemas (`ToolSpec`) and round-trips each call, so the trait/registry are pure runtime vocabulary; delete the dead `Holly.cfg`/`Holly.root` fields and `Session::replay`'s unused `_root`; `ToolSpec`/`ToolCall` stay in provider (re-exported), resolving the placement question 0053 left open — a neutral types crate is the deferred fallback (refines [0006](0006-core-dependency-hygiene-gate.md)/[0010](0010-single-head-crate-and-bash-opt-in.md), resolves [0053](0053-invert-core-provider-seam.md)'s open question, #206) | Accepted |
| [0060](0060-filechange-audit-via-executor-as-path-kind-hash.md) | `OutEvent::FileChange` (dead surface since [0031](0031-write-host-tool-whole-file.md)) is emitted for real by the `tool_runner` executor as `{ session, seq, path, change_kind, hash }` — a SHA-256 of the after-content, never whole-file bytes (which the `broadcast` outbox would clone per subscriber); `edit`/`write` `record` the change into a task-local capture scope the executor stamps with the in-flight call's session/seq, keeping `Tool::run` unchanged; the dead `with_on_edit`/`with_on_write` hooks and `host_tools_with_callbacks` are deleted; `rhai`/direct-`run` paths record nothing (no scope) (part of #200, #202) | Accepted |
| [0061](0061-parked-turn-state-batch-tool-resolution.md) | Parked turn state: the in-flight turn is explicit, serde-serializable session state (`Session.turn: Option<TurnState { pending, iterations }>`) instead of an async-stack continuation — a round ending in tool calls batch-emits every (`ToolCall`, `ToolExec`) pair up front and returns to the session loop, which resolves `ToolResult`s against the pending set in any order (outputs fold on arrival) and re-enters on drain; batch calls thereby execute concurrently (deliberate change from serial in-call-order); replay reconstructs a mid-turn tail as a parked `TurnState` and resume re-offers pending `ToolExec`s at-least-once (fresh `seq`, same `request_id`); the embedder persistence seam is the event log + `Holly::resume` (no snapshot API, no DB in-repo — the runtime JSONL of [0020](0020-event-sourced-session-persistence.md) is the reference); protocol unchanged (refines [0003](0003-agent-and-permission-profiles.md)/[0017](0017-stop-cancels-turn-not-session.md)/[0058](0058-mid-turn-prompt-folds-into-live-turn.md), extends [0020](0020-event-sourced-session-persistence.md), #269-#273, epic #276) | Accepted |
| [0062](0062-collapse-llmsession-placeholder-newtype.md) | Collapse the `LlmSession` placeholder newtype: it wrapped `Box<dyn Llm>`, delegated `stream` straight through, and had an uncalled `inner_mut` — the "per-session retry/rate-limit" state it advertised never existed (that state is **per endpoint**, in the provider's `EndpointPool` keyed by `(base URL, api-key hash)`, deliberately shared across sessions on one endpoint since #217/[0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)). `LlmFactory` becomes `Arc<dyn Fn() -> Box<dyn Llm>>` and `Session::llm` a plain `Box<dyn Llm>`; a session-scoped budget was rejected (would re-fragment what #217 unified). Re-introduce the newtype only when real per-session state arrives (KISS). Resolves the `LlmSession` sub-decision of [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md); provider-internal after [0053](0053-invert-core-provider-seam.md) (#195, epic #190) | Accepted |
| [0063](0063-realtime-model-provider-switch.md) | Realtime model/provider switch with no engine restart: new `InMsg::SetModel { provider, model }` + `OutEvent::ModelChanged`, resolved through a runtime-supplied `EngineConfig::model_resolver: Option<ModelResolver>` (`Arc<dyn Fn(&str,&str) -> Result<ResolvedModel, String>>`) that captures the `Catalog` + warm per-endpoint `HttpClient` (#217) and reuses the startup wire/base/key helpers. The session loop rebuilds `Session::llm` and updates per-session `model`/`generation` + re-budgets the context window (`Context::set_window`); `EngineConfig::pricing` already keys every model so pricing needs no per-session state. Catalog-qualified fields cover model-only and provider switches uniformly; deferred mid-turn like `SetAgent`; replay re-applies it. Chose two `Session` fields over re-introducing the `LlmSession` newtype [0062](0062-collapse-llmsession-placeholder-newtype.md) foresaw (model/generation aren't backend state; KISS). TUI `/model` picker now drives it (#218, epic #190) | Accepted |
| [0064](0064-message-content-blocks.md) | `Message`/`InMsg::Prompt` carry multimodal content blocks: `text: String` → `content: Vec<ContentPart>` (`Text`/`Image` tagged by `type`, `ImageSource::Base64` today) in `entanglement-provider`, threaded through `Context`/`SessionCmd`/the converters. A serde back-compat shim keeps the legacy `text: "…"` shape deserializable (`Message` via a `MessageRepr` `from`, `Prompt::content` via `alias = "text"` + a string-or-array `deserialize_with`) so pre-migration logs still replay; new writes emit `content`. Constructors keep the text ergonomics + normalize empty → no parts; `Message::text()`/`content_text` re-join the text parts. OpenAI renders `image_url`/`data:` URLs, Anthropic `image` base64 blocks (incl. image `tool_result`s). Spawn stays text-only. Migrated early — before persisted logs accumulate — ahead of image capture (#197, epic #196; unblocks #221) | Accepted |
| [0065](0065-read-emits-image-content-blocks.md) | `read` emits image files (`png`/`jpg`/`jpeg`/`gif`/`webp` by extension) as base64 image content blocks: the whole tool-result path goes multimodal, mirroring [0064](0064-message-content-blocks.md)'s `Prompt` migration. `Tool` grows a defaulted `run_content -> Vec<ContentPart>` (only `read` overrides — the other ten tools keep text `run`); `InMsg::ToolResult { output }` → `{ content }` (serde `alias = "output"` shim); `OutEvent::ToolOutput` grows a skip-when-empty `content` field so **replay** rebuilds the image instead of the `[image: …]` display placeholder; `Context::push_tool_content`. OpenAI's `role:"tool"` can't hold an image, so the image rides a trailing `role:"user"` `image_url` message; Anthropic reuses the [0064](0064-message-content-blocks.md) image `tool_result` block array; `rhai` collapses a delegated result to its text parts. Rejected: migrating every tool's `run` (ripple for one producer), text-only `ToolOutput` (loses images on resume), base64-as-text (#221, epic #196) | Accepted |
| [0066](0066-lifecycle-hooks-as-runtime-interceptors.md) | Lifecycle hooks as a **runtime interceptor**, configured in the layered user config (`hooks:` section, `entanglement-runtime::hooks`): `pre_tool_use` (runs at the top of the generic `dispatch`, a non-zero exit **vetoes** the tool before permission), `post_tool_use` (observational, in `run_and_reply` after the result — formatter/telemetry side-effect, can't rewrite the `ToolResult`), and `user_prompt_submit` (fired off the inbound `InMsg::Prompt` fan-out by the executor's `Stop` watcher). Each hook is an `sh -c` child fed a JSON payload on stdin (`{event, session, tool?, input?, output?, prompt?}`) + env vars, run under a timeout in its **own process group** ([0009](0009-edit-and-bash-host-tools.md)/#168); a timeout/spawn-failure fails **closed** on the pre-hook gate. Scoped to the generic `Intercept::Permission` route only (orchestration + `rhai` bypass). New `spawn_tool_executor_with_hooks` seam; the 4-arg `spawn_tool_executor` stays a no-hook wrapper; inbound subscription hoisted synchronous so a first `Prompt` can't race the watcher. Rejected: hooks in core (crate-seam violation), result-rewriting post-hook (multimodal surface), dylib plugin ABI (over-scoped), folding the veto into permission (static grade vs dynamic policy) (#199, epic #196) | Accepted |
| [0067](0067-mcp-client-as-runtime-tool-provider.md) | MCP client as a **runtime-side tool provider** — attach external tool servers with **no core change** ([0059](0059-tool-trait-and-registry-live-in-the-runtime.md)): each configured server is spawned as a subprocess speaking JSON-RPC 2.0 over **stdio** (newline-framed; `initialize`/`notifications/initialized` handshake, then `tools/list`/`tools/call`), and every discovered tool is registered into the same `ToolRegistry` as a `dyn Tool` (`mcp::McpTool`), so its schema rides `EngineConfig.tool_specs` and execution round-trips over `ToolExec`/`ToolResult` like any host tool — governed by the same permission profiles. Advertised name `mcp__<server>__<tool>` (sanitized to `^[A-Za-z0-9_-]+$`). Servers declared in the layered user config's `mcp:` map (`{command, args, env, disabled}`); `build_config` goes `async` and calls `mcp::connect` before deriving `tool_specs`. Best-effort per server (a down server is logged + skipped, never fatal); a 60s request timeout + EOF-drains-pending keep a hung/crashed server from parking a turn. Lives in the lean library (tokio process + `serde_json`). Rejected: a new core tool-source seam (registry already is one), HTTP/SSE first (stdio is canonical), single `mcp` dispatcher tool (strips per-tool schema/permission), inlining image content (text-only v1), blocking connect that fails startup (#198, epic #196) | Accepted |
| [0068](0068-shared-per-session-seq-counter.md) | Shared per-session `seq` counter (`Session.seq: Arc<AtomicU64>` in a supervisor-held `SeqRegistry`): the runtime mints a **fresh** seq for events it authors while a session is parked — approval `ToolRequest`/`UserQuestion`, `Plan`/`TaskList` snapshot, `FileChange` — via a new `Holly::emit_for_session`, instead of reusing the parked `ToolExec` seq, so `(session, seq)` is unique across every authored event (unblocks a strict `seq > last` WS-reconnect dedupe). Seq-less `Status` goes through `Holly::emit_status`; the raw `Holly::events()` sender is removed. Supervisor lifecycle errors mint from the live counter, or carry `seq == 0` for an id with no live session — a value core never mints, rendered unconditionally by the seq-`0` bypass (fixes the TUI-invisible supervisor errors, absorbs #159). Rejected: `Option<u64>` seq exemption (loses ordering; content snapshots still need a shared counter), a runtime-local high-watermark (collides on un-park) (refines [0002](0002-session-multiplexed-protocol.md), rides [0061](0061-parked-turn-state-batch-tool-resolution.md), #157/#159) | Accepted |
| [0069](0069-trusted-untrusted-wire-frame-split.md) | Trusted/untrusted wire-frame split: `InMsg::wire_allowed()` allowlist refuses the runtime-authored trio (`ToolResult`/`Spawn`/`Resume`) at a new untrusted `Holly::send_from_wire` entry (`WireError`), so a forged `ToolResult` can't resolve a parked turn (bypassing execution + permission) nor a forged `Spawn` bypass `spawn_refusal`; the executor folds results back over the privileged `Holly::submit_tool_result` handle, `pipe` calls `send_from_wire`. Local single-user scope ([0048](0048-serve-head-local-trust-model.md)) makes this robustness/UX (P2); WS `send_from_wire` + per-connection `Approve` ownership deferred to #153. Rejected: a second privileged inbox, per-frame origin tokens, `#[serde(skip)]` on the trio, supervisor-side filtering (#155) | Accepted |
| [0070](0070-authoritative-tool-exec-profile-and-fail-closed-fallback.md) | Authoritative `ToolExec` profile + fail-closed fallback + lag-proof decision delivery: the executor's per-session profile map was folded from the **lossy** `SessionStarted`/`AgentChanged` broadcast with a fail-*open* default (unseen ⇒ `Allow`/unmasked), so a dropped lifecycle frame under burst ran a restricted session allow-all — the security posture inverted under overload. `OutEvent::ToolExec` now carries `agent: String` (the emitting session's profile name, `serde` default for old logs); the executor **self-heals** `active` from it before gating, so the leaf is authoritative regardless of the drop. `permission_for`/`tool_masked` now fail **closed** (`Deny`/masked) for the residual unknown. Parked approvals register a `oneshot` in a `PendingDecisions` map keyed by `(session, request_id)`; a single light inbound router fans each `Approve`/`Reject`/`AnswerQuestion` to its waiter — replacing the per-task `broadcast` park that silently dropped a lagged decision (request parked forever). Rejected: a core-authoritative shared profile registry (new seam, ancestors still broadcast-folded), fixing only the lag, a `Lagged`-recovery mailbox (reinvents the registry) (#156, part of #153) | Accepted |
| [0071](0071-parked-turn-reoffer-timer.md) | In-process parked-turn re-offer timer + executor `request_id` dedupe: `OutEvent::ToolExec` rides the lossy outbound `broadcast`, so an executor that lags (`RecvError::Lagged`) drops an offer and strands the parked turn until a restart/resume. Core arms a timer while a turn is parked (`EngineConfig::reoffer_interval: Option<Duration>`, default 60s; the parked `rx.recv()` becomes a `tokio::time::timeout`) and, after that much *silence* (no `ToolResult`), re-emits every `TurnState::pending` call via the same `emit_tool_exec` resume uses (same `request_id`, fresh `seq`). Sound **only** because the runtime executor now dedupes: a per-session `HashSet<request_id>` of **in-flight** ids (single-threaded loop → lock-free), skipping a re-offer of a call still running, dropped again on the resolving `ToolOutput` (so a later round may reuse the id — core matches by id only within a round's pending set) and on `SessionEnded` — so a re-offer to a merely-slow executor is a no-op, not a double-run. At-least-once like resume (extends [0061](0061-parked-turn-state-batch-tool-resolution.md)); supersedes the closed #154. Rejected: a dedicated reliable `mpsc` for `ToolExec` (#154's direction — splits the outbound contract), the [0070](0070-authoritative-tool-exec-profile-and-fail-closed-fallback.md) pending-registry shape (`ToolExec` is fan-out content, not a point-to-point decision), a shorter default (fires on slow legit tools) (#274, part of #153) | Accepted |
| [0072](0072-protocol-warts-settled-before-serve.md) | Protocol warts settled before `serve` freezes the wire: expedients that are cosmetic in-process become expensive once external clients pin the JSON. `ListSessions`/`SessionList` carry a `correlation_id: String` instead of an overloaded `SessionId`, so `InMsg::session()`/`OutEvent::session()` return `Option<&SessionId>` (`None` for these session-less queries; the TUI router drops a `None` rather than keying a phantom view). `OutEvent::seq()` returns `Option<u64>` — `None` for lifecycle/query events — so the real seq-`0` supervisor-error sentinel ([0068](0068-shared-per-session-seq-counter.md)) is a distinct `Some(0)`. A new `AgentState::WaitingAnswer` is emitted while an `ask_user` question parks, leaving `WaitingApproval` to genuine permission decisions; cancel paths already emit `Status::Idle`. `msg_to_cmd` returns `Option<SessionCmd>` (log-and-drop) instead of an `unreachable!` that would panic the whole supervisor. New `InMsg::ReplayFrom { session, correlation_id, after_seq }` (wire-allowed) is answered **out-of-core** by a runtime history responder that reads the persistence log and broadcasts `OutEvent::History { correlation_id, session, events }` (content past the cursor) via a seq-less `Holly::emit_history`; neither query nor reply is persisted/replayed. Rejected: keep overloading `SessionId`, leave `seq()` returning `0`, a dedicated `Stopped` ack (Idle already acks), answer `ReplayFrom` in core (core holds no log), per-connection replay now (needs the WS connection registry — #153) (#160, part of #153) | Accepted |
| [0073](0073-managed-env-file-writer-and-key-surfaces.md) | One shared writer for the managed provider-key env file (#220), with a CLI + TUI surface. The file was scaffold-and-read only; setting a key meant hand-editing it. `config::env_key` adds a pure `upsert(text, key, value) -> String` (replace the first *live* `KEY=` line — first-occurrence-wins, matching `load()` — else the `#KEY=`/`# KEY=` placeholder, else append; other lines preserved byte-for-byte; idempotent) + `set_key(key, value) -> Result<PathBuf>` (loud error when there's no managed path; create from `template` when missing; atomic temp-file-in-dir + rename; `0o600` on unix; reject empty/`\n` values). `skutter config set-key <provider> [--key V]` (pre-engine fast path, `config::keys`, behind `cli`+`provider`) resolves the catalog `key_env` (keyless Ollama → clean error), sources the value from `--key`, a hidden `rpassword` prompt, or piped stdin — never echoed — and warns when the process env already holds a *different* value (env > file). The TUI `/key` dialog (`tui::key_dialog`) is a two-stage modal after the `/model` pattern (provider list → masked input, `masked()` renders bullets only); on submit it calls `set_key(...)` then `std::env::set_var(...)` so the live model resolver ([0063](0063-realtime-model-provider-switch.md)) binds the key on the next `/model` switch — no restart — recording a status line (never the key) into the transcript, `Esc` wipes the buffer. `env_key` is pure std+anyhow (lean/gate-clean); only `keys` is feature-gated. Rejected: write from each surface directly (duplicates the line rules), a dotenv lib (won't preserve comments/order byte-for-byte), non-atomic rewrite (corrupts the credential file on a crash), send `SetModel` on submit (rebinds the LLM as a side effect of setting a key) (#304, part of #302) | Accepted |
| [0074](0074-cross-vendor-skill-and-agent-discovery.md) | Cross-vendor skill & agent discovery: the shared layered loader (#204) scans Claude Code's and the `.agents` convention's dirs alongside the native ones — user layer `~/.claude/<kind>` (lenient) < `${config_dir}/entanglement/<kind>` (strict); project layer `.claude/<kind>` < `.agents/<kind>` (both lenient) < `.entanglement/<kind>` (strict) — same `Layer` slots, native wins on a `name` collision, `.agents` outranks `.claude` matching the brief chain (cross-vendor beats vendor-specific). Foreign dirs parse through permissive mirror structs (only `name`+`description`; Claude's `disable-model-invocation` → `user_only`, its `allowed-tools` dropped; foreign agents default `mode: all` so they are spawnable delegation targets) and a malformed foreign file is warned-and-skipped — files entanglement doesn't own must not abort the load; native dirs keep `deny_unknown_fields` + abort. `ENTANGLEMENT_{SKILLS,AGENTS}_DIR` replaces the *whole* user layer (test hermeticity + the opt-out). Trust unchanged per [0047](0047-local-trust-boundary.md); mitigation stays inspect provenance (the `replaces=` log now names the shadowed file). Rejected: extra `Layer` variants per dir, lenient-everywhere, config-file opt-in | Accepted |
| [0075](0075-provider-side-web-search-mvp.md) | Provider-side web search MVP, opt-in, **zero core/protocol change**. Both first-class providers execute a search **mid-turn, server-side** (no client tool round-trip): z.ai via a `{"type":"web_search","web_search":{…}}` `tools` entry; Anthropic via a `{"type":"web_search_20250305","name":"web_search"}` server tool streaming `server_tool_use` + `web_search_tool_result` blocks (the Anthropic parser dropped both before). Modeled as **client-construction-time config**, not a client tool, so the frozen wire ([0069](0069-trusted-untrusted-wire-frame-split.md)/[0072](0072-protocol-warts-settled-before-serve.md)) is untouched: `WebSearchConfig { enabled, max_uses, allowed_domains }` (leaf `entanglement-provider::web_search`, `deny_unknown_fields`, re-exported through core so the lean runtime lib names it without the `provider` feature), a `#[serde(default)] web_search:` section on the layered `config.yml` (`mcp:` pattern), threaded as `Option<WebSearchConfig>` (`Some` only when `enabled`) into both client factories **and** the live-switch resolver ([0063](0063-realtime-model-provider-switch.md)) so startup and `/model` bind identically. Results surface on the **reasoning channel** (`LlmEvent::Reasoning` → `OutEvent::ReasoningDelta`, **not** committed to history): Anthropic `server_tool_use` tracked with `is_server`, its query emitted as `Reasoning` on stop — **never** a `ToolCall` — and `web_search_tool_result`/error rendered as `Reasoning`; z.ai's cited text already flows as `Text`, the `web_search` source array parsed **defensively** (streaming placement unverified → worst case = cited-text-only floor). Permission ladder bypassed **by design**: the config file is trusted ([0047](0047-local-trust-boundary.md)), so opting in *is* consent. MVP limits: not persisted into history (citations + Anthropic search-cache pricing lost across turns, follow-up on [0064](0064-message-content-blocks.md)); `pause_turn` ends the turn; `_20260209` needs a `ModelEntry` capability flag. Rejected: first-class `WebSearch` protocol events + core-driven tool (breaks the frozen wire, models server execution as a client round-trip), a client-side host tool (re-implements provider-native search, loses citation), on-by-default (cost/privacy → must be explicit opt-in that doubles as consent) (#305, part of #302) | Accepted |
| [0076](0076-per-session-dynamic-tool-specs.md) | Per-session dynamic tool specs: `EngineConfig.tool_spec_resolver: Option<Arc<dyn Fn(&SessionId) -> Vec<ToolSpec> + Send + Sync>>` (type alias `ToolSpecResolver`) lets one `Holly` advertise a different base tool surface per session — the seam multi-tenant embedding needs (#307) so user A's discovered MCP-server tools ([0067](0067-mcp-client-as-runtime-tool-provider.md)) never leak into user B's sessions and a site's `enabled_mcp_server_ids` restriction is expressible, without one engine per user. Consulted fresh at every turn build in `run_round` (`session/turn.rs`), so a backing-store edit lands on the **next turn** with no respawn; its output **replaces** the engine-global `tool_specs` for that session (embedder composes if it wants both), `profile_tool_specs` ([0040](0040-per-profile-spawn-control.md)) still append, and the profile mask ([0038](0038-physical-per-agent-tool-restriction.md)) still filters the result — the resolver **widens discovery, never bypasses masking** (it runs *before* the mask). Sync `Fn` by design (turn hot path); documented pattern is an embedder-owned `Arc<RwLock<..>>` snapshot cache. `None` default = engine-global specs, a pure no-op for single-user heads; no protocol/wire change (a construction-time knob like `model_resolver` [0063](0063-realtime-model-provider-switch.md)). Rejected: merge-in-core (forces one composition policy, can't narrow), a static per-session `HashMap` baked at spawn (can't reflect a mid-session edit), an `async` resolver (I/O on the turn path), one engine per tenant (the status quo cost), resolving *after* the mask (turns the resolver into a permission bypass) (#308, part of #307) | Accepted |
| [0077](0077-session-hibernation-evictable-resumable.md) | Session hibernation — a third lifecycle state between `live` and the terminal tombstone: new **trusted-only** `InMsg::HibernateSession { session }` (`Holly::hibernate`) evicts a session's task + `Context` **without** tombstoning the id, so `Holly::resume` rebuilds it from the embedder's event log exactly like the restart path (re-offering a turn parked mid-approval, [0061](0061-parked-turn-state-batch-tool-resolution.md)/[0071](0071-parked-turn-reoffer-timer.md)). The supervisor cascades the spawn sub-tree like `CloseSession` but skips the `closed` set (memory released, gone from `ListSessions`, id stays resumable); the session task emits a distinct lifecycle `OutEvent::SessionHibernated { session, ts }` (no `seq`) — the executor releases its per-session bookkeeping on it as on `SessionEnded`. Not wire-allowed ([0069](0069-trusted-untrusted-wire-frame-split.md)): a wire head can't evict another session. Mid-stream hibernate = **stop-then-hibernate** (the sender-drop cancels the round; the uncommitted text-only tail is dropped exactly as `Session::replay` drops it, so resume is lossless vs the log); parked-on-approval safe via re-offer. Core snapshots nothing — rebuild is the embedder's log replay (no DB in core, [0061](0061-parked-turn-state-batch-tool-resolution.md)). `EngineConfig.idle_ttl` auto-hibernation deferred (needs outbound-activity tracking the supervisor lacks; left to the embedder's policy). Rejected: reuse `CloseSession` + clear the tombstone on resume (conflates spent vs evicted, #105), refuse-during-stream (needs turn state the supervisor lacks; loses nothing over stop-then-hibernate), core-side snapshot/restore (duplicates the embedder log in core) (#318, part of #307) | Accepted |
| [0078](0078-per-turn-dynamic-system-prompt.md) | Per-turn dynamic system prompt: `EngineConfig.system_prompt_resolver: Option<Arc<dyn Fn(&SessionId, &AgentProfile) -> Option<String> + Send + Sync>>` (type alias `SystemPromptResolver`) lets an embedder whose prompt is user-editable content — a site serving its prompt from a CMS page — override the active profile's `system_prompt` per turn, so an edit lands on the **next turn** with no engine respawn (which would tear down every live session). Sibling of [0076](0076-per-session-dynamic-tool-specs.md): same construction-time-resolver seam, applied to the prompt instead of the tool surface. Consulted fresh in `run_round` (`session/turn.rs`), resolved once and threaded into `stream_round` where `s.profile.system_prompt` was read directly; a `Some(prompt)` return **overrides**, `None` (or an absent resolver) falls back to the profile prompt. The `Fn` sees `(&SessionId, &AgentProfile)` — the running session's *own* id + resolved profile — so sub-agent turns resolve against **that child's** profile (per-profile prompts keep working), an embedder keys off the root session for tenant context, and passing the profile lets a resolver compose (prepend a preamble) rather than only replace. Sync `Fn` by design (turn hot path); documented pattern is an embedder-owned `Arc<RwLock<HashMap<SessionId, String>>>` snapshot cache. `None` default = profile's static prompt, a pure no-op for single-user heads; no protocol/wire change (a construction-time knob like `model_resolver` [0063](0063-realtime-model-provider-switch.md)/`tool_spec_resolver` [0076](0076-per-session-dynamic-tool-specs.md)). Rejected: a `system_prompt` field on `InMsg::Prompt`/`SetAgent` (puts the prompt on the wire, lets an untrusted head set it, [0069](0069-trusted-untrusted-wire-frame-split.md)), a mutable session field pushed by a new `InMsg` (wire + state for an embedder-owned lookup, still stale vs an external edit), an `async` resolver (I/O on the turn path), respawn-on-edit (the status quo cost), reusing `tool_spec_resolver`'s profile-less signature (breaks per-profile sub-agent prompts) (#310, part of #307) | Accepted |
| [0079](0079-pluggable-permission-resolver-and-grant-store.md) | Pluggable permission policy in the tool executor: `spawn_tool_executor_with_policy(…, resolver: Arc<dyn PermissionResolver>, grants: Arc<dyn GrantStore>, …)` (module `entanglement-runtime::policy`) drives two trait objects so a multi-tenant embedder (#307) swaps its DB-backed allow/deny/prompt rules + "always allow" writes for the CLI defaults **without forking the ~350-line executor** — keeping the interception ladder, spawn/mask gating, hooks, rhai, plan/tasks. `PermissionResolver::resolve(session, tool, input) → Permission` (async, DB-friendly) decides one session's **own** grade; the executor snapshots the ancestor chain (`permission::ancestor_chain`) and takes the least-privileged grade across it (`resolve_effective`), so the ADR-0024 privilege ceiling + spawn/mask gating stay **on top of** the resolver (a tenant rule can't widen a child beyond its parent). `GrantStore` has async `record` (an `ApprovalScope::Always` may hit a DB) + sync `is_granted` (a multi-tenant store no-ops the read and resolves grants through its resolver on the next call). Defaults are **byte-identical**: `ProfileResolver` shares the executor's `Arc<Mutex<active-profile map>>` and returns own-profile clamped by the base ceiling (monotonic `clamp_to_base` ⇒ min-of-clamped = the pre-seam `effective_permission` + clamp); `DefaultGrantStore` wraps the managed-file store (`grants::GrantStore` renamed `FileGrantStore`). `rhai` keeps the profile/base path (its inner bindings are a separate sync mechanism). Rejected: resolver walks the chain itself (custom resolver would skip the ADR-0024 clamp), a single `record_always` method (too narrow for the default file store), sync-in-loop resolution (serializes dispatch behind one DB call) (#311, part of #307) | Accepted |
| [0080](0080-mcp-streamable-http-transport.md) | MCP streamable-HTTP transport with per-server headers/auth: `McpClient` becomes an enum over `Stdio` ([0067](0067-mcp-client-as-runtime-tool-provider.md)/#198) and a new `Http` transport, chosen per server by a `command` XOR `url` config (`McpServerConfig::transport()` rejects both-set/neither-set); `McpTool` unchanged (it holds `Arc<McpClient>` and only calls `list_tools`/`call_tool`). `mcp::http::HttpClient` `POST`s JSON-RPC with `Accept: application/json, text/event-stream`, draining an SSE response until the matching JSON-RPC `id` or decoding a lone JSON body; static per-server `headers` (`${VAR}`-expanded from env) authenticate every request; an `Mcp-Session-Id` from `initialize` + the negotiated `MCP-Protocol-Version` are echoed on later requests. Behind a new `mcp-http` feature (`reqwest`+`futures`, in `default`) so the lean/`--no-default-features` build stays transport-free ([0025](0025-runtime-cargo-feature-gates.md)); a `url:` server on a build without the feature logs-and-skips. `HttpClient`/`StdioClient`/`Transport` are public so a multi-tenant embedder ([0076](0076-per-session-dynamic-tool-specs.md)) can assemble per-user registries with per-user tokens, bypassing YAML. Enabling a server is consent ([0047](0047-local-trust-boundary.md)); no new permission surface. Internal split: `mcp::stdio` (former `client.rs`, `StdioClient`) + `mcp::http` (gated) + slimmed `mcp::client` (enum + shared JSON-RPC helpers). Rejected: a `transport:`-tag/untagged enum (bare `command`/`url` gives clearer XOR errors), unconditional `reqwest` (web stack in the lean surface), a generic `Transport` trait (premature over two transports), a provider-style connection pool (low-rate tool calls; reqwest pooling suffices) (#312, epic #307) | Accepted |
| [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md) | Per-agent-profile provider/model pinning + rebind on `SetAgent`. `AgentProfile` gains `provider: Option<String>` (`#[serde(default)]`) beside `model`; both set = a *model pin* (`AgentProfile::model_pin()`), `model` alone stays the legacy request-level fallback, `provider` without `model` is a loud load error. The rebind lives in **core's `SetAgent`** (one locus for Tab cycle / `/agent` / `--agent` / spawn / wire; replay consistent for free), driven off the existing `model_resolver` seam ([0063](0063-realtime-model-provider-switch.md)); core stays policy-free (the **runtime** injects persisted-file>frontmatter into the assembled profile). `Session` gains `provider` (no-op guard so an already-correct binding isn't rebuilt) + `profile_models` (per-profile session memory of `/model` choices); the `SetModel` success arm is factored into `Session::rebind`. Precedence: **session memory > static pin > keep current binding** (a pin-less profile with no memory emits no `ModelChanged`, a live override survives an agent switch); session-start applies the pin when `s.model` is unset (covers default `build` + spawned sub-agents like a cheap `explore`); replay reconstructs `profile_models`/`provider` from `ModelChanged`. Persistence: a **managed** `${config_dir}/entanglement/agent-models.yml` (override `ENTANGLEMENT_AGENT_MODELS_FILE`, `BTreeMap` for stable output), `AgentModelStore { load, get, set, apply }` overlaid onto the registry at startup; `atomic_write` extracted into shared `config::atomic`. TUI persists on the confirming `ModelChanged` (matched to a `pending_model_persist`), never on `Error` or a `SetAgent`-driven `ModelChanged`. Rejected: head-driven follow-up `SetModel` (every head re-implements it, races the switch, replay needs a synthesized follow-up), a layered `config.yml` section (runtime-rewritten machine state doesn't belong in the hand-edited config, like grants [0052](0052-approval-scope-and-persisted-grants.md)), live registry mutation (concurrency hazard for no gain; the pin lives on `Session` via the resolver) (#323, part of #302) | Accepted |
| [0082](0082-single-shot-session-ops-and-persisted-compaction.md) | Single-shot session ops (`InMsg::Oneshot { session, op: String, args: Value }`) with session compaction as the first op. Generic **wire envelope**, not a plugin registry: `session::ops::run_oneshot` is a plain `match op.as_str()`, so a future op needs no new `InMsg` variant/`wire_allowed`/`SessionCmd` — just a new match arm. `SessionCmd::Oneshot` uses the same defer-via-stash gate as `SetAgent`/`SetModel` (never runs concurrently with a live turn) and `run_oneshot` reuses `&mut *s.llm` directly (sound only because of that gate) via a tool-less `LlmRequest` (`tools: &[]`), draining the stream for text + `Finish` usage. `"compact"` renders the transcript, summarizes it, and calls the new `Context::apply_compaction(summary, kept)` — split off the trailing `kept` messages, clear, push one **user-role** summary message (`system` has no in-history wire mapping; a user-authored summary is trusted more reliably than an assistant-authored one), re-append the tail. The result is `OutEvent::Compacted { session, seq, summary, kept }` — a **persisted, seq-bearing** content event, so the existing variant-agnostic persistence tap and `ReplayFrom` history responder cover it for free; `Session::replay`'s new `Compacted` arm flushes pending buffers (like the `Done` arm) then calls the same `apply_compaction`, so live and replayed sessions converge. `kept` always `0` in v1 (keep-tail deferred — a `Tool` message replayed without its assistant parent breaks Anthropic's `tool_use`/`tool_result` pairing) but ships as real wire surface. The old prune-only `Context::compact` (#178) is unchanged, still the automatic pre-round fallback; `"compact"` only runs on request (`InMsg::Oneshot`, TUI `/compact`) — auto-summarize-on-threshold is a follow-up. Rejected: a typed op enum (re-opens the wire per op; the ADR-0072 wire freeze), runtime-side execution (no handle to mutate core's private `Context`), a generic `OneshotResult` envelope (replay needs to pattern-match `Compacted` specifically to call `apply_compaction`), streaming the summary as `TextDelta` (that channel belongs to turn output), keep-tail in v1 (turn-boundary detection not built yet) (#324) | Accepted |
| [0083](0083-in-app-tool-allowlist-editing-as-user-layer-materialization.md) | In-app tool-allowlist editing materializes a **user-layer override**, not a new config surface: the layered agent loader ([0034](0034-file-defined-agent-profiles.md)) already shadows a same-`name` definition (built-in included), so editing just needs to *write* the shadow. `agents::materialize::save_tools_override(root, name, allowed)` resolves the currently effective definition's raw text (`winning_raw_text`, same precedence as `load_registry`), rewrites only `tools:`/`disallowed_tools:` via a `serde_yaml::Mapping` round-trip (`rewrite_tools` — order-preserving, everything else untouched), and writes atomically via `config::atomic::atomic_write` to `${config_dir}/entanglement/agents/<name>.md` (or `ENTANGLEMENT_AGENTS_DIR`). TUI: `e` on the `/agent` picker's highlighted profile opens a checklist dialog (`tui::tools_dialog::ToolsDialog`) over the full advertised tool roster (`EngineConfig.tool_specs`, captured before `Holly::spawn` consumes it), seeded from the profile's current effective mask; `Space` toggles, `Enter` saves, `Esc` discards; applies on next restart (no live registry reload yet). Rejected: a managed sibling file like [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md)'s `agent-models.yml` (a tool mask is already part of the definition, unlike a model pin — a second store would fight the layered loader for authority over the same field), byte-level line-splicing like `config::env_key::upsert` (YAML sequences need a YAML-aware rewrite, not single-line KEY=VALUE splicing) (#330) | Accepted |
| [0084](0084-runtime-live-reload-and-managed-file-locking.md) | Runtime-side live reload (inotify) + advisory locking for managed files. `config::lock::with_locked_file(path, f)` takes an exclusive `fd-lock` on `path`'s sibling `.lock` file (never the target — `atomic_write` replaces it wholesale) and runs `f`, which re-reads the current on-disk state itself (read-current → merge → write → adopt); `grants::FileGrantStore::record`/`config::agent_models::AgentModelStore::set`/`config::env_key::set_key` all route their managed read-modify-write through it, so two `skutter` instances no longer clobber each other's `Always` grant / `/model` pin / provider key. `write_grants` migrates onto the shared `atomic_write`. A new `watch.rs` (`spawn_debounced_watcher`, a pure primitive over `notify_debouncer_mini`, independently unit-tested) watches every resolvable agent/skill dir plus `${config_dir}/entanglement/` and `<root>/.entanglement/`; on a debounced change it reloads skills+agents and swaps the result into **runtime-held mirrors** (`watch::LiveDefinitions { profiles, skills, agent_models, grants }`, all `Arc<RwLock<..>>`/`Arc<Mutex<..>>`) — never core's `EngineConfig.profiles`, which stays pinned for the process lifetime per the [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md) "live registry mutation is a rejected design" precedent. `tool_runner::spawn_tool_executor_with_policy`'s `profiles` param and `skills::load_skill::LoadSkillTool`'s `registry` field both move onto these live handles (brief read locks per lookup); the TUI's `/agent` picker + Tab-cycle roster refresh on a reload notice (`App::refresh_profiles`/`record_reload_status`). A live session's already-resolved system prompt/tool mask is unaffected — only the next `SetAgent`/new session/picker pick sees the edit. Rejected: live-mutate `EngineConfig.profiles` in core (the ADR-0081 concurrency-hazard-for-no-gain reasoning applies identically), a wire-level reload `InMsg` (a runtime-internal mirror swap needs no core protocol change), polling instead of inotify (wastes CPU, adds latency, `notify`/`notify-debouncer-mini`/`fd-lock` are already hygiene-gate-clean) (#329) | Accepted |
| [0085](0085-gemini-native-wire-and-opaque-provider-meta.md) | Native Gemini wire + opaque `provider_meta` on `ToolCall`: a third provider wire (`Wire::Gemini`) beside the OpenAI-compat and Anthropic clients ([0007](0007-streaming-llm-and-provider-crate.md)), needed because Gemini's OpenAI-compat endpoint can't round-trip the `thoughtSignature` a 2.5 thinking model attaches to each function call — the compat surface doesn't expose it, so multi-turn tool use with a thinking model 4xxs on replayed history. Two changes: (1) a generic `ToolCall.provider_meta: Option<serde_json::Value>` — provider-private state that round-trips verbatim through persistence + replay, never inspected by core, using the [0064](0064-message-content-blocks.md) back-compat shim so old logs replay unchanged (`ToolCall`/`LlmEvent`/`LlmResponse` drop `Eq`, staying `PartialEq`, since `serde_json::Value` isn't `Eq`); (2) `GeminiLlm` implementing `Llm::stream` against `:streamGenerateContent?alt=sse` (candidate parts → `Text`/`Reasoning`/`ToolCall`, `usageMetadata` → `Usage`, `finishReason` → `StopReason`), stashing/restoring `thoughtSignature` via `provider_meta`, reusing the shared `HttpClient` pool/retry/rate-limit ([0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)). `ToolCall.id` is set to the function name (Gemini matches `functionResponse` by name), so same-name parallel calls in one turn share an id — a known, accepted edge, not silently wrong (dedupe is by `request_id`). Rejected: routing Gemini through `wire: openai` + `base_url` (silently breaks thinking-model tool loops), a Gemini-specific `thought_signature` field instead of a generic slot (solves only this vendor) (#309, part of #307) | Accepted |
| [0086](0086-recordsink-pluggable-persistence-append-target.md) | `RecordSink` — pluggable persistence append target: splits the persistence tap's *what to persist* (event-sourcing logic — route each record to its root session, tombstone broadcast-lag gaps, #104) from its *where to persist* (JSONL file), so a Postgres-backed embedder (#307) implements one method and inherits the tap's gap/lag handling instead of forking the ~subscriber. `pub trait RecordSink { fn append(&self, root: &SessionId, record: &LogRecord) -> anyhow::Result<()>; }`, `spawn_persistence_subscriber_with_sink(holly, sink: Arc<dyn RecordSink>)`; the existing `spawn_persistence_subscriber(holly, cwd)` becomes a thin wrapper over it + a new `FileSink` — file behavior byte-identical, existing tests unchanged. `append` is sync by design (the tap's receive loop isn't async-blocked on persistence); a blocking backing store must put a bounded channel + writer task behind it and return immediately, or it starves the broadcast receiver and manufactures the very `Gap` tombstones the trait exists to avoid — same sync-boundary-plus-documented-buffering shape as the `tool_spec_resolver`/`system_prompt_resolver` `Fn` seams ([0076](0076-per-session-dynamic-tool-specs.md)/[0078](0078-per-turn-dynamic-system-prompt.md)). `session_store::read`/`pair_records` stay file-side only — `Holly::resume` already accepts records from anywhere, so no read-side trait is needed. Rejected: an `async` `append` (slows the common file case for the rare DB one), a `Write`-based sink (JSONL framing is `session_store`'s concern), a mirroring `RecordSource` read trait (no embedder need identified) (#313, part of #307) | Accepted |
