# Architecture Decision Records

An **ADR** is a short, immutable record of *why* an architecture decision was
made. The [architecture doc](../architecture.md) describes the current state
(*what is*); ADRs are the decision log (*how we got here, and what else we
considered*). The two run in parallel: a decision lands here first, then the
arch doc is updated to reflect it.

## When to write one

Write an ADR for any decision that is **hard to reverse** or that a reader would
reasonably ask *"why?"* about — protocol shapes, crate boundaries, a chosen
pattern over an obvious alternative, security/permission models. Don't write one
for trivial refactors or local naming.

## Format

File name: `NNNN-kebab-case-title.md` (zero-padded, monotonically numbered).
Each record:

```
# NNNN. Title
- Status: Proposed | Accepted | Superseded by ADR-XXXX | Deprecated
- Date: YYYY-MM-DD

## Context
Why this came up — forces, constraints, what the reference projects do.

## Decision
What we chose, precisely.

## Consequences
Positive / negative / neutral effects.

## Alternatives considered
The options rejected and why. (This is the part the arch doc can't carry.)
```

## Status lifecycle

`Proposed` → `Accepted` → (`Superseded by ADR-XXXX` | `Deprecated`). Never edit
an accepted ADR in place — supersede it with a new one that links back.

## Index

| # | Title | Status |
| --- | --- | --- |
| [0001](0001-actor-model-abi.md) | Actor model is the integration ABI | Accepted |
| [0002](0002-session-multiplexed-protocol.md) | Session-multiplexed wire protocol | Accepted — id minting amended by [0164](0164-short-sortable-kind-tagged-ids.md) |
| [0003](0003-agent-and-permission-profiles.md) | Agent + permission profiles (opencode-style) | Accepted |
| [0004](0004-structured-plan-and-task-events.md) | Structured Plan & TaskList events (profiles + events, both) | Accepted (`TaskList` half superseded by [0039](0039-markdown-task-list.md)) |
| [0005](0005-ndjson-stdio-head.md) | NDJSON stdio head (`run` + `pipe`) | Accepted |
| [0006](0006-core-dependency-hygiene-gate.md) | Layering: core / provider / runtime + core hygiene gate | Accepted (direction superseded by [0053](0053-invert-core-provider-seam.md)) |
| [0007](0007-streaming-llm-and-provider-crate.md) | `entanglement-provider`: streaming `Llm` trait, pooling, retry, rate-limit, reasoning | Accepted (trait placement superseded by [0053](0053-invert-core-provider-seam.md)) |
| [0008](0008-host-tools-workdir-and-bounded-output.md) | Host tools: working-directory root + bounded output | Accepted (lexical-only containment superseded-by-addition by [0054](0054-canonicalizing-symlink-safe-root-containment.md); output bounds converged by [0161](0161-unified-async-work-background-flag-and-one-poll.md)) |
| [0009](0009-edit-and-bash-host-tools.md) | Host tools: `edit` (search/replace) and `bash` (subprocess + timeout) | Accepted |
| [0010](0010-single-head-crate-and-bash-opt-in.md) | `entanglement-runtime`: the head crate — tools, execution, permissions, sessions | Accepted (§3's `call`-piggyback framing superseded by [0093](0093-call-registration-independent-of-bash-opt-in.md); `bash` opt-in itself unchanged) |
| [0011](0011-tui-head-ratatui-crossterm.md) | TUI head: ratatui + crossterm in `entanglement-runtime` | Accepted |
| [0012](0012-tui-event-buffering-rendering.md) | TUI event-buffering & rendering model | Accepted |
| [0013](0013-keybinding-leader-which-key.md) | Keybinding scheme: leader key + which-key | Accepted |
| [0014](0014-tool-approval-inline-modal.md) | Tool approval UX: inline card vs modal | Accepted |
| [0015](0015-rich-text-pipeline-syntect.md) | Rich-text pipeline: pulldown-cmark → ratatui Text, syntect for code blocks | Accepted |
| [0016](0016-host-tool-empty-result-contract.md) | Host tools: empty-result contract (no silent zero-output) | Accepted — Amended by [0150](0150-search-tool-cli-ergonomics.md) |
| [0017](0017-stop-cancels-turn-not-session.md) | `InMsg::Stop` cancels the turn, not the session | Accepted — Amended by [0144] (pause/resume) |
| [0018](0018-turn-loop-stash-discipline.md) | Turn-loop command stash discipline | Accepted |
| 0019 | — _(number skipped; no ADR-0019)_ | — |
| [0020](0020-event-sourced-session-persistence.md) | Event-sourced session persistence | Accepted |
| [0021](0021-hierarchical-session-model.md) | Hierarchical session data model | Accepted |
| [0022](0022-subagent-spawn.md) | Sub-agent spawn and parent→child answer relay | Accepted |
| [0023](0023-subagent-spawn-limits.md) | Sub-agent spawn recursion / fan-out limits | Accepted — Amended by [0138](0138-sponsored-build-child-and-propose-plan-cycle.md) |
| [0024](0024-subagent-permission-gating.md) | Sub-agent spawn permission gating and privilege ceiling | Accepted — Amended by [0137](0137-explore-ask-grade-shell-access.md), [0138](0138-sponsored-build-child-and-propose-plan-cycle.md) |
| [0025](0025-runtime-cargo-feature-gates.md) | `entanglement-runtime` cargo feature gates (`cli`/`tui`) for lean library embedding | Accepted (lean-transport claim amended by [0053](0053-invert-core-provider-seam.md)) |
| [0026](0026-async-subagent-spawn-and-poll.md) | Non-blocking sub-agent spawn with handle + `agent_poll` | Accepted — launch/join **tool split** superseded by [0161](0161-unified-async-work-background-flag-and-one-poll.md) |
| [0027](0027-ask-user-interactive-prompt.md) | `ask_user` tool — model-driven user decision prompt | Accepted — Amended by [0127](0127-ask-user-v2-multi-question-envelope.md), [0146](0146-ask-user-list-retract-replace.md) |
| [0028](0028-session-lifecycle-enumeration-and-backpressure.md) | Session lifecycle: enumeration, explicit close, non-blocking routing | Accepted |
| [0029](0029-external-editor-and-markdown-export.md) | External `$EDITOR` compose + Markdown transcript export | Accepted |
| [0030](0030-tui-file-mentions-and-bash-passthrough.md) | TUI `@file` mention completion + `!bash` passthrough | Accepted |
| [0031](0031-write-host-tool-whole-file.md) | Host tool `write`: whole-file create/overwrite (quartet → quintet) | Accepted |
| [0032](0032-yaml-provider-model-catalog.md) | YAML provider/model catalog: embedded defaults + user override | Accepted |
| [0033](0033-agent-tool-family-and-blocking-agent.md) | `agent_*` tool family: rename `spawn_agent` → `agent_spawn`, add blocking `agent` | Accepted — separate-tools rationale superseded by [0161](0161-unified-async-work-background-flag-and-one-poll.md) |
| [0034](0034-file-based-agent-definitions.md) | File-based agent definitions: discovery, frontmatter, registry | Accepted |
| [0035](0035-deterministic-system-prompt-assembly.md) | Deterministic system-prompt assembly: preamble + body + brief + env + skills | Accepted |
| [0036](0036-skill-discovery-and-registry.md) | Skill discovery + registry: SKILL.md frontmatter, tier-1 disclosure | Accepted |
| [0037](0037-load-skill-tool-deterministic-resolution.md) | `load_skill` tool: deterministic resolution + path substitution | Accepted |
| [0038](0038-physical-per-agent-tool-restriction.md) | Physical per-agent tool restriction: allowlist/denylist masks specs + dispatch | Accepted — Amended by [0137](0137-explore-ask-grade-shell-access.md), [0148](0148-glob-patterns-in-the-agent-tool-mask.md) |
| [0039](0039-markdown-task-list.md) | Markdown task list: structured `Vec<TaskItem>` → plain snapshot | Accepted |
| [0040](0040-per-profile-spawn-control.md) | Per-profile spawn control: `can_spawn` + spawnable-agents allowlist + target-mode gate | Accepted |
| [0041](0041-update-plan-ownership-default-closed.md) | `update_plan` ownership: `owns_plan` default-closed plan authority + physical read-only `plan` | Superseded by [0049](0049-plan-task-tools-as-runtime-state-tools.md) — Amended by [0142](0142-trusted-scratch-dir-and-plans-folder-carve-outs.md) |
| [0042](0042-plan-acceptance-via-propose-plan-approval-roundtrip.md) | Plan acceptance via `propose_plan` approval round-trip: accept → fresh root `build` session (head-policy handoff) | Accepted — Amended by [0138](0138-sponsored-build-child-and-propose-plan-cycle.md), [0145](0145-one-plan-tool-file-backed-plans-and-blocking-review-loop.md) |
| [0043](0043-skill-preload-vs-access-independent-mechanisms.md) | Skill preload (`skills:` frontmatter) vs access (`load_skill` tool mask) as two independent agent-definition mechanisms | Accepted |
| [0044](0044-agents-skills-system-prompt-epic-synthesis.md) | Agents, skills & system prompt — epic synthesis: six principles → enforcement map, disclosure tiers, enforcement-locus split, deferred follow-ups | Accepted |
| [0045](0045-call-host-tool-argv-exec-tailed-output.md) | Host tool `call`: argv exec (no shell) with auto-tailed output (`tail=30`, `tail=0` = full) | Accepted (§3 registration-gate reasoning superseded by [0093](0093-call-registration-independent-of-bash-opt-in.md); output bounds converged by [0161](0161-unified-async-work-background-flag-and-one-poll.md)) |
| [0046](0046-rhai-sandboxed-script-tool.md) | `rhai` host tool: embedded capability-sandboxed script engine; quintet bindings permission-checked per call via a sync/async bridge | Amended by [0115](0115-rhai-exec-bindings-call-bash.md) |
| [0047](0047-local-trust-boundary.md) | Local trust boundary: repo is trusted, config precedence system < user < repo, inspection over enforcement | Accepted |
| [0048](0048-serve-head-local-trust-model.md) | `serve` head: local-only WebSocket protocol interface (Vue SPA primary/non-exclusive; browser surface out of scope; loopback + opt-in handshake) | Accepted |
| [0049](0049-plan-task-tools-as-runtime-state-tools.md) | `update_plan`/`update_tasks` as runtime state tools: out of core, gated by the ordinary permission path; plan authorship default-closed via explicit allowlist membership (supersedes 0041) | Partially superseded by [0145](0145-one-plan-tool-file-backed-plans-and-blocking-review-loop.md) (`update_plan` half only) — Amended by [0142](0142-trusted-scratch-dir-and-plans-folder-carve-outs.md) |
| [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md) | Provider resilience keyed **per `(endpoint, api-key)`**: RPM budget + `Retry-After` window per base URL + key hash (isolates throttling; multiple keys each get their own limit); retry classifies the response status inside the loop (fixes dead-code #193); `LlmSession` references per-endpoint state (#195) | Accepted (both accepted "Negative/neutral" consequences — endpoint-spelling splits, `DefaultHasher` instability — fixed by [0156](0156-normalize-and-stabilize-the-endpoint-pool-key.md); widened beyond LLM traffic by [0157](0157-mcp-http-transport-shares-the-endpoint-pool.md)) |
| [0051](0051-argument-scoped-permission-rules.md) | Argument-scoped permission rule keys `tool(pattern)`: `PermissionProfile::resolve(name, arg)` + dependency-free `*`/`?` glob in core; runtime extracts the argument (command for `bash`/`call`, path for `edit`/`write`/`read`) and threads it through the ancestor clamp, config ceiling, and rhai bindings (#173) | Accepted |
| [0052](0052-approval-scope-and-persisted-grants.md) | Approval scope `Once \| Session \| Always` on `InMsg::Approve` (default `Once`, wire-additive) + a runtime `GrantStore` that upgrades a resolved `Ask` → `Allow` for an exact `(tool, arg)` already granted (never overrides `Deny`, not sub-agent-inherited); `Always` persists to a managed `grants.yml` sibling of `config.yml`, not its ceiling `permissions` section (#174) | Accepted |
| [0053](0053-invert-core-provider-seam.md) | Invert the core↔provider seam: `entanglement-provider` becomes a leaf owning the `Llm` trait + DTOs + `Message` (usable standalone for raw LLM); `entanglement-core` depends on provider and is no longer transport-free; `make tree`/`make check-lean` gates narrowed to UI/web-server + CLI/TUI (supersedes direction of 0006/0007, amends 0025) | Accepted |
| [0054](0054-canonicalizing-symlink-safe-root-containment.md) | Canonicalizing, symlink-safe host-tool root containment: canonicalize `root` once at startup; `resolve_under_root` canonicalizes the resolved target's deepest existing ancestor and requires it under canonical root (blocks final- and middle-component symlink escape for `read`/`edit`/`write` while keeping the create path); `list_files` drops any `glob`/`grep` match whose canonical path escapes — supersedes-by-addition the lexical-only containment of 0008 point 3 (#163) | Accepted |
| [0055](0055-usage-cost-and-stop-reason-surfacing.md) | Usage/cost surfacing: normalize `LlmEvent::Finish` to `{ stop_reason: StopReason, usage: Usage }` (cache dimensions split so each prices once), price it via `ModelPricing::cost_usd`, fold into `SessionUsage`, and emit per-round-trip `OutEvent::Usage { …, cost_usd }`; `MaxTokens` surfaces as a warning; `OutEvent`/`InMsg` drop `Eq` (float cost) (#192) | Accepted |
| [0056](0056-closesession-cascades-over-spawn-subtree.md) | `CloseSession` cascades over the spawn sub-tree: the supervisor breadth-first walks `parent_links` (child→parent) and retires the target plus every transitive descendant — dropping each command channel (→ `SessionEnded`) and tombstoning the id — so an orphaned sub-agent can't keep burning provider tokens with no consumer; explicit-destroy path only, parent `Stop` still does not cascade ([0026](0026-async-subagent-spawn-and-poll.md)) (#180) | Accepted |
| [0057](0057-mid-stream-error-partial-commit-and-retry.md) | Mid-stream LLM error: keep committed context aligned with the display — re-request the turn once if the stream fails before any `TextDelta`/`ReasoningDelta` (`STREAM_RETRIES = 1`, covering the first-byte drop the provider's connect-level retry [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md) can't); if a delta was already shown, commit the partial assistant message with an appended `\n\n[interrupted]` marker (streamed as a final `TextDelta`) before `Error` + `Done`, dropping any half-assembled tool calls — no new wire variant (#181) | Accepted |
| [0058](0058-mid-turn-prompt-folds-into-live-turn.md) | Mid-turn `Prompt` folds into the live turn: drain stashed `Prompt`s into `Context` (`push_user`) at the top of each inner-loop iteration, before the next `LlmRequest`, so mid-turn guidance steers the running turn on the next round-trip instead of replaying as a fresh turn after `Done`; fold site reached only when the prior round called tools, so a post-answer prompt still starts a new turn via the stash; non-`Prompt` commands keep ADR-0018's replay-after-turn discipline (refines [0018](0018-turn-loop-stash-discipline.md), #182) | Accepted |
| [0059](0059-tool-trait-and-registry-live-in-the-runtime.md) | `Tool` trait + `ToolRegistry` move from core to `entanglement-runtime` (re-exported at its crate root): core holds no executable tools, only advertises schemas (`ToolSpec`) and round-trips each call, so the trait/registry are pure runtime vocabulary; delete the dead `Holly.cfg`/`Holly.root` fields and `Session::replay`'s unused `_root`; `ToolSpec`/`ToolCall` stay in provider (re-exported), resolving the placement question 0053 left open — a neutral types crate is the deferred fallback (refines [0006](0006-core-dependency-hygiene-gate.md)/[0010](0010-single-head-crate-and-bash-opt-in.md), resolves [0053](0053-invert-core-provider-seam.md)'s open question, #206) | Accepted |
| [0060](0060-filechange-audit-via-executor-as-path-kind-hash.md) | `OutEvent::FileChange` (dead surface since [0031](0031-write-host-tool-whole-file.md)) is emitted for real by the `tool_runner` executor as `{ session, seq, path, change_kind, hash }` — a SHA-256 of the after-content, never whole-file bytes (which the `broadcast` outbox would clone per subscriber); `edit`/`write` `record` the change into a task-local capture scope the executor stamps with the in-flight call's session/seq, keeping `Tool::run` unchanged; the dead `with_on_edit`/`with_on_write` hooks and `host_tools_with_callbacks` are deleted; `rhai`/direct-`run` paths record nothing (no scope) (part of #200, #202) | Accepted |
| [0061](0061-parked-turn-state-batch-tool-resolution.md) | Parked turn state: the in-flight turn is explicit, serde-serializable session state (`Session.turn: Option<TurnState { pending, iterations }>`) instead of an async-stack continuation — a round ending in tool calls batch-emits every (`ToolCall`, `ToolExec`) pair up front and returns to the session loop, which resolves `ToolResult`s against the pending set in any order (outputs fold on arrival) and re-enters on drain; batch calls thereby execute concurrently (deliberate change from serial in-call-order); replay reconstructs a mid-turn tail as a parked `TurnState` and resume re-offers pending `ToolExec`s at-least-once (fresh `seq`, same `request_id`); the embedder persistence seam is the event log + `Holly::resume` (no snapshot API, no DB in-repo — the runtime JSONL of [0020](0020-event-sourced-session-persistence.md) is the reference); protocol unchanged (refines [0003](0003-agent-and-permission-profiles.md)/[0017](0017-stop-cancels-turn-not-session.md)/[0058](0058-mid-turn-prompt-folds-into-live-turn.md), extends [0020](0020-event-sourced-session-persistence.md), #269-#273, epic #276) | Accepted |
| [0062](0062-collapse-llmsession-placeholder-newtype.md) | Collapse the `LlmSession` placeholder newtype: it wrapped `Box<dyn Llm>`, delegated `stream` straight through, and had an uncalled `inner_mut` — the "per-session retry/rate-limit" state it advertised never existed (that state is **per endpoint**, in the provider's `EndpointPool` keyed by `(base URL, api-key hash)`, deliberately shared across sessions on one endpoint since #217/[0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)). `LlmFactory` becomes `Arc<dyn Fn() -> Box<dyn Llm>>` and `Session::llm` a plain `Box<dyn Llm>`; a session-scoped budget was rejected (would re-fragment what #217 unified). Re-introduce the newtype only when real per-session state arrives (KISS). Resolves the `LlmSession` sub-decision of [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md); provider-internal after [0053](0053-invert-core-provider-seam.md) (#195, epic #190) | Accepted |
| [0063](0063-realtime-model-provider-switch.md) | Realtime model/provider switch with no engine restart: new `InMsg::SetModel { provider, model }` + `OutEvent::ModelChanged`, resolved through a runtime-supplied `EngineConfig::model_resolver: Option<ModelResolver>` (`Arc<dyn Fn(&str,&str) -> Result<ResolvedModel, String>>`) that captures the `Catalog` + warm per-endpoint `HttpClient` (#217) and reuses the startup wire/base/key helpers. The session loop rebuilds `Session::llm` and updates per-session `model`/`generation` + re-budgets the context window (`Context::set_window`); `EngineConfig::pricing` already keys every model so pricing needs no per-session state. Catalog-qualified fields cover model-only and provider switches uniformly; deferred mid-turn like `SetAgent`; replay re-applies it. Chose two `Session` fields over re-introducing the `LlmSession` newtype [0062](0062-collapse-llmsession-placeholder-newtype.md) foresaw (model/generation aren't backend state; KISS). TUI `/model` picker now drives it (#218, epic #190) | Accepted |
| [0064](0064-message-content-blocks.md) | `Message`/`InMsg::Prompt` carry multimodal content blocks: `text: String` → `content: Vec<ContentPart>` (`Text`/`Image` tagged by `type`, `ImageSource::Base64` today) in `entanglement-provider`, threaded through `Context`/`SessionCmd`/the converters. A serde back-compat shim keeps the legacy `text: "…"` shape deserializable (`Message` via a `MessageRepr` `from`, `Prompt::content` via `alias = "text"` + a string-or-array `deserialize_with`) so pre-migration logs still replay; new writes emit `content`. Constructors keep the text ergonomics + normalize empty → no parts; `Message::text()`/`content_text` re-join the text parts. OpenAI renders `image_url`/`data:` URLs, Anthropic `image` base64 blocks (incl. image `tool_result`s). Spawn stays text-only. Migrated early — before persisted logs accumulate — ahead of image capture (#197, epic #196; unblocks #221) | Accepted |
| [0065](0065-read-emits-image-content-blocks.md) | `read` emits image files (`png`/`jpg`/`jpeg`/`gif`/`webp` by extension) as base64 image content blocks: the whole tool-result path goes multimodal, mirroring [0064](0064-message-content-blocks.md)'s `Prompt` migration. `Tool` grows a defaulted `run_content -> Vec<ContentPart>` (only `read` overrides — the other ten tools keep text `run`); `InMsg::ToolResult { output }` → `{ content }` (serde `alias = "output"` shim); `OutEvent::ToolOutput` grows a skip-when-empty `content` field so **replay** rebuilds the image instead of the `[image: …]` display placeholder; `Context::push_tool_content`. OpenAI's `role:"tool"` can't hold an image, so the image rides a trailing `role:"user"` `image_url` message; Anthropic reuses the [0064](0064-message-content-blocks.md) image `tool_result` block array; `rhai` collapses a delegated result to its text parts. Rejected: migrating every tool's `run` (ripple for one producer), text-only `ToolOutput` (loses images on resume), base64-as-text (#221, epic #196) | Accepted |
| [0066](0066-lifecycle-hooks-as-runtime-interceptors.md) | Lifecycle hooks as a **runtime interceptor**, configured in the layered user config (`hooks:` section, `entanglement-runtime::hooks`): `pre_tool_use` (runs at the top of the generic `dispatch`, a non-zero exit **vetoes** the tool before permission), `post_tool_use` (observational, in `run_and_reply` after the result — formatter/telemetry side-effect, can't rewrite the `ToolResult`), and `user_prompt_submit` (fired off the inbound `InMsg::Prompt` fan-out by the executor's `Stop` watcher). Each hook is an `sh -c` child fed a JSON payload on stdin (`{event, session, tool?, input?, output?, prompt?}`) + env vars, run under a timeout in its **own process group** ([0009](0009-edit-and-bash-host-tools.md)/#168); a timeout/spawn-failure fails **closed** on the pre-hook gate. Scoped to the generic `Intercept::Permission` route only (orchestration + `rhai` bypass). New `spawn_tool_executor_with_hooks` seam; the 4-arg `spawn_tool_executor` stays a no-hook wrapper; inbound subscription hoisted synchronous so a first `Prompt` can't race the watcher. Rejected: hooks in core (crate-seam violation), result-rewriting post-hook (multimodal surface), dylib plugin ABI (over-scoped), folding the veto into permission (static grade vs dynamic policy) (#199, epic #196) | Accepted |
| [0067](0067-mcp-client-as-runtime-tool-provider.md) | MCP client as a **runtime-side tool provider** — attach external tool servers with **no core change** ([0059](0059-tool-trait-and-registry-live-in-the-runtime.md)): each configured server is spawned as a subprocess speaking JSON-RPC 2.0 over **stdio** (newline-framed; `initialize`/`notifications/initialized` handshake, then `tools/list`/`tools/call`), and every discovered tool is registered into the same `ToolRegistry` as a `dyn Tool` (`mcp::McpTool`), so its schema rides `EngineConfig.tool_specs` and execution round-trips over `ToolExec`/`ToolResult` like any host tool — governed by the same permission profiles. Advertised name `mcp__<server>__<tool>` (sanitized to `^[A-Za-z0-9_-]+$`). Servers declared in the layered user config's `mcp:` map (`{command, args, env, disabled}`); `build_config` goes `async` and calls `mcp::connect` before deriving `tool_specs`. Best-effort per server (a down server is logged + skipped, never fatal); a 60s request timeout + EOF-drains-pending keep a hung/crashed server from parking a turn. Lives in the lean library (tokio process + `serde_json`). Rejected: a new core tool-source seam (registry already is one), HTTP/SSE first (stdio is canonical), single `mcp` dispatcher tool (strips per-tool schema/permission), inlining image content (text-only v1), blocking connect that fails startup (#198, epic #196) | Accepted |
| [0068](0068-shared-per-session-seq-counter.md) | Shared per-session `seq` counter (`Session.seq: Arc<AtomicU64>` in a supervisor-held `SeqRegistry`): the runtime mints a **fresh** seq for events it authors while a session is parked — approval `ToolRequest`/`UserQuestion`, `Plan`/`TaskList` snapshot, `FileChange` — via a new `Holly::emit_for_session`, instead of reusing the parked `ToolExec` seq, so `(session, seq)` is unique across every authored event (unblocks a strict `seq > last` WS-reconnect dedupe). Seq-less `Status` goes through `Holly::emit_status`; the raw `Holly::events()` sender is removed. Supervisor lifecycle errors mint from the live counter, or carry `seq == 0` for an id with no live session — a value core never mints, rendered unconditionally by the seq-`0` bypass (fixes the TUI-invisible supervisor errors, absorbs #159). Rejected: `Option<u64>` seq exemption (loses ordering; content snapshots still need a shared counter), a runtime-local high-watermark (collides on un-park) (refines [0002](0002-session-multiplexed-protocol.md), rides [0061](0061-parked-turn-state-batch-tool-resolution.md), #157/#159) | Accepted |
| [0069](0069-trusted-untrusted-wire-frame-split.md) | Trusted/untrusted wire-frame split: `InMsg::wire_allowed()` allowlist refuses the runtime-authored trio (`ToolResult`/`Spawn`/`Resume`) at a new untrusted `Holly::send_from_wire` entry (`WireError`), so a forged `ToolResult` can't resolve a parked turn (bypassing execution + permission) nor a forged `Spawn` bypass `spawn_refusal`; the executor folds results back over the privileged `Holly::submit_tool_result` handle, `pipe` calls `send_from_wire`. Local single-user scope ([0048](0048-serve-head-local-trust-model.md)) makes this robustness/UX (P2); WS `send_from_wire` + per-connection `Approve` ownership deferred to #153. Rejected: a second privileged inbox, per-frame origin tokens, `#[serde(skip)]` on the trio, supervisor-side filtering (#155) | Amended by [0124](0124-wire-refused-mcp-mutation-and-stdio-key-scrub.md) |
| [0070](0070-authoritative-tool-exec-profile-and-fail-closed-fallback.md) | Authoritative `ToolExec` profile + fail-closed fallback + lag-proof decision delivery: the executor's per-session profile map was folded from the **lossy** `SessionStarted`/`AgentChanged` broadcast with a fail-*open* default (unseen ⇒ `Allow`/unmasked), so a dropped lifecycle frame under burst ran a restricted session allow-all — the security posture inverted under overload. `OutEvent::ToolExec` now carries `agent: String` (the emitting session's profile name, `serde` default for old logs); the executor **self-heals** `active` from it before gating, so the leaf is authoritative regardless of the drop. `permission_for`/`tool_masked` now fail **closed** (`Deny`/masked) for the residual unknown. Parked approvals register a `oneshot` in a `PendingDecisions` map keyed by `(session, request_id)`; a single light inbound router fans each `Approve`/`Reject`/`AnswerQuestion` to its waiter — replacing the per-task `broadcast` park that silently dropped a lagged decision (request parked forever). Rejected: a core-authoritative shared profile registry (new seam, ancestors still broadcast-folded), fixing only the lag, a `Lagged`-recovery mailbox (reinvents the registry) (#156, part of #153) | Accepted |
| [0071](0071-parked-turn-reoffer-timer.md) | In-process parked-turn re-offer timer + executor `request_id` dedupe: `OutEvent::ToolExec` rides the lossy outbound `broadcast`, so an executor that lags (`RecvError::Lagged`) drops an offer and strands the parked turn until a restart/resume. Core arms a timer while a turn is parked (`EngineConfig::reoffer_interval: Option<Duration>`, default 60s; the parked `rx.recv()` becomes a `tokio::time::timeout`) and, after that much *silence* (no `ToolResult`), re-emits every `TurnState::pending` call via the same `emit_tool_exec` resume uses (same `request_id`, fresh `seq`). Sound **only** because the runtime executor now dedupes: a per-session `HashSet<request_id>` of **in-flight** ids (single-threaded loop → lock-free), skipping a re-offer of a call still running, dropped again on the resolving `ToolOutput` (so a later round may reuse the id — core matches by id only within a round's pending set) and on `SessionEnded` — so a re-offer to a merely-slow executor is a no-op, not a double-run. At-least-once like resume (extends [0061](0061-parked-turn-state-batch-tool-resolution.md)); supersedes the closed #154. Rejected: a dedicated reliable `mpsc` for `ToolExec` (#154's direction — splits the outbound contract), the [0070](0070-authoritative-tool-exec-profile-and-fail-closed-fallback.md) pending-registry shape (`ToolExec` is fan-out content, not a point-to-point decision), a shorter default (fires on slow legit tools) (#274, part of #153) | Accepted |
| [0072](0072-protocol-warts-settled-before-serve.md) | Protocol warts settled before `serve` freezes the wire: expedients that are cosmetic in-process become expensive once external clients pin the JSON. `ListSessions`/`SessionList` carry a `correlation_id: String` instead of an overloaded `SessionId`, so `InMsg::session()`/`OutEvent::session()` return `Option<&SessionId>` (`None` for these session-less queries; the TUI router drops a `None` rather than keying a phantom view). `OutEvent::seq()` returns `Option<u64>` — `None` for lifecycle/query events — so the real seq-`0` supervisor-error sentinel ([0068](0068-shared-per-session-seq-counter.md)) is a distinct `Some(0)`. A new `AgentState::WaitingAnswer` is emitted while an `ask_user` question parks, leaving `WaitingApproval` to genuine permission decisions; cancel paths already emit `Status::Idle`. `msg_to_cmd` returns `Option<SessionCmd>` (log-and-drop) instead of an `unreachable!` that would panic the whole supervisor. New `InMsg::ReplayFrom { session, correlation_id, after_seq }` (wire-allowed) is answered **out-of-core** by a runtime history responder that reads the persistence log and broadcasts `OutEvent::History { correlation_id, session, events }` (content past the cursor) via a seq-less `Holly::emit_history`; neither query nor reply is persisted/replayed. Rejected: keep overloading `SessionId`, leave `seq()` returning `0`, a dedicated `Stopped` ack (Idle already acks), answer `ReplayFrom` in core (core holds no log), per-connection replay now (needs the WS connection registry — #153) (#160, part of #153) | Accepted — Amended by [0139](0139-visible-session-states-working-and-waitingagent.md) |
| [0073](0073-managed-env-file-writer-and-key-surfaces.md) | One shared writer for the managed provider-key env file (#220), with a CLI + TUI surface. The file was scaffold-and-read only; setting a key meant hand-editing it. `config::env_key` adds a pure `upsert(text, key, value) -> String` (replace the first *live* `KEY=` line — first-occurrence-wins, matching `load()` — else the `#KEY=`/`# KEY=` placeholder, else append; other lines preserved byte-for-byte; idempotent) + `set_key(key, value) -> Result<PathBuf>` (loud error when there's no managed path; create from `template` when missing; atomic temp-file-in-dir + rename; `0o600` on unix; reject empty/`\n` values). `skutter config set-key <provider> [--key V]` (pre-engine fast path, `config::keys`, behind `cli`+`provider`) resolves the catalog `key_env` (keyless Ollama → clean error), sources the value from `--key`, a hidden `rpassword` prompt, or piped stdin — never echoed — and warns when the process env already holds a *different* value (env > file). The TUI `/key` dialog (`tui::key_dialog`) is a two-stage modal after the `/model` pattern (provider list → masked input, `masked()` renders bullets only); on submit it calls `set_key(...)` then `std::env::set_var(...)` so the live model resolver ([0063](0063-realtime-model-provider-switch.md)) binds the key on the next `/model` switch — no restart — recording a status line (never the key) into the transcript, `Esc` wipes the buffer. `env_key` is pure std+anyhow (lean/gate-clean); only `keys` is feature-gated. Rejected: write from each surface directly (duplicates the line rules), a dotenv lib (won't preserve comments/order byte-for-byte), non-atomic rewrite (corrupts the credential file on a crash), send `SetModel` on submit (rebinds the LLM as a side effect of setting a key) (#304, part of #302) | Accepted |
| [0074](0074-cross-vendor-skill-and-agent-discovery.md) | Cross-vendor skill & agent discovery: the shared layered loader (#204) scans Claude Code's and the `.agents` convention's dirs alongside the native ones — user layer `~/.claude/<kind>` (lenient) < `${config_dir}/entanglement/<kind>` (strict); project layer `.claude/<kind>` < `.agents/<kind>` (both lenient) < `.entanglement/<kind>` (strict) — same `Layer` slots, native wins on a `name` collision, `.agents` outranks `.claude` matching the brief chain (cross-vendor beats vendor-specific). Foreign dirs parse through permissive mirror structs (only `name`+`description`; Claude's `disable-model-invocation` → `user_only`, its `allowed-tools` dropped; foreign agents default `mode: all` so they are spawnable delegation targets) and a malformed foreign file is warned-and-skipped — files entanglement doesn't own must not abort the load; native dirs keep `deny_unknown_fields` + abort. `ENTANGLEMENT_{SKILLS,AGENTS}_DIR` replaces the *whole* user layer (test hermeticity + the opt-out). Trust unchanged per [0047](0047-local-trust-boundary.md); mitigation stays inspect provenance (the `replaces=` log now names the shadowed file). Rejected: extra `Layer` variants per dir, lenient-everywhere, config-file opt-in | Accepted |
| [0075](0075-provider-side-web-search-mvp.md) | Provider-side web search MVP, opt-in, **zero core/protocol change**. Both first-class providers execute a search **mid-turn, server-side** (no client tool round-trip): z.ai via a `{"type":"web_search","web_search":{…}}` `tools` entry; Anthropic via a `{"type":"web_search_20250305","name":"web_search"}` server tool streaming `server_tool_use` + `web_search_tool_result` blocks (the Anthropic parser dropped both before). Modeled as **client-construction-time config**, not a client tool, so the frozen wire ([0069](0069-trusted-untrusted-wire-frame-split.md)/[0072](0072-protocol-warts-settled-before-serve.md)) is untouched: `WebSearchConfig { enabled, max_uses, allowed_domains }` (leaf `entanglement-provider::web_search`, `deny_unknown_fields`, re-exported through core so the lean runtime lib names it without the `provider` feature), a `#[serde(default)] web_search:` section on the layered `config.yml` (`mcp:` pattern), threaded as `Option<WebSearchConfig>` (`Some` only when `enabled`) into both client factories **and** the live-switch resolver ([0063](0063-realtime-model-provider-switch.md)) so startup and `/model` bind identically. Results surface on the **reasoning channel** (`LlmEvent::Reasoning` → `OutEvent::ReasoningDelta`, **not** committed to history): Anthropic `server_tool_use` tracked with `is_server`, its query emitted as `Reasoning` on stop — **never** a `ToolCall` — and `web_search_tool_result`/error rendered as `Reasoning`; z.ai's cited text already flows as `Text`, the `web_search` source array parsed **defensively** (streaming placement unverified → worst case = cited-text-only floor). Permission ladder bypassed **by design**: the config file is trusted ([0047](0047-local-trust-boundary.md)), so opting in *is* consent. MVP limits: not persisted into history (citations + Anthropic search-cache pricing lost across turns, follow-up on [0064](0064-message-content-blocks.md)); `pause_turn` ends the turn; `_20260209` needs a `ModelEntry` capability flag. Rejected: first-class `WebSearch` protocol events + core-driven tool (breaks the frozen wire, models server execution as a client round-trip), a client-side host tool (re-implements provider-native search, loses citation), on-by-default (cost/privacy → must be explicit opt-in that doubles as consent) (#305, part of #302) | Amended by [0131](0131-web-search-post-mvp-follow-ups.md), [0171](0171-zai-streaming-web-search-placement-confirmed.md) |
| [0076](0076-per-session-dynamic-tool-specs.md) | Per-session dynamic tool specs: `EngineConfig.tool_spec_resolver: Option<Arc<dyn Fn(&SessionId) -> Vec<ToolSpec> + Send + Sync>>` (type alias `ToolSpecResolver`) lets one `Holly` advertise a different base tool surface per session — the seam multi-tenant embedding needs (#307) so user A's discovered MCP-server tools ([0067](0067-mcp-client-as-runtime-tool-provider.md)) never leak into user B's sessions and a site's `enabled_mcp_server_ids` restriction is expressible, without one engine per user. Consulted fresh at every turn build in `run_round` (`session/turn.rs`), so a backing-store edit lands on the **next turn** with no respawn; its output **replaces** the engine-global `tool_specs` for that session (embedder composes if it wants both), `profile_tool_specs` ([0040](0040-per-profile-spawn-control.md)) still append, and the profile mask ([0038](0038-physical-per-agent-tool-restriction.md)) still filters the result — the resolver **widens discovery, never bypasses masking** (it runs *before* the mask). Sync `Fn` by design (turn hot path); documented pattern is an embedder-owned `Arc<RwLock<..>>` snapshot cache. `None` default = engine-global specs, a pure no-op for single-user heads; no protocol/wire change (a construction-time knob like `model_resolver` [0063](0063-realtime-model-provider-switch.md)). Rejected: merge-in-core (forces one composition policy, can't narrow), a static per-session `HashMap` baked at spawn (can't reflect a mid-session edit), an `async` resolver (I/O on the turn path), one engine per tenant (the status quo cost), resolving *after* the mask (turns the resolver into a permission bypass) (#308, part of #307) | Accepted |
| [0077](0077-session-hibernation-evictable-resumable.md) | Session hibernation — a third lifecycle state between `live` and the terminal tombstone: new **trusted-only** `InMsg::HibernateSession { session }` (`Holly::hibernate`) evicts a session's task + `Context` **without** tombstoning the id, so `Holly::resume` rebuilds it from the embedder's event log exactly like the restart path (re-offering a turn parked mid-approval, [0061](0061-parked-turn-state-batch-tool-resolution.md)/[0071](0071-parked-turn-reoffer-timer.md)). The supervisor cascades the spawn sub-tree like `CloseSession` but skips the `closed` set (memory released, gone from `ListSessions`, id stays resumable); the session task emits a distinct lifecycle `OutEvent::SessionHibernated { session, ts }` (no `seq`) — the executor releases its per-session bookkeeping on it as on `SessionEnded`. Not wire-allowed ([0069](0069-trusted-untrusted-wire-frame-split.md)): a wire head can't evict another session. Mid-stream hibernate = **stop-then-hibernate** (the sender-drop cancels the round; the uncommitted text-only tail is dropped exactly as `Session::replay` drops it, so resume is lossless vs the log); parked-on-approval safe via re-offer. Core snapshots nothing — rebuild is the embedder's log replay (no DB in core, [0061](0061-parked-turn-state-batch-tool-resolution.md)). `EngineConfig.idle_ttl` auto-hibernation deferred (needs outbound-activity tracking the supervisor lacks; left to the embedder's policy). Rejected: reuse `CloseSession` + clear the tombstone on resume (conflates spent vs evicted, #105), refuse-during-stream (needs turn state the supervisor lacks; loses nothing over stop-then-hibernate), core-side snapshot/restore (duplicates the embedder log in core) (#318, part of #307) | Accepted — Amended by [0168](0168-lazy-prompt-refuses-a-known-hibernated-child.md) (`parent_links` survives hibernation) |
| [0078](0078-per-turn-dynamic-system-prompt.md) | Per-turn dynamic system prompt: `EngineConfig.system_prompt_resolver: Option<Arc<dyn Fn(&SessionId, &AgentProfile) -> Option<String> + Send + Sync>>` (type alias `SystemPromptResolver`) lets an embedder whose prompt is user-editable content — a site serving its prompt from a CMS page — override the active profile's `system_prompt` per turn, so an edit lands on the **next turn** with no engine respawn (which would tear down every live session). Sibling of [0076](0076-per-session-dynamic-tool-specs.md): same construction-time-resolver seam, applied to the prompt instead of the tool surface. Consulted fresh in `run_round` (`session/turn.rs`), resolved once and threaded into `stream_round` where `s.profile.system_prompt` was read directly; a `Some(prompt)` return **overrides**, `None` (or an absent resolver) falls back to the profile prompt. The `Fn` sees `(&SessionId, &AgentProfile)` — the running session's *own* id + resolved profile — so sub-agent turns resolve against **that child's** profile (per-profile prompts keep working), an embedder keys off the root session for tenant context, and passing the profile lets a resolver compose (prepend a preamble) rather than only replace. Sync `Fn` by design (turn hot path); documented pattern is an embedder-owned `Arc<RwLock<HashMap<SessionId, String>>>` snapshot cache. `None` default = profile's static prompt, a pure no-op for single-user heads; no protocol/wire change (a construction-time knob like `model_resolver` [0063](0063-realtime-model-provider-switch.md)/`tool_spec_resolver` [0076](0076-per-session-dynamic-tool-specs.md)). Rejected: a `system_prompt` field on `InMsg::Prompt`/`SetAgent` (puts the prompt on the wire, lets an untrusted head set it, [0069](0069-trusted-untrusted-wire-frame-split.md)), a mutable session field pushed by a new `InMsg` (wire + state for an embedder-owned lookup, still stale vs an external edit), an `async` resolver (I/O on the turn path), respawn-on-edit (the status quo cost), reusing `tool_spec_resolver`'s profile-less signature (breaks per-profile sub-agent prompts) (#310, part of #307) | Accepted |
| [0079](0079-pluggable-permission-resolver-and-grant-store.md) | Pluggable permission policy in the tool executor: `spawn_tool_executor_with_policy(…, resolver: Arc<dyn PermissionResolver>, grants: Arc<dyn GrantStore>, …)` (module `entanglement-runtime::policy`) drives two trait objects so a multi-tenant embedder (#307) swaps its DB-backed allow/deny/prompt rules + "always allow" writes for the CLI defaults **without forking the ~350-line executor** — keeping the interception ladder, spawn/mask gating, hooks, rhai, plan/tasks. `PermissionResolver::resolve(session, tool, input) → Permission` (async, DB-friendly) decides one session's **own** grade; the executor snapshots the ancestor chain (`permission::ancestor_chain`) and takes the least-privileged grade across it (`resolve_effective`), so the ADR-0024 privilege ceiling + spawn/mask gating stay **on top of** the resolver (a tenant rule can't widen a child beyond its parent). `GrantStore` has async `record` (an `ApprovalScope::Always` may hit a DB) + sync `is_granted` (a multi-tenant store no-ops the read and resolves grants through its resolver on the next call). Defaults are **byte-identical**: `ProfileResolver` shares the executor's `Arc<Mutex<active-profile map>>` and returns own-profile clamped by the base ceiling (monotonic `clamp_to_base` ⇒ min-of-clamped = the pre-seam `effective_permission` + clamp); `DefaultGrantStore` wraps the managed-file store (`grants::GrantStore` renamed `FileGrantStore`). `rhai` keeps the profile/base path (its inner bindings are a separate sync mechanism). Rejected: resolver walks the chain itself (custom resolver would skip the ADR-0024 clamp), a single `record_always` method (too narrow for the default file store), sync-in-loop resolution (serializes dispatch behind one DB call) (#311, part of #307) | Accepted |
| [0080](0080-mcp-streamable-http-transport.md) | MCP streamable-HTTP transport with per-server headers/auth: `McpClient` becomes an enum over `Stdio` ([0067](0067-mcp-client-as-runtime-tool-provider.md)/#198) and a new `Http` transport, chosen per server by a `command` XOR `url` config (`McpServerConfig::transport()` rejects both-set/neither-set); `McpTool` unchanged (it holds `Arc<McpClient>` and only calls `list_tools`/`call_tool`). `mcp::http::HttpClient` `POST`s JSON-RPC with `Accept: application/json, text/event-stream`, draining an SSE response until the matching JSON-RPC `id` or decoding a lone JSON body; static per-server `headers` (`${VAR}`-expanded from env) authenticate every request; an `Mcp-Session-Id` from `initialize` + the negotiated `MCP-Protocol-Version` are echoed on later requests. Behind a new `mcp-http` feature (`reqwest`+`futures`, in `default`) so the lean/`--no-default-features` build stays transport-free ([0025](0025-runtime-cargo-feature-gates.md)); a `url:` server on a build without the feature logs-and-skips. `HttpClient`/`StdioClient`/`Transport` are public so a multi-tenant embedder ([0076](0076-per-session-dynamic-tool-specs.md)) can assemble per-user registries with per-user tokens, bypassing YAML. Enabling a server is consent ([0047](0047-local-trust-boundary.md)); no new permission surface. Internal split: `mcp::stdio` (former `client.rs`, `StdioClient`) + `mcp::http` (gated) + slimmed `mcp::client` (enum + shared JSON-RPC helpers). Rejected: a `transport:`-tag/untagged enum (bare `command`/`url` gives clearer XOR errors), unconditional `reqwest` (web stack in the lean surface), a generic `Transport` trait (premature over two transports), a provider-style connection pool (low-rate tool calls; reqwest pooling suffices) (#312, epic #307) | Amended by [0128](0128-mcp-http-var-header-expansion-leak-surface.md)/[0153](0153-mcp-server-oauth.md) |
| [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md) | Per-agent-profile provider/model pinning + rebind on `SetAgent`. `AgentProfile` gains `provider: Option<String>` (`#[serde(default)]`) beside `model`; both set = a *model pin* (`AgentProfile::model_pin()`), `model` alone stays the legacy request-level fallback, `provider` without `model` is a loud load error. The rebind lives in **core's `SetAgent`** (one locus for Tab cycle / `/agent` / `--agent` / spawn / wire; replay consistent for free), driven off the existing `model_resolver` seam ([0063](0063-realtime-model-provider-switch.md)); core stays policy-free (the **runtime** injects persisted-file>frontmatter into the assembled profile). `Session` gains `provider` (no-op guard so an already-correct binding isn't rebuilt) + `profile_models` (per-profile session memory of `/model` choices); the `SetModel` success arm is factored into `Session::rebind`. Precedence: **session memory > static pin > keep current binding** (a pin-less profile with no memory emits no `ModelChanged`, a live override survives an agent switch); session-start applies the pin when `s.model` is unset (covers default `build` + spawned sub-agents like a cheap `explore`); replay reconstructs `profile_models`/`provider` from `ModelChanged`. Persistence: a **managed** `${config_dir}/entanglement/agent-models.yml` (override `ENTANGLEMENT_AGENT_MODELS_FILE`, `BTreeMap` for stable output), `AgentModelStore { load, get, set, apply }` overlaid onto the registry at startup; `atomic_write` extracted into shared `config::atomic`. TUI persists on the confirming `ModelChanged` (matched to a `pending_model_persist`), never on `Error` or a `SetAgent`-driven `ModelChanged`. Rejected: head-driven follow-up `SetModel` (every head re-implements it, races the switch, replay needs a synthesized follow-up), a layered `config.yml` section (runtime-rewritten machine state doesn't belong in the hand-edited config, like grants [0052](0052-approval-scope-and-persisted-grants.md)), live registry mutation (concurrency hazard for no gain; the pin lives on `Session` via the resolver) (#323, part of #302) | Accepted |
| [0082](0082-single-shot-session-ops-and-persisted-compaction.md) | Single-shot session ops (`InMsg::Oneshot { session, op: String, args: Value }`) with session compaction as the first op. Generic **wire envelope**, not a plugin registry: `session::ops::run_oneshot` is a plain `match op.as_str()`, so a future op needs no new `InMsg` variant/`wire_allowed`/`SessionCmd` — just a new match arm. `SessionCmd::Oneshot` uses the same defer-via-stash gate as `SetAgent`/`SetModel` (never runs concurrently with a live turn) and `run_oneshot` reuses `&mut *s.llm` directly (sound only because of that gate) via a tool-less `LlmRequest` (`tools: &[]`), draining the stream for text + `Finish` usage. `"compact"` renders the transcript, summarizes it, and calls the new `Context::apply_compaction(summary, kept)` — split off the trailing `kept` messages, clear, push one **user-role** summary message (`system` has no in-history wire mapping; a user-authored summary is trusted more reliably than an assistant-authored one), re-append the tail. The result is `OutEvent::Compacted { session, seq, summary, kept }` — a **persisted, seq-bearing** content event, so the existing variant-agnostic persistence tap and `ReplayFrom` history responder cover it for free; `Session::replay`'s new `Compacted` arm flushes pending buffers (like the `Done` arm) then calls the same `apply_compaction`, so live and replayed sessions converge. `kept` always `0` in v1 (keep-tail deferred — a `Tool` message replayed without its assistant parent breaks Anthropic's `tool_use`/`tool_result` pairing) but ships as real wire surface. The old prune-only `Context::compact` (#178) is unchanged, still the automatic pre-round fallback; `"compact"` only runs on request (`InMsg::Oneshot`, TUI `/compact`) — auto-summarize-on-threshold is a follow-up. Rejected: a typed op enum (re-opens the wire per op; the ADR-0072 wire freeze), runtime-side execution (no handle to mutate core's private `Context`), a generic `OneshotResult` envelope (replay needs to pattern-match `Compacted` specifically to call `apply_compaction`), streaming the summary as `TextDelta` (that channel belongs to turn output), keep-tail in v1 (turn-boundary detection not built yet) (#324) | Accepted |
| [0083](0083-in-app-tool-allowlist-editing-as-user-layer-materialization.md) | In-app tool-allowlist editing materializes a **user-layer override**, not a new config surface: the layered agent loader ([0034](0034-file-based-agent-definitions.md)) already shadows a same-`name` definition (built-in included), so editing just needs to *write* the shadow. `agents::materialize::save_tools_override(root, name, allowed)` resolves the currently effective definition's raw text (`winning_raw_text`, same precedence as `load_registry`), rewrites only `tools:`/`disallowed_tools:` via a `serde_yaml::Mapping` round-trip (`rewrite_tools` — order-preserving, everything else untouched), and writes atomically via `config::atomic::atomic_write` to `${config_dir}/entanglement/agents/<name>.md` (or `ENTANGLEMENT_AGENTS_DIR`). TUI: `e` on the `/agent` picker's highlighted profile opens a checklist dialog (`tui::tools_dialog::ToolsDialog`) over the full advertised tool roster (`EngineConfig.tool_specs`, captured before `Holly::spawn` consumes it), seeded from the profile's current effective mask; `Space` toggles, `Enter` saves, `Esc` discards; applies on next restart (no live registry reload yet). Rejected: a managed sibling file like [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md)'s `agent-models.yml` (a tool mask is already part of the definition, unlike a model pin — a second store would fight the layered loader for authority over the same field), byte-level line-splicing like `config::env_key::upsert` (YAML sequences need a YAML-aware rewrite, not single-line KEY=VALUE splicing) (#330) | Accepted |
| [0084](0084-runtime-live-reload-and-managed-file-locking.md) | Runtime-side live reload (inotify) + advisory locking for managed files. `config::lock::with_locked_file(path, f)` takes an exclusive `fd-lock` on `path`'s sibling `.lock` file (never the target — `atomic_write` replaces it wholesale) and runs `f`, which re-reads the current on-disk state itself (read-current → merge → write → adopt); `grants::FileGrantStore::record`/`config::agent_models::AgentModelStore::set`/`config::env_key::set_key` all route their managed read-modify-write through it, so two `skutter` instances no longer clobber each other's `Always` grant / `/model` pin / provider key. `write_grants` migrates onto the shared `atomic_write`. A new `watch.rs` (`spawn_debounced_watcher`, a pure primitive over `notify_debouncer_mini`, independently unit-tested) watches every resolvable agent/skill dir plus `${config_dir}/entanglement/` and `<root>/.entanglement/`; on a debounced change it reloads skills+agents and swaps the result into **runtime-held mirrors** (`watch::LiveDefinitions { profiles, skills, agent_models, grants }`, all `Arc<RwLock<..>>`/`Arc<Mutex<..>>`) — never core's `EngineConfig.profiles`, which stays pinned for the process lifetime per the [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md) "live registry mutation is a rejected design" precedent. `tool_runner::spawn_tool_executor_with_policy`'s `profiles` param and `skills::load_skill::LoadSkillTool`'s `registry` field both move onto these live handles (brief read locks per lookup); the TUI's `/agent` picker + Tab-cycle roster refresh on a reload notice (`App::refresh_profiles`/`record_reload_status`). A live session's already-resolved system prompt/tool mask is unaffected — only the next `SetAgent`/new session/picker pick sees the edit. Rejected: live-mutate `EngineConfig.profiles` in core (the ADR-0081 concurrency-hazard-for-no-gain reasoning applies identically), a wire-level reload `InMsg` (a runtime-internal mirror swap needs no core protocol change), polling instead of inotify (wastes CPU, adds latency, `notify`/`notify-debouncer-mini`/`fd-lock` are already hygiene-gate-clean) (#329) | Accepted |
| [0085](0085-gemini-native-wire-and-opaque-provider-meta.md) | Native Gemini wire + opaque `provider_meta` on `ToolCall`: a third provider wire (`Wire::Gemini`) beside the OpenAI-compat and Anthropic clients ([0007](0007-streaming-llm-and-provider-crate.md)), needed because Gemini's OpenAI-compat endpoint can't round-trip the `thoughtSignature` a 2.5 thinking model attaches to each function call — the compat surface doesn't expose it, so multi-turn tool use with a thinking model 4xxs on replayed history. Two changes: (1) a generic `ToolCall.provider_meta: Option<serde_json::Value>` — provider-private state that round-trips verbatim through persistence + replay, never inspected by core, using the [0064](0064-message-content-blocks.md) back-compat shim so old logs replay unchanged (`ToolCall`/`LlmEvent`/`LlmResponse` drop `Eq`, staying `PartialEq`, since `serde_json::Value` isn't `Eq`); (2) `GeminiLlm` implementing `Llm::stream` against `:streamGenerateContent?alt=sse` (candidate parts → `Text`/`Reasoning`/`ToolCall`, `usageMetadata` → `Usage`, `finishReason` → `StopReason`), stashing/restoring `thoughtSignature` via `provider_meta`, reusing the shared `HttpClient` pool/retry/rate-limit ([0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)). `ToolCall.id` is set to the function name (Gemini matches `functionResponse` by name), so same-name parallel calls in one turn share an id — a known, accepted edge, not silently wrong (dedupe is by `request_id`). Rejected: routing Gemini through `wire: openai` + `base_url` (silently breaks thinking-model tool loops), a Gemini-specific `thought_signature` field instead of a generic slot (solves only this vendor) (#309, part of #307) | Accepted |
| [0086](0086-recordsink-pluggable-persistence-append-target.md) | `RecordSink` — pluggable persistence append target: splits the persistence tap's *what to persist* (event-sourcing logic — route each record to its root session, tombstone broadcast-lag gaps, #104) from its *where to persist* (JSONL file), so a Postgres-backed embedder (#307) implements one method and inherits the tap's gap/lag handling instead of forking the ~subscriber. `pub trait RecordSink { fn append(&self, root: &SessionId, record: &LogRecord) -> anyhow::Result<()>; }`, `spawn_persistence_subscriber_with_sink(holly, sink: Arc<dyn RecordSink>)`; the existing `spawn_persistence_subscriber(holly, cwd)` becomes a thin wrapper over it + a new `FileSink` — file behavior byte-identical, existing tests unchanged. `append` is sync by design (the tap's receive loop isn't async-blocked on persistence); a blocking backing store must put a bounded channel + writer task behind it and return immediately, or it starves the broadcast receiver and manufactures the very `Gap` tombstones the trait exists to avoid — same sync-boundary-plus-documented-buffering shape as the `tool_spec_resolver`/`system_prompt_resolver` `Fn` seams ([0076](0076-per-session-dynamic-tool-specs.md)/[0078](0078-per-turn-dynamic-system-prompt.md)). `session_store::read`/`pair_records` stay file-side only — `Holly::resume` already accepts records from anywhere, so no read-side trait is needed. Rejected: an `async` `append` (slows the common file case for the rare DB one), a `Write`-based sink (JSONL framing is `session_store`'s concern), a mirroring `RecordSource` read trait (no embedder need identified) (#313, part of #307) | Accepted |
| [0087](0087-two-stage-ctrl-c.md) | Two-stage Ctrl+C in the TUI: a first press clears transient input (text buffer, `@file` popup, multiline mode) and arms a pending quit; a second within 3s quits. Intercepted **once** at the top of `handle_event`'s key-press block (before any modal/approval routing), replacing eleven duplicate `Char('q') \| Char('c')` arms across `event_loop.rs` + `modal_events.rs` with a single `App::handle_quit_key` (`quit_pending`/`quit_pending_at` fields, `tui::app::quit`). Ctrl+Q stays an unconditional immediate quit (escape hatch); the first press does **not** close modals (`Esc` already owns that). An external `SIGINT` (`kill -INT`, or a terminal ignoring crossterm's keyboard-enhancement flags — in raw mode Ctrl+C arrives as a key event with ISIG suppressed) is caught by a `tokio::signal::ctrl_c()` task at TUI startup and forwarded as a synthetic `Event::Interrupt` through the same `handle_quit_key` path, so an out-of-band signal can never leave the terminal "half killed" (raw mode / alternate screen / mouse capture left on) — the panic hook covered crashes, this covers the signal path. The timeout is checked lazily (correctness) and eagerly in the render loop (so the "Press Ctrl+C again to quit" hint clears promptly). Rejected: keep single-press quit + a separate clear key (Ctrl+C is the muscle-memory clear gesture), close modals on the first press (`Esc` already owns modal close), a force-quit SIGINT handler (fixes the terminal but re-introduces the single-press surprise) | Accepted |
| [0088](0088-session-aware-tool-execution.md) | Session-aware tool execution (#360, follow-up to the policy seam #311): `Tool` gains a default-delegating `run_for_session(&self, session: &SessionId, input: &str)` (mirrors the `run`/`run_content` pattern), and `ToolRegistry::execute(&self, call: &ToolCall, session: &SessionId)` threads the caller's `SessionId` through to it — the three in-tree call sites (`tool_runner::run_and_reply`, `script::exec`) already held the session at the point they called `execute`. Existing tools are unaffected (default body delegates to `run_content`); a multi-tenant embedder overrides `run_for_session` to dispatch per-tenant MCP endpoints or scope a DB-backed tool's writes to the caller, closing the gap between session-aware policy resolution (#311) and session-blind execution. `McpTool` stays session-blind — its config is fixed at registration; an embedder needing per-tenant MCP wraps several `HttpClient`s in its own `Tool`. Rejected: a tokio task-local for the current session (implicit, easy to miss in a spawned subtask), one `ToolRegistry` per tenant (defeats the shared executor #311 was built to enable), a separate `SessionAwareTool` trait (two trait objects per registration for no benefit over a default method) | Accepted |
| [0089](0089-user-configurable-max-turns.md) | User-configurable `max_turns`: the inner LLM→tool loop cap (#177) becomes an `EngineConfig` field fed by the layered user config (`config.yml`'s `max_turns:`, default 200), threaded through the standard resolution path (config > embedded default; no env tier) and read by `run_round` from the `&EngineConfig` it already holds. The cap is a structural runaway guard reset per prompt, not a cost control (ADR-0047 local trust). Rejected: an env var (third precedence tier, no per-invocation need), per-profile `max_turns` (over-scoped — the bound is on the loop, not the role), keeping a hard constant (leaves workload variance unsolved) | Accepted |
| [0090](0090-idle-ttl-auto-hibernation.md) | Optional `EngineConfig.idle_ttl: Option<Duration>` (#363, follow-up to #318/ADR-0077, default `None` = byte-identical to today): the supervisor sweeps on `max(idle_ttl / 4, 30s)` — a `tokio::select!` branch armed only when configured — and auto-hibernates a **settled** root (and its whole spawn sub-tree) once idle past the TTL, driving the existing `HibernateSession` cascade rather than a new eviction path. Settledness is `Session::turn.is_none()` alone (mid-turn and parked-on-approval/question both mean `turn.is_some()`, so no runtime `AgentState` or outbound-broadcast subscription is needed — narrower than the gap ADR-0077 assumed), published by each session task to a shared `ActivityRegistry` (`tokio::time::Instant`, mirroring `SeqRegistry`'s sharing pattern) at every settle/park transition; a sub-tree's idle-since point is the *latest* member's settle time, so one parked child pins the whole ancestry live. Deliberately stricter than manual `HibernateSession` (stop-then-hibernate on request): a background timer never cancels live work, only evicting a session already at rest. Rejected: a per-session self-timer (wrong owner — cascade-safety needs the supervisor's `parent_links`), inferring settledness from outbound traffic (strictly more machinery for the same answer `Session::turn` already gives), a dedicated `Clock` trait for tests (tokio's own `pause`/`start_paused` auto-advance already gives wall-clock-free determinism) | Accepted |
| [0091](0091-grep-file-scan-size-cap-decoupled-from-output-cap.md) | `grep` file-scan size cap decoupled from the output cap (#380): a grep-local `MAX_SCAN_BYTES` (1 MiB) replaces the reused `MAX_OUTPUT_BYTES` (32 KiB) as the per-file skip threshold, fixing a silent-skip bug where a match in a file over 32 KiB produced output indistinguishable from "no match"; skip reasons (`TooLarge`/`Binary`, the latter via a NUL-byte sniff) are tracked and surfaced as a labeled, capped-preview notice whenever non-empty, regardless of match count — supersedes the grep clause of [0008](0008-host-tools-workdir-and-bounded-output.md) point 4, whose claimed-but-unimplemented 4× multiplier is dropped in favor of an independent flat cap | Accepted |
| [0092](0092-call-file-based-stdin-stdout.md) | `call` gains optional `input_file`/`output_file` (#381), both resolved under root via the `read`/`edit`/`write` containment (`resolve_under_root`) and validated **before spawn** (bad path → clean error, child never launches). `input_file` is read in full and piped to the child's stdin from a background task running concurrently with the stdout/stderr drain (avoids a full-pipe deadlock either direction); **its absence now closes stdin (`Stdio::null()`) instead of inheriting the engine's real stdin** — an undocumented leak fixed as a side effect. `output_file` gets the full untruncated raw stdout (missing parent dirs created) with a `<output_file>.stderr` sibling always alongside; **with no `output_file` an artifact is written anyway**, auto-named `.entanglement/tmp/call-output/call-{pid}-{seq}.stdout` (`{seq}` a per-process atomic counter so concurrent calls never collide), its root-relative path always named in the result header (`[output: …] [stderr: …]`) — every `call` gets a durable, inspectable trace by default. An explicit `output_file` write failure is a hard error (it was requested); a default-artifact write failure is logged + degraded-noticed, never failing a command that would otherwise have succeeded. The timeout path's already-preserved partial buffer (#169) is what gets persisted on a killed call. `bash` unaffected. Rejected: persist only when `output_file` is explicit (restates the status quo the issue exists to fix), return artifact content inline (reintroduces the ADR-0045 tail-cap problem), extend the same default to `bash` (deferred — `bash` already has `run_in_background`/`bash_output` as its own durability story) | Accepted |
| [0093](0093-call-registration-independent-of-bash-opt-in.md) | `call` gains a `workdir` param mirroring `bash`'s (#386): resolved under root via the shared `resolve_workdir` helper (factored out of `bash.rs`, now used by both), validated to be a directory; no `workdir` → unchanged cwd = root. `call`'s **registration** stops piggybacking `bash`'s `ENTANGLEMENT_ENABLE_BASH` opt-in gate: it now registers unconditionally in `main.rs`, alongside the root-contained quintet — a fixed argv (no shell) is auditable, so its blast radius no longer needs gating with the shell's; per-profile permission (`Allow`/`Ask`/`Deny`) remains the actual dispatch gate, unchanged for both built-in profiles. `bash`/`bash_output` stay behind the flag exactly as [0010](0010-single-head-crate-and-bash-opt-in.md) established — supersedes [0010](0010-single-head-crate-and-bash-opt-in.md) §3 and [0045](0045-call-host-tool-argv-exec-tailed-output.md) §3 for `call`'s registration only | Accepted |
| [0094](0094-reasoning-effort-and-per-profile-generation-persistence.md) | `reasoning_effort: Option<ReasoningEffort>` (`Low\|Medium\|High`) added to `GenerationParams` (#374), OpenAI-native and mapped onto fixed thinking-budget tiers for Anthropic/Gemini (no effort concept of their own); `GenerationParams::apply_overrides` is the partial-merge primitive behind the new live `InMsg::SetGeneration`/`OutEvent::GenerationChanged` (always acks, no resolver to fail against, unlike `SetModel`). Per-profile persistence mirrors [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md)'s precedence (session memory > persisted > current binding) but through a **separate** `EngineConfig.generation_resolver` seam keyed by profile name, not baked into `AgentProfile` — `GenerationParams`'s `f32` fields have no total `Eq`, so they can't join `AgentProfile`'s `PartialEq + Eq` derive the way the pin's `provider`/`model` fields do. The runtime's `AgentGenerationStore` (managed `agent-generation.yml`, sibling of `agent-models.yml`) has no `apply(&mut ProfileRegistry)` — its `resolver(...)` builds the `GenerationResolver` closure directly. Rejected: baking `generation: Option<GenerationParams>` onto `AgentProfile` (breaks its `Eq` derive), a hand-rolled bit-pattern `Eq`/hash on `GenerationParams` (a footgun for a type whose only real comparison is user intent), routing through the existing `model_resolver`/`ResolvedModel` seam (wrong shape — keyed by `(provider, model)` and fallible, this lookup is keyed by profile name alone and cannot fail) | Accepted |
| [0095](0095-tui-set-show-generation-persist-on-confirmation.md) | TUI `/set`/`/show` for generation parameters (#376, on top of [0094](0094-reasoning-effort-and-per-profile-generation-persistence.md)'s wire + resolver seam): `/set <key> <value>` (`temperature`/`effort`/`thinking_budget`/`max_tokens`, `/compact`-style raw-text re-parse since `parse_command` drops trailing args) sends `InMsg::SetGeneration` and records `pending_generation_persist = (agent, overrides)`; `/show` sends a no-override `SetGeneration` as a query (no pending recorded) — reusing the merge's always-reply behavior rather than adding a read event. The confirming `OutEvent::GenerationChanged` is matched by "does it reflect every field the pending override set" (not an exact-tuple match, since only `/set`'s named fields are known ahead of time) — on a match the TUI commits the write via `AgentGenerationStore::set` and renders a transcript status line; an `Error` clears the pending without writing; a `GenerationChanged` with no pending (a `/show` query, or a `SetAgent`/session-start generation overlay from 0094) is rendered but never persisted. `Command::Set`/`Command::Show` join `all_commands()`; `CommandPalette` is split out of `commands.rs` into its own module once the new commands pushed it past the 400-line cap. Rejected: a dedicated read-only `ShowGeneration` event (the no-override query already gives a head the current value for free) | Accepted |
| [0096](0096-dynamic-toolregistry-sharedregistry.md) | Dynamic `ToolRegistry` (#372, phase 3 of the live-MCP-management umbrella): `SharedRegistry = Arc<std::sync::RwLock<ToolRegistry>>` replaces `spawn_tool_executor_with_policy`'s owned `tools: ToolRegistry` parameter — its two convenience wrappers (`spawn_tool_executor`/`_with_hooks`) keep the historical owned signature and `.shared()`-wrap internally, mirroring the existing `wrap_profiles` pattern, so the ~30 test call sites through them are untouched. `ToolRegistry` gains `unregister`/`unregister_prefix`/`contains`/`names`. Each `ToolExec` dispatch takes a brief `std::sync::RwLock` read lock (not `tokio::sync` — [0076](0076-per-session-dynamic-tool-specs.md)'s `tool_spec_resolver` is a sync `Fn` that must never block on I/O) and clones an owned snapshot *before* spawning its detached task, never held across a tool's `.await`. `main.rs` wires `EngineConfig.tool_spec_resolver` to read through the same `SharedRegistry` handle, reproducing the exact `cfg.tool_specs` composition (registry tools + the three runtime-intercepted pseudo-tool specs `update_tasks`/`ask_user`/`rhai`) so today's advertised schemas are byte-identical — behavior-neutral now, live on the *next turn* for any future registry mutation (unblocks #4's MCP add/remove) with no `EngineConfig` reload. Rejected: `tokio::sync::RwLock` throughout (forces the sync resolver closure into `blocking_read` or breaks 0076's no-I/O contract), changing all three `spawn_tool_executor*` signatures (unnecessary churn on ~30 test call sites that never need live mutation) | Accepted |
| [0097](0097-live-mcp-server-management.md) | Live MCP server management (#375, phase 4 of the MCP umbrella #377, on top of [0096](0096-dynamic-toolregistry-sharedregistry.md)): `InMsg::McpList`/`McpAdd`/`McpRemove` are engine-global like `ListSessions` (`session()` → `None`, `msg_to_cmd` → no session task, wire-allowed — enabling a server *is* consent per ADR-0047/[0080](0080-mcp-streamable-http-transport.md)), answered by a new runtime-side `mcp::spawn_mcp_responder` off `Holly::subscribe_inbound()` — mirroring `history::spawn_history_responder`'s answer to `ReplayFrom` — rather than the core supervisor, since the runtime alone holds the `SharedRegistry` + `ActiveServers`/`ServerConfigs` these ops mutate; two new `Holly::emit_mcp_list`/`emit_mcp_changed` helpers mirror `emit_history`. `InMsg::McpAdd`'s `config` is a new core-owned `McpServerSpec` DTO — a field-for-field mirror of the runtime's `McpServerConfig`, never a runtime type, since core cannot depend on the runtime crate. `mcp_add` upserts (unregister-then-reregister under the same name, so reconfiguring a broken/failed server replaces it live) and runs its connect/`tools/list` awaits *before* taking any lock (0096: never hold a lock across `.await`); `mcp_remove` drops the last `Arc<McpClient>`, triggering `StdioClient`'s `kill_on_drop`. Persistence (`config::save_mcp`, new `config/mcp_persist.rs`) is a **surgical `serde_yaml::Value` edit** of `config.yml`'s `mcp:` key — not a sibling managed file like grants/agent-models/agent-generation/the env file — because MCP servers are meant to stay part of the primary hand-edited config; locked + atomic like every other managed write, but does not preserve comments (no layer in this loader does). A failed add/remove is `tracing::warn!`-logged, not a new `OutEvent` (no session to attach one to, matching ADR-0067's best-effort MCP philosophy). Rejected: a sibling `mcp.yml` managed file (splits server config across two files), deserializing into typed `Config` and re-serializing the whole struct (silently drops unknown keys under `deny_unknown_fields`), a dedicated failure `OutEvent` (no session to attach it to) | Amended by [0124](0124-wire-refused-mcp-mutation-and-stdio-key-scrub.md) |
| [0098](0098-rhai-json-yaml-loader-and-read-raw.md) | `rhai` JSON/YAML loader (`parse_json`/`to_json`/`parse_yaml`/`to_yaml`, pure functions built on `rhai::serde`, no bridge/permission round-trip) and a new `read_raw(path)` binding (file content with no line-number prefix — `read`'s `"{lineno}: {line}"` format isn't parseable as JSON/YAML). `read_raw` is never advertised as a standalone tool (registered into the `ToolRegistry` only after `cfg.tool_specs` is snapshotted) and is graded/masked as an alias of `read`, not a distinct permission surface — a profile author restricting `read` has no way to know `read_raw` exists to restrict separately. An out-of-`i64`-range JSON/YAML integer silently widens to an approximate float (verified against `rhai`'s own `serialize_u64` fallback) rather than erroring, matching JS's `JSON.parse`. Rejected: a standalone `validate` tool (near-redundant with `try { parse_json(x) } catch`), XML/TOML support, JSON Schema validation, forcing a throw on oversized integers via a recursive pre-walk | Accepted |
| [0099](0099-glob-grep-exclude-and-default-git-exclusion.md) | `glob`/`grep` gain an optional `exclude: string[]` input (glob patterns matched against the root-relative path, threaded through the shared `list_files`) and an unconditional `.git` exclusion — any walked entry with a `.git` path component is dropped via a component check (not a glob pattern), independent of `excludes` and unable to be overridden, since there's no legitimate reason for an agent to read `.git` internals directly. Excluded entries are dropped before the file/dir metadata check, so they don't count toward `matched_dirs`/`skipped_errors` either. Rejected: making the default exclusion just `exclude`'s default value (overridable via `exclude: []` — a footgun for no benefit), expressing the default as a `.git/**` glob pattern (confirmed via scratch test it doesn't match the bare `.git` entry itself) | Accepted |
| [0100](0100-tui-mcp-command.md) | TUI `/mcp` command (#373, phase 5/final of the MCP umbrella, on top of [0097](0097-live-mcp-server-management.md)): `Command::Mcp` joins `all_commands()`; its subcommand grammar (`list`/bare `/mcp` defaults to `list`, `add <name> -- <command> [args...]`, `add <name> --url <url> [--header KEY:VALUE]...`, `remove <name>`) is the same raw-text re-parse pattern `/compact`/`/set` use. Parsing (`parse_mcp_args`) and the async wire senders (`send_mcp`/`send_mcp_list`) move into a **new sibling module** `tui/mcp_command.rs` rather than `commands.rs`/`event_loop.rs` (both already past the 400-line cap before this issue — the same reasoning 0095 used to carve `CommandPalette` out). `send_mcp_list` records a fresh correlation id on a new `tui::mcp_panel::McpPanel` before sending `InMsg::McpList`; the panel opens only when the matching `OutEvent::McpList` answers that exact id (`McpPanel::apply_list`), so a stray reply — another head sharing the engine, or an interleaved second `/mcp list` — never pops it open with the wrong snapshot. The panel (`modals::draw_mcp_panel`) is read-only (`Esc` only, no `ListState`), listing each server's transport, connected/error status, and namespaced tools. `OutEvent::McpChanged` (add/remove confirmation) and a parse error both render as a transcript status line on the active session (`App::handle_mcp_changed`/`record_mcp_error`), mirroring `/key`'s save notice — MCP config is engine-global (0097), so there is no owning session to target instead. No new wire surface. Rejected: folding the new code into `commands.rs`/`event_loop.rs` despite the cap (0095's precedent says split instead), a dedicated `McpAddFailed`/`McpRemoveFailed` event (diverges from 0097's head-agnostic best-effort philosophy), an always-open sidebar MCP section (scope creep past the issue's `list` + on-demand panel ask) | Accepted |
| [0101](0101-compaction-forks-into-a-new-session-copy-on-write.md) | Compaction forks into a new session (copy-on-write). Supersedes [0082](0082-single-shot-session-ops-and-persisted-compaction.md)'s in-place design. `/compact`'s `compact_op` no longer calls `Context::apply_compaction` — the source session's `Context` is **never mutated**. `OutEvent::Compacted { summary, kept }` becomes a *report* ("summary ready, source untouched"), and the head that issued the compaction forks the summary into a new session via `InMsg::Spawn { session: new_id, parent: source_id, agent: source_profile, prompt: summary }`, switching its active view to the fork (a two-phase collaboration: a session task cannot create sessions, only the supervisor can). `Spawn` already inherits the profile/model pin, seeds the summary as the first message, and records lineage — the fork is a child of the source, which stays idle, intact, independently resumable. Two bug fixes ship with the fork: (1) `compact_op` now refuses a truncated summary (`StopReason::MaxTokens` → `Error`, never forked, never mutating) — mirrors `turn.rs`'s truncation surfacing, which 0082 omitted; (2) it guards an oversized transcript against `s.ctx.limit()` before shipping a request the provider would 4xx. `Session::replay`'s `Compacted` fold becomes a **no-op** — the source is never mutated, so a resumed source recovers its full pre-compaction history (the implicit undo). The wire shape is unchanged (`Oneshot`/`Compacted` stay); `kept` is now wire-legacy only (always `0`, the source keeps everything). The TUI records a pending `Spawn` on the sync `App` (`pending_compact_fork`) that the async main loop (which holds `Holly`) drains; deduped by seq against the source view's watermark so a replayed/lagged `Compacted` doesn't fork twice. Rejected: in-place mutation + refuse-truncation only (stops future botched compacts but a *successful* summary still destroys the original — the fork is the real fix), fork-from-inside-the-session-task (session tasks have no `Holly` handle and cannot mint siblings), keep-tail under copy-on-write (no tail to keep — the source retains everything) (#324) | Amended by [0110](0110-compaction-successor-closes-predecessor.md) |
| [0102](0102-compact-keep-tail-verbatim-in-the-fork-prompt.md) | `/compact` keep-tail: a verbatim tail composed into the fork prompt (#397). Supersedes 0101's "keep-tail under copy-on-write" rejection for the *fork* (0101's reasoning was about the source, which already keeps everything). `Context::safe_kept(requested_kept)` clamps a keep-tail request to the nearest safe turn boundary — the tail must start at a `User` message, or a `Tool` reply could replay without its paired `Assistant` tool-call half (0082's deferred-to-v1 blocker); `apply_compaction` calls it internally too. `compact_op` splits `s.ctx.messages()` into head/tail at that boundary, summarizes only the head, and appends the tail's rendered transcript after the LLM summary — the composed text ships inside the existing `summary` field, so **no wire change** is needed; the TUI's existing fork path carries it unmodified. `head` empty (an over-large `kept`) and a tail whose rendered size alone exceeds the context budget are both rejected before/without an LLM call. `kept` on the wire now reports the real clamped count instead of a hardcoded `0`. TUI: `/compact [--keep N] [instructions]` (`tui::commands::parse_compact_args`); the command-palette pick still defaults to `kept: 0`. Rejected: extending `Compacted`/`Spawn` to carry structured `Vec<Message>` (strictly more faithful but touches the wire, persistence, and every `Spawn` call site for one caller's benefit), having the TUI reconstruct the tail from its own transcript view (a display projection, not guaranteed to line up with `Context::messages()`) | Accepted |
| [0103](0103-auto-summarize-on-context-overflow.md) | Auto-summarize on context overflow, in place — distinct from `/compact`'s copy-on-write (#398). `session/turn.rs`'s overflow guard (#178) tries an LLM summary before falling back to `Context::compact`'s placeholder pruning, gated by `EngineConfig::auto_compact` (default `true`); the summarization core (`session/summarize.rs::summarize`) is extracted and shared with `compact_op`, requesting a small fixed keep-tail clamped by the same `Context::safe_kept` #397/0102 uses. On success it mutates the session's `Context` **in place** via `Context::apply_compaction` — the fundamental split from `/compact`: a turn mid-flight has no head to fork a copy-on-write report into, so the only sound recovery is compacting the live context and continuing the same turn. `OutEvent::Compacted` gains `#[serde(default)] auto: bool` (`false` = every existing manual/copy-on-write record, `true` = this in-place path) rather than a new variant, since the payload is identical and only what a subscriber does with it differs; `Session::replay` folds `auto: true` by replaying the same `apply_compaction` call (flushing pending assistant/tool state first), unlike the manual path's no-op fold. Heads must not fork on `auto: true` — the TUI renders an in-place notice on the same view instead of `handle_compacted`'s new-session fork. Falls through to prune-then-refuse exactly as before when auto-summarize is disabled or its own guard trips (oversized transcript/tail, an LLM error, a truncated summary). Rejected: gating on N consecutive prune-fallbacks instead of every overflow (needless per-session state to save a round-trip in an already-rare case), a copy-on-write auto path (a parked/streaming turn has no head to fork into), a distinct `OutEvent::AutoCompacted` variant (doubles match arms in every subscriber for an identical payload) | Accepted |
| [0104](0104-bubblewrap-sandbox-for-bash-call.md) | OS-level process confinement for `bash`/`call` via bubblewrap (`bwrap`) — opt-in, fail-closed (#399). `ENTANGLEMENT_SANDBOX=bwrap` turns on a fixed recipe (`--ro-bind / /`, root re-bound read-write at the same absolute path so [0054](0054-canonicalizing-symlink-safe-root-containment.md)'s containment keeps working unmodified, `--unshare-pid/-ipc/-uts/-cgroup` always, `--unshare-net` unless `ENTANGLEMENT_SANDBOX_NETWORK=1`, `--die-with-parent`) for every `bash`/`call` invocation in the process; unset is byte-identical to today. A new `entanglement-runtime::host::sandbox` module (`SandboxBackend`/`SandboxPolicy`/`SandboxPolicy::from_env()`/`sandbox::command(...)`) wraps the spawned `tokio::process::Command`; `BashTool`/`CallTool` gain `.with_sandbox(policy)`. Fail-closed by omission (no unsandboxed fallback branch — a missing `bwrap` or disabled unprivileged userns just makes `spawn()` error). Global/opt-in only for v1 (per-profile wants the [0088](0088-session-aware-tool-execution.md) session-aware seam, tracked separately); the existing process-group SIGKILL timeout/cancel path needs no change (verified: killing the outer `bwrap` tears down the whole PID-namespace tree). Rejected: raw `unshare`/a namespaces crate (reinvents `bwrap`), firejail (SUID-root, worse threat-model fit), seccomp as the primary mechanism (doesn't confine filesystem/network), fail-open sandboxing (defeats the point) | Accepted |
| [0105](0105-expose-idle-ttl-via-runtime-config.md) | Expose `EngineConfig.idle_ttl` (#363, [0090](0090-idle-ttl-auto-hibernation.md)) as a `config.yml` setting — the deferred wiring both 0090 and [0077](0077-session-hibernation-evictable-resumable.md) named (#401). New `idle_ttl_secs: Option<u64>` in `entanglement-runtime`'s layered user config (`RawConfig`/`Config`, whole seconds — no duration-string crate in the tree, so this mirrors `max_turns`'s plain-integer shape rather than adding one), parsed to `Duration::from_secs` and copied onto `cfg.idle_ttl` in `build_config` alongside the existing `max_turns` wiring. One engine-global setting, not a `serve`-only CLI flag: every head shares the one `EngineConfig` a process builds (`Holly::spawn` runs once before the subcommand match), and the sweep only ever evicts an already-settled session, so a CLI/TUI process that also sets it pays nothing beyond a resumable hibernate it will rarely hit. `None` (unset, the default) leaves the sweep disabled exactly as before this setting existed. Rejected: a `serve`-only `--idle-ttl` CLI flag (would special-case one head's `EngineConfig` construction for no isolation benefit — the sweep's own settledness check is the actual safety property, not which head asked for it), a humantime/duration-string dependency (no precedent in the tree; whole seconds is enough precision for an eviction TTL) | Accepted |
| [0106](0106-skill-scoped-allowed-tools-enforcement.md) | Skill-scoped `allowed_tools` enforcement (#400): a session-keyed, runtime-only `ActiveSkill { skill_id, allowed_tools }` map in `tool_runner`, set when a resolved `load_skill` result's `skill_id:` header names a skill with an `allowed_tools` list, cleared on that session's next `Done` (or session end) — never a core-protocol field on `ToolCall`/`ToolExec`, matching skills' existing runtime-only status. `permission::skill_masked` is checked in `ToolExec` handling strictly *after* the existing #116 agent mask (`tool_masked`); unlike the agent mask it does **not** clamp down the ancestor/spawn chain — a skill's scope is the session's current turn, not an inheritable profile trait. No exemption for `load_skill` itself: a skill whose `allowed_tools` omits it blocks switching skills until the mask clears. New `OutEvent::SkillActive { session, seq, skill_id: Option<String>, allowed_tools: Option<Vec<String>> }` mirrors `FileChange`'s shape (fresh per-session seq, no `Session::replay` fold, persisted for free) as the wire-facing posture surface, rendered as a one-line notice by both the stdio `run --format text` head and the TUI transcript. `spawn_tool_executor`/`_with_hooks`'s ~30 test call sites are unaffected (a new `wrap_skills` helper defaults them to an empty registry, mirroring `wrap_profiles`); only `spawn_tool_executor_with_policy` gained the `skills` parameter. Rejected: a `skill_id` field on `ToolCall`/`ToolExec` (exactly the "protocol change with no behaviour" ADR-0037 avoided — the session-keyed map needs no core change at all), folding the posture into `ProfileDetail`/`SessionInfo` (those are static per-profile core projections; skill activation is a dynamic runtime mutation core has no other reason to know about), ancestor-chain clamping like `tool_masked` (no inheritance rationale for a self-chosen, turn-scoped mask), an explicit `unload_skill` tool or TTL-based expiry (no concrete need yet; `Done` is already a clean boundary) | Accepted |
| [0107](0107-ws-per-connection-approval-ownership.md) | WS per-connection approval ownership (#402, the #153/ADR-0069 residual): a new `SessionOwners` (`Mutex<HashMap<SessionId, ConnId>>` on `serve::ServeState`) claims a session for the first connection to send any frame referencing it (`touch`, first-writer-wins); only `Approve`/`Reject`/`AnswerQuestion` are gated on ownership (every other `InMsg` variant passes through regardless), a refused frame is logged and dropped without closing the connection — mirroring ADR-0069's forged-`ToolResult` refusal exactly, no new `OutEvent`. Released wholesale on disconnect (`SessionOwners::release`), so a still-parked approval is reclaimable by the next connection rather than deadlocking behind a client that went away. Entirely inside `entanglement-runtime::serve`, no core-protocol change, orthogonal to `PendingDecisions`/the reoffer timer ([0061](0061-parked-turn-state-batch-tool-resolution.md)/[0071](0071-parked-turn-reoffer-timer.md)). Rejected: request-id-scoped ownership captured at `ToolRequest`/`UserQuestion` emission (no wire-connection context exists at those call sites without a core change), a targeted "owned by another client" hint event over a per-connection side channel (avoidable complexity for a UX nicety, deferred), keep-parked-until-reconnect instead of release-on-disconnect (risks a permanent deadlock) | Accepted |
| 0108 | — _(number skipped; no ADR-0108)_ | — |
| [0109](0109-escape-root-access-via-approval.md) | Access outside the project root, approval-gated (#escape-root): root containment ([0054](0054-canonicalizing-symlink-safe-root-containment.md)) becomes subordinate to an explicit grant instead of absolute. A `read`/`edit`/`write` path or a `bash`/`call` `workdir` resolving outside root is detected in the executor (`permission::escape_root_target` + `host::escaping_path`) and forces an approval prompt even when the profile would `Allow` (a `Deny` floor still wins); on approval the `(tool, resolved-absolute-path)` grant is recorded in a **separate** `runtime::extra_roots::ExtraRootStore` (managed `extra-roots.yml`, override `ENTANGLEMENT_EXTRA_ROOTS_FILE`) at `Once`/`Session`/`Always` scope, **per tool** (a `read` grant never unlocks `write`). The host tools consult it via `resolve_under_root_or_grant`, matched against the symlink-canonicalized target; `extra == None` (all tests, both default executor wrappers) is byte-identical to strict containment. Reuses `ToolRequest`/`Approve{scope}` (no new wire variant); `glob`/`grep` originally stayed strictly root-contained, until [0132](0132-glob-grep-escape-root-search-via-durable-grant.md) let them ride an existing durable `read` grant; `Session` scope is process-wide (single-user local model). Rejected: reusing `grants.yml` (different key spaces — permission `tool(command)` vs containment `(tool, abs-path)`), threading the approved path through `run_for_session` instead of a shared store, a static `config.yml` root allow-list (can't express the interactive once/session/always prompt), session-id-scoped grants (moot least-privilege gain in the single-session local case for a heavier threading cost) | Amended by [0119](0119-rhai-bindings-route-through-the-escape-root-gate.md)/[0120](0120-once-scoped-escape-root-grant-bound-to-request-id.md)/[0132](0132-glob-grep-escape-root-search-via-durable-grant.md) |
| [0110](0110-compaction-successor-closes-predecessor.md) | `/compact` forks a *successor* and retires the source (amends [0101](0101-compaction-forks-into-a-new-session-copy-on-write.md)); each session tracks two-way lineage. `InMsg::Spawn.parent` becomes `Option<SessionId>` and gains `predecessor: Option<SessionId>`; `OutEvent::SessionStarted` carries `predecessor` too. The TUI compaction fork spawns the successor as a fresh **root** (`parent: None`, `predecessor: Some(source)` — lineage, not a spawn edge, so a `CloseSession` on the source can't cascade onto it) and closes the source right after, so the user moves forward into the compacted successor while the original's interactive session is retired (its log preserved). `Session` gains `children: Vec<SessionId>` (populated via internal `SessionCmd::ChildSpawned`/`ChildClosed` the supervisor sends on `Spawn`/`CloseSession`, rebuilt on replay by inverting the log's parent edges) alongside the existing `parent` and the new `predecessor`. Auto-compaction (`auto: true`, [0103](0103-auto-summarize-on-context-overflow.md)) is unchanged (in-place, no fork, no close). Also: TUI new-session ids are now opaque v4 UUIDs (the former `{root}-{ordinal}` suffix removed). Rejected: keeping the fork a child of the source and closing it anyway (cascade would kill the successor), a redundant stored `successor` field (derivable as the inverse of `predecessor`), serializing `children` explicitly (reconstructible from parent edges, avoids drift), leaving the source interactive and only relabeling the UI (the user wants it closed) | Accepted |
| [0111](0111-adaptive-endpoint-pacing-and-429-retry-until-clear.md) | Per-endpoint concurrency cap, adaptive pacing, and bounded 429 retry (amends [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)) (#413/#414): the shared per-endpoint pool ([0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)) gains a **concurrency semaphore** (a permit held across the whole stream so spawned sub-agents queue instead of 429-storming, default 3 — now catalog data mirroring `rpm`: `ProviderEntry.concurrency`, env `{NAME}_CONCURRENCY` > user `providers.yml` > embedded default, falling back to `RetryConfig::concurrency`, itself process-wide-overridable via `ENTANGLEMENT_MAX_CONCURRENCY`), an **adaptive pacing gate** (AIMD `penalize`/`relax` self-tuning RPM), and a 429 that **parks every concurrent session's window and retries** (5s→10min, bounded ≈15min then surfaced as an error so a saturated endpoint fails a sub-agent's turn rather than hanging its parent). Rejected: per-session resilience state (the pool is shared per endpoint, not per session), an unbounded 429 retry (a permanently-saturated endpoint would hang the turn forever), a global concurrency knob only (per-provider catalog data mirrors how `rpm` is already expressed) | Accepted (deferred-concurrency framing superseded by [0122](0122-per-provider-concurrency-and-rpm-as-catalog-data.md)) — Amended by [0141](0141-wire-visible-throttle-transitions.md), [0155](0155-errored-subagent-turn-parks-for-steering.md) |
| [0112](0112-resume-cascades-over-the-spawn-subtree.md) | `Resume` cascades over the spawn sub-tree, mirroring `CloseSession`/`HibernateSession`'s teardown cascade in reverse (#415): `Session::replay` gains an explicit `target: &SessionId` parameter (generalized from always assuming "the log's own root") so the same fold can reconstruct any session — root or descendant — from a shared root log; a new supervisor helper `spawn_resumed` factors out "replay one session and register it like a live `Spawn`" so `InMsg::Resume` walks the target's replayed `children` and recursively re-spawns each still-live one (BFS via a plain `Vec` + cursor), re-registering `parent_links` as it goes. Also fixes two latent bugs the cascade exposed: `resume_meta` returned the *first* `SessionStarted` in the whole log unscoped by session (harmless for a root-only resume, wrong for a cascaded child) — now matches on `session == target`; and `session_loop`'s re-announced `SessionStarted` used the raw (always-`None`-on-resume) `predecessor` parameter instead of the replay-resolved `Session.predecessor`, so a resumed compaction successor's persisted log could regress its own lineage on a second resume. No wire/protocol shape changes. Rejected: leaving child-cascade as a documented gap (asymmetric with `CloseSession`/`HibernateSession`), pushing the cascade to the head via the reconstructed `children` (not wire-exposed, per [0110](0110-compaction-successor-closes-predecessor.md)'s no-redundant-field reasoning), serializing a sub-tree manifest into `SessionStarted` (redundant, derivable, same reasoning [0110](0110-compaction-successor-closes-predecessor.md) used for `successor`) | Accepted |
| [0113](0113-persistence-synthesizes-a-spawned-childs-initiating-prompt.md) | Persistence synthesizes a spawned child's initiating prompt (#421, a non-goal left by [0112](0112-resume-cascades-over-the-spawn-subtree.md)): `InMsg::Spawn` delivers its `prompt` straight to the child's session-command channel, bypassing the inbound broadcast the persistence tap observes, so no `InMsg::Prompt` record ever existed for it. The tap now caches a `Spawn`'s `prompt` (`pending_spawn_prompts: HashMap<SessionId, String>`) instead of dropping it outright, and once the child's `SessionStarted` resolves `roots` (the point `InMsg::Spawn` itself still can't be persisted verbatim without becoming a stray bogus-root file), synthesizes `InMsg::prompt(child, prompt)` and appends it right after — so `pair_records` pairs it with the child's own next event, and `Session::replay` folds it as the child's opening user message like any live `Prompt`. The cache entry is consumed on first use, so a resumed child's re-announced `SessionStarted` (resume never re-sends `Spawn`) never re-synthesizes or duplicates the record. No wire/protocol change. Inherits the pre-existing #275 pairing hazard under true session concurrency (tracked separately, not reopened here). Rejected: a new protocol field carrying the prompt explicitly (duplicates what `InMsg::Prompt` already represents), routing the child's initial prompt through `Holly::send` instead of the direct channel send (reopens spawn-hot-path ordering questions to satisfy only the tap's observation point), persisting `InMsg::Spawn` verbatim once resolvable (needs a new replay fold arm duplicating `InMsg::Prompt`'s existing one) | Accepted |
| [0114](0114-capability-level-permission-keys.md) | Capability-level permission keys `read`/`write`/`call` (#418, part of the #416 epic): expanded at **parse time** in the shared `agents::permission_from_value` chokepoint (agent frontmatter + the user-config ceiling, #172) into literal per-tool rules — core's `PermissionProfile`/`resolve` are untouched and stay capability-unaware ([0006](0006-core-dependency-hygiene-gate.md)). `tool_names::CAPABILITIES` maps `read`⇒read/grep/glob, `write`⇒edit/write, `call`⇒bash; `tool_names::MULTI_GROUP` (`call`, `rhai`) are general-purpose tools graded separately by a pre-scan that takes the least-privileged (`min`) of every bare `read`/`write`/`call`/literal-`rhai` grade, emitted first so a later arg-scoped `call(...)` rule can still refine `call`. An arg-scoped capability key (`read(src/*)`) fans out per-member with the pattern attached, `call`'s arg-scoped list additionally including the literal `call` tool. `plan.md`'s `read: allow` now also flips `grep`/`glob` to `allow` — accepted and pinned by test, not silently absorbed. Rejected: expanding capabilities inside core's `resolve` (reintroduces the capability-aware dependency ADR-0006 forbids), a nested `{allow:[…],deny:[…]}` YAML shape for command sets (a second incompatible rule shape for no gain over flat `tool(pattern)` lines), a static tie-break for multi-group `Ask` (there's nothing to tie-break — `min` already resolves correctly and lets a real `Ask` surface) | Accepted |
| [0115](0115-rhai-exec-bindings-call-bash.md) | `rhai` exec bindings (#419, part of the #416 epic, amends [0046](0046-rhai-sandboxed-script-tool.md)): `exec(command)`/`exec(command, args)` (marshalled to the `call` tool) and `bash(command)` (marshalled to `bash`, registered only when the host `bash` tool is) join `tool_names::BINDING_TOOLS` (5→7), graded through the same `Allow`/`Ask`/`Deny` + argument-scoped-rule chain as the quintet — the Call capability ([0114](0114-capability-level-permission-keys.md)) closes the "would let a script escalate past its sandbox" gap 0046 deferred on. Named `exec`, not `call`, in script source: `call` is a hard-reserved Rhai keyword (`KEYWORD_FN_PTR_CALL`, function-pointer invocation) the interpreter special-cases ahead of any registered function of the same name — the dispatched tool name, permission grade, and `BINDING_TOOLS`/capability membership all stay the literal `call` regardless. Two pre-existing bridge sharp edges closed in the same change: (A) the `approved` Ask-cache moves from `HashSet<&'static str>` (bare tool name) to `HashSet<String>` keyed by `approval_cache_key` — for `call`/`bash` that's `"{tool}:{permission_arg(tool, input)}"` (the resolved command line), so approving one command can't silently pre-clear a different, more dangerous one in the same run; every other binding keeps its existing bare-tool-name key. (B) `register_bindings` derives a per-call `timeout` from the script's own remaining wall-clock budget (`remaining_timeout_secs(start, timeout)`, clamped to a 1s floor) and stamps it into every `exec`/`bash` call's marshalled JSON, since rhai's `on_progress` timeout interrupt can't reach a binding call parked in `blocking_recv` — without this the exec tool's own much longer default (120s, capped 600s) would be the only real bound. Rejected: reclaiming `call` as the script-facing name via `disable_symbol` (turns it into a parse error, not a normal function name — strictly worse), exempting `call`/`bash` from Ask-caching entirely (worse UX for no extra safety over command-scoped caching), computing the derived timeout once instead of per call (a second sequential `exec()` would incorrectly see the *original* budget), a bridge-level `tokio::time::timeout` wrapper instead of passing the derived value through (would race the delegated tool's own already-correct `kill_on_drop`/`own_process_group` cancellation) | Amended by [0130](0130-rhai-exec-bindings-marshal-workdir.md) |
| [0116](0116-workdir-scoped-permission-rules-for-bash-call.md) | Workdir-scoped permission rules for `bash`/`call` (#425, part of the #416 epic, deferred by [0114](0114-capability-level-permission-keys.md)): a second, independent rule-key scope clause `tool{pattern}` matches a `bash`/`call` call's `workdir` instead of its command line, alongside the existing `tool(pattern)` (#173, [0051](0051-argument-scoped-permission-rules.md)) argument scope — both compose freely in one ordered last-match-wins list, no compound-key grammar. `PermissionProfile::resolve_scoped(name, arg, workdir)` is the new three-argument entry point; the existing `resolve(name, arg)` is unchanged, defined as `resolve_scoped(.., None)`, so `tool{pattern}` is inert by construction for a tool with no workdir concept. `workdir`/`arg` are kept as separate typed parameters end-to-end rather than composed into one string, since both are attacker-controlled and a separator-based composition would be spoofable. `runtime::permission::permission_workdir` mirrors `permission_arg`'s shape and threads through `permission_for`/`clamp_to_base`/`effective_permission`; the `rhai` `exec`/`bash` bindings marshal no `workdir` field, so a workdir-scoped rule never fires for a binding call. Capability fan-out (#418) mirrors the arg-scoped case: `call{pattern}` expands to both `call{pattern}` and `bash{pattern}`. Rejected: composing `workdir` into the single existing `arg` string (spoofable via an embedded separator), a compound rule key requiring both scopes to match at once (unneeded — two independent clauses already express any such policy), a distinct key name/top-level `workdir:` section instead of bracket syntax (`tool{pattern}` reads as `tool(pattern)`'s natural sibling) | Amended by [0130](0130-rhai-exec-bindings-marshal-workdir.md) |
| [0117](0117-mcp-tool-capability-fan-out.md) | Config-side capability hints fan MCP tools into `read`/`write`/`call` (#426, part of the #416 epic, deferred by [0114](0114-capability-level-permission-keys.md)): each `mcp:` server block gains an optional `capabilities: HashMap<String, String>` (raw tool name → `read`/`write`/`call`, hand-annotated since no MCP-protocol capability hint exists to derive it from), folded by `mcp::capability_index` into an `McpCapabilityIndex` (capability → namespaced `mcp__<server>__<tool>` names, reusing `McpTool`'s own naming helper so it can't drift). `agents::expand_capabilities` takes this index as a new parameter and extends only the **bare** capability case (`read: allow` also covers an annotated MCP tool) — argument-/workdir-scoped capability keys and the `call`/`rhai` multi-group stay untouched, since an MCP tool has no command/workdir argument to scope against and isn't a general-purpose host tool. Computed once at startup from config alone (no live server connection needed) and threaded into `agents::load_registry`/the config ceiling/the live-reload watcher's snapshot — matching how the ceiling itself is already startup-only, not live. An annotation naming a tool the server never registers is simply inert, not a startup error. `inspect agents`/`prompt_report`/`built_in_registry` deliberately keep an empty index (out of scope — those already don't reflect the ceiling clamp either). Rejected: deriving capabilities from a live `tools/list` response (no such field in the MCP spec), expanding capabilities inside core (breaks ADR-0006), threading the index through every `inspect`/test-helper call site (disproportionate for a debug view with a pre-existing, different omission) | Accepted |
| [0118](0118-ambiguous-stop-reason-bounded-retry.md) | Bounded retry on an ambiguous LLM stop instead of ending the turn: `session::turn::run_round`'s only prior termination signal was `tool_calls.is_empty()` — `stop_reason` besides `MaxTokens` (ADR-0055) was ignored, so a provider (Ollama via the shared OpenAI-compat client — no per-provider wire handling exists) that closes its stream mid-generation with no `finish_reason`, or drops a tool call for malformed JSON (which previously still reported a contradictory `Some(ToolUse)` with zero actual calls), got silently committed as a clean, complete turn. `is_confident_stop` now classifies `EndTurn`/`MaxTokens`/`StopSequence` as deliberate (unchanged `Done` path) and `None`/`Other`/contradictory-`ToolUse` as ambiguous: injects a synthetic user-role nudge and retries the round **in place** (same `run_round` call, no new park), bounded by a new `TurnState::ambiguous_retries` counter capped by `EngineConfig::max_ambiguous_stop_retries` (default 2, separate from `max_turns` — a retry round still counts against it too, so `max_turns` remains the hard outer backstop) and reset to 0 by any round with a confident outcome. Exhausting the budget emits a distinct warning `Error` before the normal `Done`, instead of silently succeeding. Companion fixes: `openai.rs` now gates its `ToolUse` fallback on `emitted_any_tool_call` (what was actually yielded) rather than `has_pending_tools` (what was merely accumulated before the malformed-JSON filter ran); `DummyLlm`/`EchoLlm`/`stream_from_response` now report an honest `Some(EndTurn)`/`Some(ToolUse)` instead of bare `None`, since `None` became load-bearing. Rejected: heuristic text-content sniffing for "announced intent" (fragile, a structural signal already existed), unbounded retry (infinite-loop surface), retrying on any empty `tool_calls` regardless of `stop_reason` (adds latency/cost to the deliberate-stop common case), fixing only the OpenAI-compat client (the gap is architectural, in the turn loop's termination decision — Anthropic/Gemini can produce `None`/`Other` too), bundling the `[DONE]`-terminator/trailing-buffer-flush wire fixes or an Ollama `max_output_tokens` catalog default (no remaining user-visible bug once the engine-level retry exists; the catalog default is a separate product judgment call) | Accepted |
| [0119](0119-rhai-bindings-route-through-the-escape-root-gate.md) | `rhai` file/exec bindings route through the escape-root gate (#446, amends [0109](0109-escape-root-access-via-approval.md)): the `Intercept::Rhai` route never called `tool_runner::dispatch`, so the ADR-0109 forced-`Ask`/warning/`ExtraRootStore::record` steps never ran for a binding — a first-time out-of-root `read`/`edit`/`write`/`exec`/`bash` binding call hard-failed with no way to prompt, while a *pre-existing* durable grant was still silently honored (same `ToolRegistry` instance, no signal). `EscapeRoot::escaping` becomes `pub(crate)`; `Option<EscapeRoot>` is threaded one hop further, `Intercept::Rhai` → `run_rhai` → `execute_script` → `service_binding`, which now computes the identical `escape` value `dispatch` does and, for an escaping call, forces the approval path (bypassing the coarse per-run `approved` cache), appends the same "⚠ accesses a path OUTSIDE the project root" warning to the nested approval card, and — the `Approval::Approved` variant now carrying `ApprovalScope` — calls `store.record(tool, abs, scope)` on approval instead of caching locally. A durably-allowed escape still resolves silently (`is_durably_allowed` clears `escape` to `None`), matching `dispatch` exactly — intentional, not reopened. `Option<EscapeRoot>` stays opt-in (`None` for every existing test/default wrapper), so the pre-existing hard-fail test is unchanged; two new integration tests cover the previously-untested combination. Rejected: documenting the asymmetry instead of closing it (no workaround for a legitimate script use case, for no security benefit — the binding is already permission-graded identically), a separate rhai-only escape store (fragments grants by call origin against one shared `ExtraRootStore` execution already consults regardless of caller), warning again on every durably-granted reuse inside a script (diverges from `dispatch`'s identical-case behavior for no reason) | Accepted |
| [0120](0120-once-scoped-escape-root-grant-bound-to-request-id.md) | `Once`-scoped escape-root grants are bound to the approving `request_id` (#449, amends [0109](0109-escape-root-access-via-approval.md)): `ExtraRootStore`'s `once` set previously keyed on `(tool, path)` alone, so per-call detached executor tasks meant a `Once` token approved for one call could be consumed by a *different* concurrent call to the same escaping path — whichever `take_allowance` reached it first, and a `HashSet` silently deduped two separately-approved `Once` grants to the same key into one. `record`/`take_allowance` now key `Once` on `(tool, path, request_id)`; `Session`/`Always` are unchanged, matching `(tool, path)` alone (the correct semantics for a grant meant to cover every later call). `Tool::run_for_session` gains a `request_id: &str` parameter (`ToolRegistry::execute` forwards `call.id`, which it already had but wasn't passing on) — source-compatible via a default that ignores it and delegates to `run_content` as before, since no tool outside `tools.rs` overrode `run_for_session` before this change; the six escape-root-capable host tools (`read`/`edit`/`write`/`apply_patch`/`bash`/`call`) now override it, threading the id into `resolve_under_root_or_grant`/`resolve_workdir_or_grant`. `script.rs`'s rhai bridge computes its per-binding id (`bind_rid`) once up front and threads it into both `store.record` and the `exec()` call that follows (previously `exec` minted an unrelated `"rhai:{tool}"` id, so a same-binding record→consume pair didn't even share an id). Rejected: a multiset/counted `once` (fixes the double-insert loss but not the "wrong call consumes it" property), an ambient `tokio::task_local!` read by the containment check instead of an explicit parameter (each call *does* run in its own detached task, so it would work, but makes request identity invisible/ambient against this codebase's preference for explicit seams like `SessionId` on `run_for_session`, [0088](0088-session-aware-tool-execution.md)) | Accepted |
| [0121](0121-prune-only-compact-stays-silent.md) | Prune-only `Context::compact` stays silent — accepted live/replay divergence (#450): the context-overflow guard's fallback step (placeholder-pruning the oldest tool outputs when auto-summarize is disabled/fails/still doesn't fit, [0103](0103-auto-summarize-on-context-overflow.md)) mutates `Session.ctx` in place with no `OutEvent`, so `Session::replay` never replays it and a resumed session reconstructs the full unpruned history the live session had already discarded. Decision: leave it silent rather than add a wire event — `compact` is a deterministic, idempotent function of the existing log and the model's token budget alone (unlike auto-summarize's LLM-authored, unrecoverable-from-the-log rewrite), so `enforce_context_window` re-derives the identical result on the resumed session's very next round; the divergence self-heals within one round-trip and never ships an over-window request. Matches the precedent `Session::replay`'s `Compacted { auto: false, .. }` arm already set for pre-ADR-0101 legacy records. Rejected: reusing `Compacted { auto: true, summary: "", .. }` for the prune (replay's `auto: true` arm calls `apply_compaction`, which collapses the *whole* history to one summary message — wrong for a placeholder-only prune, trading a benign divergence for real data loss), adding a new `OutEvent` variant + a `replay` arm calling `Context::compact()` (the honest full fix, deferred rather than rejected — permanent wire surface to close a gap that already self-heals in one round-trip) | Accepted |
| [0122](0122-per-provider-concurrency-and-rpm-as-catalog-data.md) | Per-provider concurrency and RPM as catalog data (#414): `ProviderEntry` gains optional `rpm: Option<u32>` and `concurrency: Option<usize>` fields mirroring the client-level `RetryConfig` knobs, with the catalog's standard precedence **env (`<NAME>_RPM`/`<NAME>_CONCURRENCY`) > user `providers.yml` > embedded `defaults.yml` > client default**, the last still process-wide-overridable via `ENTANGLEMENT_MAX_CONCURRENCY`. Neither field is set in the embedded defaults (a wrong static concurrency ceiling is worse than the conservative client default 3 the user can override; AIMD self-tunes RPM but concurrency has no such self-correction). Supersedes [0111](0111-adaptive-endpoint-pacing-and-429-retry-until-clear.md)'s "Deferred: per-provider concurrency in the catalog" follow-up — the feature has shipped; ADR-0111 itself is unchanged per the immutability rule. Rejected: seeding embedded per-provider guesses (no authoritative ceilings; a wrong default storms or serializes), dropping `ENTANGLEMENT_MAX_CONCURRENCY` (still useful as a raise-every-endpoint escape hatch, removing it breaks existing setups for no behavior win) | Accepted |
| [0123](0123-agent-poll-zero-timeout-waits-for-notification.md) | `agent_poll` `timeout_secs: 0` waits for the child's notification: the `0` value was previously a degenerate no-op (the bounded-wait wrapper fired immediately, returning a still-running status without ever waiting). Reclaim it as the sentinel for "wait for the child's completion notification, no caller-side bound" — `run_agent_poll` branches on `0` and calls `wait_complete` directly, skipping the `tokio::time::timeout` wrapper, exactly as the blocking `agent` tool ([0033](0033-agent-tool-family-and-blocking-agent.md)) already does (that path is known hang-safe — `wait_complete` breaks on watch-sender-drop). Positive timeouts keep the `MAX_TIMEOUT_SECS = 600` cap. Amends [0026](0026-async-subagent-spawn-and-poll.md)'s "never-park-indefinitely" contract with the explicit `0` carve-out (ADR-0026 itself unchanged, per immutability). Rejected: `timeout_secs: -1` (would force `u64 → i64` across parse/schema/UI-render for no gain over reclaiming `0`), a separate `block: true` field (vaguer, new field vs. a dead value, forces setting two things), capping `0` to `MAX_TIMEOUT_SECS` (defeats the purpose) | Accepted — Amended by [0155](0155-errored-subagent-turn-parks-for-steering.md), [0161](0161-unified-async-work-background-flag-and-one-poll.md) |
| [0124](0124-wire-refused-mcp-mutation-and-stdio-key-scrub.md) | MCP mutation is trusted-only, the wire gate fails closed, and stdio servers lose the provider keys (#472, amends [0069](0069-trusted-untrusted-wire-frame-split.md)/[0097](0097-live-mcp-server-management.md)): the 2026-07-21 security audit found (1) `StdioClient::spawn` applied the per-server `env:` map onto an *inherited* parent env, so every MCP server received the provider API keys #164 deliberately scrubs from `bash`/`call` children — `spawn` now threads `secret_env` (`catalog.key_envs()`) through `mcp::connect`/`mcp_add`/`spawn_mcp_responder` and `env_remove`s each var *before* `.envs(env)`, so an explicit per-server `env:` entry still wins (deliberate consent; a `build_command` helper makes the env shape unit-testable); (2) `InMsg::McpAdd` was wire-allowed with **no approval** while the `serve` origin check is opt-in-off — a hostile web page could drive a cross-origin WS into spawning an arbitrary local subprocess; ADR-0097's "enabling is consent" covers the trusted config *file*, not an unauthenticated wire frame, so `McpAdd`/`McpRemove` join the wire-refused set (read-only `McpList` stays; the TUI `/mcp` path uses privileged `Holly::send`, unaffected); (3) `wire_allowed` was a fail-open negated blocklist — now an explicit exhaustive allowlist `match`, so a new `InMsg` variant is wire-refused until deliberately opted in (compile error to skip the decision, mirroring `session()`/`variant_name()`). Rejected: approval-gating `McpAdd` on the wire (new approval semantics for a frame with no known wire consumer — may supersede later), defaulting the origin check on (doesn't close the surface, breaks ADR-0048's raw-script clients), `env_clear()` + allowlist (breaks servers needing `PATH`/`HOME`; #164's scrub is the established model) | Accepted |
| [0125](0125-permission-arguments-for-path-tools-are-normalized-root-relative.md) | Permission arguments for path tools are normalized root-relative (#485): `permission_path::grading_arg` wraps `permission_arg` with lexical `.`/`..`/`//` folding (`normalize_lexical`) plus a root-prefix strip for the path-arg tools (`read`/`edit`/`write`/`apply_patch`/`glob`/`grep`, never `bash`/`call`) when a project root is wired (`rooted_arg`), so an absolute in-root path grades and grant-keys identically to its root-relative spelling — the bug: `read(src/*)` matched the relative spelling but not `/root/src/main.rs`, and a Session/Always grant recorded against one spelling never covered the other. `permission_arg` itself stays verbatim (the TUI transcript's literal-display source); an out-of-root absolute path stays verbatim too (the escape-root gate's problem, [0109](0109-escape-root-access-via-approval.md)), as does `permission_workdir` (host-absolute by design, [0116](0116-workdir-scoped-permission-rules-for-bash-call.md)). `ProfileResolver` gains a `root: Option<PathBuf>` constructor argument (test wrappers pass `None`, byte-identical to before); `tool_runner::dispatch` computes the grading arg once and threads it into both the pre-prompt grant lookup and the post-approval grant record. Rejected: canonicalizing (resolving symlinks) instead of lexical-only normalization (would need filesystem access from the grading path, which must stay synchronous/side-effect-free; symlink resolution is already the escape-root gate's job), relativizing inside `permission_arg` itself (would rewrite what the TUI shows the user the model actually sent) | Accepted |
| [0126](0126-session-scoped-directory-grants.md) | Session-scoped directory grants via `ApprovalScope::SessionDir` and TUI `/allow` (#486, blocked by [0125](0125-permission-arguments-for-path-tools-are-normalized-root-relative.md)): a new additive `ApprovalScope` variant (not a flag on `Approve`) widens a grant to every later call whose #485-normalized grading argument falls under the approved call's directory (`grants::dir_covers`, a plain path-component-prefix check) instead of matching one exact call — restricted to the read-only triad (`read`/`grep`/`glob`, the ADR-0114 `read` capability's members, reused directly so the "is this tool read-like" check can't drift); any other tool, or an escape-forced prompt (`ExtraRootStore`), degrades it to an exact `Session` grant rather than widening. Session-only by construction — no persisted `Always`-directory scope, so `FileGrantStore`'s new `session_dirs: HashMap<SessionId, BTreeSet<String>>` never touches `grants.yml`. `grants::dir_for(tool, arg)` derives the directory an approved call implies (parent dir for `read`/`edit`/`write`/`apply_patch`, the path filter verbatim for `grep`, the literal non-wildcard prefix for `glob`). `GrantStore::grant_session_dir` is default-implemented (a no-op echo) so existing custom `GrantStore` impls (`tests/policy_seam.rs`, `examples/embedded.rs`) keep compiling untouched; only `DefaultGrantStore` overrides it for real. TUI surfaces: `[d]` on an approval prompt (gated on the pending tool being read-like) and a proactive `/allow <path>` command (a new sibling `tui/allow_command.rs`, normalizing against the head's root and rejecting anything outside it) — both call `grant_session_dir` synchronously through a cloned `Arc<DefaultGrantStore>` handle, introducing no new wire surface. Rejected: an `Always`-scoped directory grant (a persisted directory-wide capability needs more UX thought than this issue covers, deferred not rejected), widening mutation tools too (their per-call blast radius is why `Session`/`Always` exact-match already exists), routing an escape-forced `SessionDir` into a new "external directory" concept in `ExtraRootStore` (out of scope, no requested need) | Accepted |
| [0127](0127-ask-user-v2-multi-question-envelope.md) | `ask_user` v2 (#488, amends [0027](0027-ask-user-interactive-prompt.md)): one `ask_user` call now carries `questions: Vec<Question>` (1..N) instead of a single question — one `OutEvent::UserQuestion` batches the whole array, the head walks it and replies once with `InMsg::AnswerQuestion.answers: Vec<Vec<String>>` (one inner vec per question), folded back as a single `ToolResult`, keeping the one-`ToolExec`/one-`ToolResult` contract core relies on. `allow_free_form` is dropped — a typed "Other" answer is unconditional on every question now — and `multi_select` is added per-question. Wire evolution mirrors [0064](0064-message-content-blocks.md): `UserQuestion.questions` flattens onto the wire over an untagged `Questions` newtype so a legacy single-question log still deserializes; `AnswerQuestion` keeps the legacy `answer: String` field alongside the new `answers`, folded to `[[answer]]` in `seam::Decision::from_inmsg`. The TUI's `PendingQuestion` now models one *call* (all its questions, a `current` index, buffered answers, the current question's checked-option set) walking sequentially inside the existing `pending_questions` batch queue (#273); `Space`/number keys toggle a multi-select's checkboxes, `Enter` submits the current question and sends one `AnswerQuestion` only once the call's last question is answered. Rejected: N events (one `UserQuestion`/`AnswerQuestion` pair per question) — reintroduces sub-request-id plumbing and complicates replay of a partially-answered batch; keeping `allow_free_form` defaulted `true` — dead weight once every head branches on a flag with one live value; `multi_select` per-call instead of per-question — forces a batch to share one selection mode even when it naturally mixes single- and multi-choice questions | Accepted — Amended by [0143](0143-ask-user-draft-until-submit.md), [0146](0146-ask-user-list-retract-replace.md) |
| [0128](0128-mcp-http-var-header-expansion-leak-surface.md) | MCP HTTP `${VAR}` header expansion is a documented, consented leak surface (#478, amends [0080](0080-mcp-streamable-http-transport.md)): `mcp::http::expand_env` resolves `${VAR}` in a configured server's static `headers` from the engine's **whole process environment**, with no allowlist — a header naming a provider secret (`Authorization: Bearer ${ZAI_API_KEY}`) sends that key's live value to the remote server on every request. Recorded as accepted, not a bug: per [0047](0047-local-trust-boundary.md) the config file is trusted and enabling a server is consent, matching the stdio transport's `env:` block (ADR-0124: "an explicit per-server `env:` entry still wins"). No code change. Constraint for future work: any future logging of resolved MCP HTTP request headers must redact expanded values — log the header name, never its post-expansion value (no such logging exists today). Rejected: allowlisting `expand_env` to the catalog's known `key_env`s (blocks legitimate custom-header use cases for an already-trusted surface), building a redact-by-default logging flag speculatively (nothing to gate yet) | Accepted |
| [0129](0129-thread-the-skill-mask-into-rhai-binding-resolution.md) | Thread the skill `allowed_tools` mask into `rhai` binding resolution (#477, amends [0106](0106-skill-scoped-allowed-tools-enforcement.md); ledger row 7, #396/#473): `BindingPolicy::capture` gains an `active_skill` parameter and folds a `skill_masked` snapshot alongside the existing agent mask — a binding excluded by the active skill's `allowed_tools` refuses with the same message shape a direct call gets, checked strictly after the agent mask, same ordering as generic dispatch. The mask is a one-time snapshot, not a live read: sound because `load_skill` is not itself a `rhai` binding, so nothing inside a running script can change which skill is active mid-run. Clears at the session's next `Done`, same as generic dispatch, since both routes read the same `ActiveSkill` map. New integration test in `tests/rhai.rs` covers both refusal (skill active) and pass-through (after `Done` clears it). Rejected: a live per-call read (nothing to observe that the snapshot doesn't already capture correctly), a distinct refusal message (the issue asked for the same shape the generic route uses), clamping the mask down the ancestor/spawn chain for bindings (would diverge from the generic route's deliberately session-local scope) | Accepted |
| [0130](0130-rhai-exec-bindings-marshal-workdir.md) | `rhai` `exec`/`bash` bindings marshal `workdir` (#480, amends [0115](0115-rhai-exec-bindings-call-bash.md)/[0116](0116-workdir-scoped-permission-rules-for-bash-call.md); ledger row 2, #396): `exec(command, args, workdir)`/`bash(command, workdir)` overloads marshal an optional `workdir` into the delegated `call`/`bash` tool's own `workdir` field — previously omitted entirely. Two independent fixes were needed: the field never rode along in the marshalled input, *and* `BindingPolicy::decide` graded through the workdir-blind `PermissionProfile::resolve` instead of `resolve_scoped` — fixed to extract `permission_workdir` and call `resolve_scoped` like the direct-call `ProfileResolver` already does. A `tool{pattern}` workdir-scoped rule (#425) now fires for a binding call exactly as for a direct `bash`/`call` call, and the escape-root gate (#446, [0119](0119-rhai-bindings-route-through-the-escape-root-gate.md)) picks up the same field with no separate wiring, since it already delegated to `permission_workdir`. The per-run `Ask` approval cache key also gained `workdir` alongside the command line, since a workdir-scoped rule can now grade the same command differently in two directories. Rejected: folding `workdir` into the existing argument via a separator or options map instead of a positional overload (Rhai's arity-based overload resolution already gives an unambiguous three/two-argument form with no new script-facing syntax), normalizing a relative `workdir` root-relative before grading (out of scope — ADR-0125 explicitly excluded `bash`/`call` from that normalization) | Accepted |
| [0131](0131-web-search-post-mvp-follow-ups.md) | Web search post-MVP follow-ups (#481, amends [0075](0075-provider-side-web-search-mvp.md), closes 3 of its 4 accepted MVP limitations): (1) **persisted results** — a new `ContentPart::ProviderSearch { provider, summary, data }` block (opaque `data`, round-trips verbatim only to the minting provider, mirrors `ToolCall.provider_meta`) joins the wire via additive `LlmEvent::ContentBlock` + a persisted seq-bearing `OutEvent::SearchResult`; the turn loop appends every round's blocks after its text when committing the assistant `Message`, replay folds `SearchResult` the same way; Anthropic's `request::anthropic_blocks` replays `data` verbatim only when `provider == "anthropic"` (the cache-benefit path), every other converter (OpenAI-compat, Gemini, Anthropic-on-a-foreign-block) renders only `summary` as plain text. (2) **`pause_turn` continuation, client-owned** — `anthropic::mod::stream()` accumulates every finalized content block into a raw array (`sse::handle_frame`, blocks stream strictly sequentially so one "current block" tracker suffices) and, on `pause_turn`, re-POSTs with that array appended as a fresh assistant turn, continuing the same `LlmStream` with no `Finish` in between, bounded by `MAX_PAUSE_CONTINUATIONS` (6); core never observes `pause_turn` — if the bound is hit, the ADR-0118 ambiguous-stop retry is the fallback. (4) **Anthropic tool-version as catalog data** — `ModelEntry.web_search_tool_version: Option<String>` selects the server-tool type string (`None` keeps the `web_search_20250305` fallback), resolved in the runtime alongside `WebSearchConfig` and threaded as a separate `anthropic_factory` parameter (not folded into `WebSearchConfig`, since it's a per-model capability, not a user search preference). (3) **z.ai streaming placement** — attempted verification against a live key, blocked by no key/network access in the implementing environment; stayed open until [0171](0171-zai-streaming-web-search-placement-confirmed.md), parser unchanged at the time, worst case still the cited-text-only floor. Also split the already-grandfathered-over-cap `anthropic.rs`/`openai.rs` into `{mod,request,sse}.rs` each (400-line file-cap gate) to land this without growing them further — both allowlist entries removed. Rejected: surfacing `pause_turn` as a new `StopReason` and driving continuation from `session/round.rs` (a provider-specific protocol has no meaning to core; ADR-0118 already composes as the generic fallback), typed per-provider `data` structs (no code path but the minting provider's own converter ever inspects it), folding `web_search_tool_version` into user-facing `WebSearchConfig` (conflates a per-model capability with a per-search user preference) | Accepted — Amended by [0171](0171-zai-streaming-web-search-placement-confirmed.md) |
| [0132](0132-glob-grep-escape-root-search-via-durable-grant.md) | `glob`/`grep` reach outside the project root by riding an existing durable `read` grant (#482, amends [0109](0109-escape-root-access-via-approval.md); closes ledger row 4, #396): search never forces its own approval prompt — `ExtraRootStore::is_durably_allowed_under(tool, path)` walks a path's ancestors, returning `true` as soon as one has a `Session`/`Always` grant, so a grant on a directory covers every descendant with no per-file re-approval. `host::list_files` gains `list_files_with_extra_roots` (the 3-arg `list_files` stays a `None`-store wrapper, byte-identical to before): a match whose canonical path escapes root is still dropped unless `is_durably_allowed_under("read", canonical_match)` — checked post-canonicalization, so a symlink inside the granted directory pointing outside it resolves to an ungranted path and is still dropped. Hardcoded to the `"read"` tool regardless of which of `glob`/`grep` is asking, matching the issue's proposed semantics; `Once` grants are structurally excluded (reusing the durable-only `is_durably_allowed` primitive) since a search's match count is unbounded ahead of time and a single-use token spending itself over arbitrarily many reads would defeat its own purpose. `GlobTool`/`GrepTool` gain the same `with_extra_roots` builder the other four escape-root tools have, needing no `SessionId`/`request_id` (no `Once` token to consume). `permission::escape_root_target` is untouched — the executor's `EscapeRoot` gate still never fires for `glob`/`grep`. `host/mod.rs`'s `list_files`/`FileList` extracted into a new `host/walk.rs` to stay under the 400-line file cap. Rejected: forcing a fresh per-external-root `ToolRequest` (more prompt fatigue for a capability the existing `read` grant already answers), widening to any of the six tools' grants instead of `read` specifically (leaks directory discovery a `write`/`bash` grant never approved), enumerating the store's grant set for prefix-matching instead of an ancestor walk (no enumeration API, by design), letting `Once` cover one search call (a search's result count isn't bounded ahead of time) | Accepted |
| [0133](0133-live-bash-enablement-graded-by-permission.md) | Live bash enablement, graded by the permission model (#498, builds on [0096](0096-dynamic-toolregistry-sharedregistry.md)/[0097](0097-live-mcp-server-management.md)): new `InMsg::BashEnable { grade: BashGrade }`/`BashDisable`, engine-global and trusted-only like `McpAdd`/`McpRemove` (#472, [0124](0124-wire-refused-mcp-mutation-and-stdio-key-scrub.md)), answered by `OutEvent::BashChanged` from a new `bash_live::spawn_bash_responder` mirroring `mcp::spawn_mcp_responder`. `BashGrade::Ask` (safe default, every call still prompts) or `Allow { pattern: Option<String> }` (blanket, or an argument-scoped `bash(pattern): allow` rule, #173). `LiveBashState` tracks registration + the live grade, seeded from the startup `ENTANGLEMENT_ENABLE_BASH` env var with **no** override (byte-identical to pre-#498 when bash was never live-enabled). `ProfileResolver` gained an opt-in `with_live_bash`: when a live grade is set, it overrides the session's own profile for `bash`/`bash_output` specifically (a profile authored before bash was live-enabled has no real opinion on it), still passed through the existing `clamp_to_base` config-ceiling clamp (#172) unconditionally — a ceiling of `bash: deny` still wins over a live `Allow`. TUI `/bash on [--allow [<pattern>]|--ask] \| off` mirrors `/mcp`'s raw-text re-parse; the `!bash` passthrough gate now reads `LiveBashState::is_enabled()` live instead of a startup-only snapshot. Enablement is process-wide (matching `SharedRegistry`'s own scope) and ephemeral (no `config.yml` persistence, unlike `Approve { scope: Always }`). Rejected: materializing the grade as an additional ceiling rule (the ceiling only tightens, documented as never loosening — contradicts an `Allow` grade), per-session live enablement (a second mechanism parallel to the process-wide registry, inconsistent with `/mcp add`), a `bash_output`-specific always-allow exemption (no command argument to scope a pattern against) | Superseded by [0163](0163-live-bash-enablement-is-a-tool-overlay-entry.md) |
| [0134](0134-per-profile-sandbox-scoping-and-spawn-chain-clamp.md) | Per-profile sandbox scoping + spawn-chain confinement clamp (#479, amends [0104](0104-bubblewrap-sandbox-for-bash-call.md); ledger row 1, #396): `AgentProfile` gains an opaque `sandbox: Option<String>` frontmatter field (`bwrap`/`none`/`inherit`, carried and serialized by core exactly like `permission`, interpreted only by the runtime's `host::sandbox`); `BashTool`/`CallTool` replace their fixed `SandboxPolicy` field with `sandbox_resolver: Arc<dyn policy::SandboxResolver>` (`fn resolve(&self, session: Option<&SessionId>) -> SandboxPolicy`), consulted per call — a fixed policy is trivially its own resolver, so the pre-#479 `.with_sandbox` builder and every existing call site keep compiling unchanged. `SandboxPolicy::resolve_profile_override` layers a profile's override over the process-global `ENTANGLEMENT_SANDBOX` default (`None` inherits, `bwrap` confines keeping the default's `network` posture, `none` forces unconfined); `most_confined` ranks confinement (unconfined < bubblewrap-with-network < bubblewrap-no-network) for the spawn-chain clamp, mirroring [0024](0024-subagent-permission-gating.md)'s least-privilege permission ceiling but computed once at `SessionStarted` from the parent's already-resolved effective policy (`policy::record_session_sandbox`/`record_own_sandbox`/`resolve_sandbox`) rather than a live per-call ancestor walk — composes transitively down an arbitrarily deep spawn chain with an O(1) read, frozen at spawn (a later `SetAgent` recomputes only the switching session's own policy, never its floor). `policy::SandboxConfig { base, own, floor }` bundles the shared state created once in `main.rs::build_config`, threaded into both `register_default_tools` (the resolver) and `spawn_tool_executor_with_policy` (the writer); `SandboxConfig::none()` is the zero-wiring default for every test helper. A live-enabled `bash` pair (#498, ADR-0133) shares the same resolver via `BashToolConfig.sandbox_resolver`, so a profile's `sandbox:` override applies uniformly whether `bash` was registered at startup or live-enabled later. Rejected: sharing `tool_runner`'s single-threaded `SpawnGuard` behind a `Mutex` for a live ancestor walk (bigger diff for a property — retroactive re-tightening on a parent's later profile switch — nothing asked for), a runtime-only sidecar table keyed by profile name instead of a core field (would need a `load_registry` signature change threaded through every caller; `AgentProfile` already carries one opaque runtime-interpreted field), a per-profile network-sharing override (unrequested scope; network stays the more conservative process-global-only knob) | Accepted |
| [0135](0135-deferred-build-speed-trims-tokio-rhai-syntect.md) | Deferred build-speed trims (#502, amends [0025](0025-runtime-cargo-feature-gates.md); touches [0046](0046-rhai-sandboxed-script-tool.md); ledger row 4, #396): (1) **per-crate tokio features** — the workspace `tokio` dependency drops `features = ["full"]` to a bare `tokio = { version = "1" }`; each crate adds only its own grep-verified surface (`entanglement-provider`: `rt`/`macros`/`sync`/`time`/`net`/`io-util`; `entanglement-core`: `rt`/`macros`/`sync`/`time`; `entanglement-runtime`: the same set plus `rt-multi-thread`/`fs`/`process`/`io-std`/`signal` — the head crate exercises nearly the whole surface). (2) **`rhai` behind a default-on `entanglement-runtime` feature** (`rhai = ["dep:rhai"]`, joins `default`) — `pub mod script;` and the two `script::rhai_spec()` advertisement sites gain `#[cfg(feature = "rhai")]` mirroring the existing `serve` feature's module-gating pattern; `tool_runner.rs`'s `Intercept::Rhai` route (variant + `classify` arm + handling arm) is individually cfg-gated, falling through to the generic `Permission` route's unknown-tool refusal when the feature is off — no new refusal path; a handful of now-conditionally-dead imports/parameters/functions (`AtomicBool`, `clamp_to_base`/`effective_permission`, the `base: PermissionProfile` parameter of `spawn_tool_executor_with_policy`, `permission::permission_chain`) get narrowly-scoped `#[cfg]`/`#[cfg_attr(.., allow(..))]` rather than a blanket module-level allow; `tests/rhai.rs` gets a `[[test]] required-features = ["rhai"]` entry mirroring `[[example]] mcp_http`. `LEAN_FORBIDDEN` is unchanged — `rhai` was never forbidden, just no longer forced-on for `--no-default-features`. (3) **trimmed `syntect` features** — `default-fancy` shrinks to `["parsing", "default-syntaxes", "default-themes", "regex-fancy"]`, dropping `html`/`plist-load`/`yaml-load`/`dump-create` (none used by `tui/markdown.rs`) while keeping the pure-Rust `regex-fancy` engine (not `onig`, which needs a C library — the wrong trade for ARM/RISC-V cross-compilation). None of the three change behavior. Rejected: routing `rhai` through the generic `Permission` dispatch unconditionally instead of cfg-gating (it resolves permission live via a captured binding-policy snapshot, not the pluggable `PermissionResolver` seam — collapsing the routes would change feature-on behavior), a blanket `#[allow(dead_code)]` instead of narrow per-item cfg (would silence `check-lean`'s clippy gate wholesale), `syntect`'s `default-onig` (C dependency, wrong cross-compile trade) | Accepted |
| [0136](0136-tui-attention-panel-signpost-and-jump.md) | TUI attention panel: a signpost + jump, not a second approval keymap (head-side only, no wire change; builds on [0014](0014-tool-approval-inline-modal.md)/[0061](0061-parked-turn-state-batch-tool-resolution.md) and the #488 question flow): a background session's parked `ToolRequest`/`UserQuestion` was tracked per-view but invisible — the approval/question UI reads only `active_view()`, leaving a bell and a bare status-bar `!`. A one-line panel above the input box (a `Length(0)` layout row while idle, so structurally absent) names the oldest waiting **background** session (8-char short id + agent + `needs approval: tool arg` / `question: text`) with a waiting count; `Ctrl+G` (intercepted ahead of approval/question routing, so it works while the active session is itself parked and never types a `g` into a reject reason) or a click switches to it, where the existing approval UI takes over unchanged. Active pending ⇒ transcript tail, background pending ⇒ panel — every parked request is visible somewhere. Aggregation (`ui::alerts::background_attention`) is derived per frame from the views' pending queues (not the `Status` flap, #273) in registry order — the same order the jump targets; the status bar shows `⚠ N` off the same function. Alongside: short ids replace full UUIDs in sidebar/status bar/sessions modal, the sidebar gains a dim first-prompt description line (`SessionView::first_prompt`, set on the first recorded user message, rebuilt by resume's Prompt replay) and distinct `needs approval`/`question` words, the sessions modal gains a `❓ question` badge, and sidebar rows + panel are click targets via a draw-time row map mirroring the chat hit-test capture. Rejected: an act-in-place panel with its own keymap (the scope keys are plain chars — it would need global plain-char interception while typing or a duplicated approval+question keymap, and free-text answers still borrow the input box), auto-switch on request arrival (yanks the user mid-typing, destructive to a half-typed reject reason), a protocol-level aggregate event (purely a head rendering concern) | Accepted |
| [0137](0137-explore-ask-grade-shell-access.md) | `explore` gains `Ask`-grade shell access (amends [0038](0038-physical-per-agent-tool-restriction.md)'s read-only mask and [0024](0024-subagent-permission-gating.md)'s reference read-only posture): the built-in `explore` profile advertised only `read`/`glob`/`grep` and `default: deny` hard-refused everything else, so a read-only delegation needing one `git status`/`git diff` (which the read triad cannot produce) was a dead-end — the agent had no escalation path short of re-delegating to the much more permissive `debug` (`default: allow`, no mask). Its mask widens to `[read, glob, grep, call, bash, rhai]` with the exec triad graded `Ask` — each exec call parks at `WaitingApproval` for explicit user approval (`y`/`s`/`a`/reject, ADR-0052), never auto-runs. The read-only safety property is preserved at the profile level: `edit`/`write`/`apply_patch` and `agent_spawn` stay mask-absent and `Deny` (never mutated, never spawns), `default: deny` stays the floor for everything not enumerated, and the approved command line is shown verbatim in the prompt. `rhai` joins the triad because its `exec`/`bash` bindings (ADR-0115) route through the same grade. Rejected: leaving `explore` strictly read-only and routing one-command inspections to `debug` (a large privilege escalation for a small need, and pushes the target choice back onto the model), grading exec `Allow`/silent (inverts the read-only guarantee the profile exists to provide), admitting only `call` (asymmetric — `bash` pipelines and `rhai` bindings would still deny), a new `inspect` profile with shell (multiplies the built-in surface for no safety gain over `Ask`-grading `explore` itself) | Accepted |
| [0138](0138-sponsored-build-child-and-propose-plan-cycle.md) | Sponsored build child — `propose_plan` approval spawns `build` as a **sponsored** child of `plan` (amends [0042](0042-plan-acceptance-via-propose-plan-approval-roundtrip.md)'s fresh-root head-policy handoff and [0024](0024-subagent-permission-gating.md)'s ancestor clamp): a sponsored child has a parent-child link (result return, lifecycle, session-tree visibility) but its permission resolution **stops at the child** — it runs with its own profile's permissions, no ancestor walk, so a `build` child of a read-only `plan` session runs full write tools. Authorization = user plan approval. The build's answer folds back as the `propose_plan` tool result, enabling plan/build cycling. `SpawnGuard` gains a `sponsored: HashSet<SessionId>` + `record_sponsored_start`/`is_sponsored`; `effective_permission`/`resolve_with_source`/`ancestor_chain`/`permission_chain`/`tool_masked` all short-circuit at a sponsored session. Sponsored spawns are exempt from `MAX_SPAWNS_PER_ROOT` (sequential, user-authorized) but not `MAX_SPAWN_DEPTH`. The SpawnGuard mutation happens in the tool executor's single-threaded loop before the detached task; `InMsg::Spawn` protocol is unchanged (sponsorship is runtime-local, unforgable from wire). The TUI's `send_approval`/`handoff_accepted_plan` head-policy handoff is deleted — pipe/WS heads get correct behavior automatically. Rejected: keep fresh-root + a new cycle protocol message (multiplies surface, leaves the pipe/WS bug), a `propose_plan`-specific permission exemption in the resolver (special case the clean ladder avoids), lifting `MAX_SPAWN_DEPTH` for sponsored (depth bounds recursion) | Amended by [0145](0145-one-plan-tool-file-backed-plans-and-blocking-review-loop.md), [0162](0162-agent-send-supervising-a-sub-agent.md) |
| [0139](0139-visible-session-states-working-and-waitingagent.md) | Visible session states — `Working` + `WaitingAgent` added to `AgentState`, and `Done` becomes the resting state (amends [0072](0072-protocol-warts-settled-before-serve.md)): pre-ADR-0139, tool execution and sub-agent parking both rendered as `Thinking` (a bash command running looked identical to the LLM generating), and a cancelled turn emitted `Idle` (same as never-run-yet). `Working` is emitted by `drive_turn` after `RoundOutcome::Parked` (the batch's `ToolExec`s are out, `ToolResult`s pending); `WaitingAgent` is emitted by `subagent::launch` (blocking `agent`) and `propose_plan::launch_sponsored_build` (ADR-0138) right before parking — the sponsored build child makes this state load-bearing. `SessionCmd::Stop` and the mid-stream `Stop` cancel now emit `Done`, leaving `Idle` for the genuine session-start case alone. The TUI ship-cruise animates during `Thinking`+`Working`; `attention_word` (derived from pending queues) still takes precedence over the state word. Rejected: derive `Working` head-side from `ToolExec`/`ToolOutput` pairing (forces every head to re-implement the inference), keep `Idle` as resting state (overloads it), a single `Busy` covering both (a tool executing vs a sub-agent wait render differently) | Accepted — Amended by [0144] (pause/resume) |
| [0140](0140-per-model-concurrency-cap-layered-on-endpoint-cap.md) | Per-model concurrency cap, layered on the endpoint cap (#521, amends [0111](0111-adaptive-endpoint-pacing-and-429-retry-until-clear.md)/[0122](0122-per-provider-concurrency-and-rpm-as-catalog-data.md); builds on the mixed-model scenario [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md) makes normal): z.ai enforces concurrency per **model**, not per endpoint (`glm-4.7-flash: 1` vs `glm-5.2: 5`, same base URL/key), which one endpoint-wide cap cannot express without either 429-storming the tighter model or needlessly serializing the looser one. `ModelEntry` gains an optional `concurrency` (catalog data mirroring `ProviderEntry`'s; YAML-only in v1, no env override — model ids contain `.`/`-` with no established env-mangling rule). `EndpointState` gains a second, lazily-created `Semaphore` per `(endpoint, model)` (`model_concurrency: Mutex<HashMap<String, Arc<ModelSlot>>>`); `HttpClient::execute_with_retry` takes `model`/`model_concurrency` and, when the latter is `Some`, acquires **that model's permit first, then the endpoint-wide one** (release in reverse) — a caller blocked on its own saturated model never holds the scarce endpoint permit hostage, which would otherwise starve sibling models sharing the endpoint even with room to spare — both riding one `StreamGuard` and releasing together. The endpoint cap stays the ceiling on the *sum* across every model (unchanged: every request still takes an endpoint permit); a model cap wider than its endpoint's is legal but logged as a likely misconfiguration (warn, not error). A model with no catalog cap never acquires a model permit — byte-identical to pre-#521. `ThrottleStatus` gains `model: Option<String>`, reporting whichever cap (endpoint or a per-model slot) currently has the tighter occupancy ratio. The 429 cool-down and pacing gate stay endpoint-wide (v1) regardless of which model triggered them. Rejected: one endpoint-wide cap tuned to the tightest model (wrong for every other model), a separate `HttpClient`/pool per model (throws away the endpoint-wide RPM/429 coordination ADR-0050/ADR-0111 exist for), acquiring the endpoint permit before the model permit (reintroduces the cross-model starvation case above), rejecting an over-endpoint model cap at load time (the config isn't unsound, just inert), scoping the 429 cool-down to the offending model now (would need parsing an unverified provider error-body shape), a `{NAME}_{MODEL}_CONCURRENCY` env override now (no established id-mangling precedent) | Accepted |
| [0141](0141-wire-visible-throttle-transitions.md) | Wire-visible throttle transitions (#517, amends [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)/[0111](0111-adaptive-endpoint-pacing-and-429-retry-until-clear.md)): the provider's per-endpoint resilience pool was visible only to the TUI, which polls `HttpClient::throttle_status()` directly — a stdio/WS head saw an opaque `Thinking` stall for up to `rate_limit_max_elapsed` (~15 min) under a 429. `ThrottleStatus` gains `next_request_in: Option<Duration>` (the AIMD pacing gate's own next-slot countdown, surfaced only while `penalized`) and a new `HttpClient::throttle_statuses() -> Vec<ThrottleStatus>` snapshots every endpoint (not just the most-throttled). `OutEvent::Throttle { endpoint, throttled, in_flight, cap, retry_in_ms?, pacing_in_ms? }` joins the protocol as an **engine-global, no-`seq`** lifecycle event (`session()` → `None`, mirroring `McpChanged`/`BashChanged`) — per-endpoint, not per-session, since the pool's whole point is that one throttled endpoint never blocks another. `entanglement-runtime::throttle::spawn_throttle_responder` polls every 500ms and emits **only on a transition**, not every tick. Rejected: attach a `session` to `Throttle` (no principled single session to name when several share an endpoint), a new per-session `AgentState::RateLimited` (the resilience layer is deliberately per-endpoint, not per-session), emitting on every poll tick (would spam ~1800 events for one worst-case stall) | Accepted |
| [0142](0142-trusted-scratch-dir-and-plans-folder-carve-outs.md) | Trusted scratch dir + plans-folder carve-out (#524, amends [0109](0109-escape-root-access-via-approval.md); amends the physical read-only mask from [0041](0041-update-plan-ownership-default-closed.md)/[0049](0049-plan-task-tools-as-runtime-state-tools.md)): `ExtraRootStore` gains an optional `scratch: Option<PathBuf>` (set once at startup from `session_store::scratch_dir`), consulted first in both `is_durably_allowed`/`take_allowance` — so the runtime's own scratch dir (already the default `call`-output target) needs no approval and no prior grant, for any tool, in any profile, and composes for free with the existing `is_durably_allowed_under` search-widening (ADR-0132). Directory-prefix (`starts_with`), not per-tool and not persisted — a property of the scratch location, not a user decision. The generated `<env>` system-prompt block now names it, steering the model off `/tmp`. Separately, the built-in `plan` profile's frontmatter adds `write`/`edit` to its `tools:` allowlist plus `write: deny` / `write(.entanglement/plans/*.md): allow` — the `write` capability key (#418) fans both bare and argument-scoped forms out to `edit`/`write`/`apply_patch`, so one YAML rule carves out the plans folder #513's unified plan tool writes into while leaving the rest of the read-only mask intact; reuses the existing argument-scoped rule language (#173) rather than a new mask-exception mechanism. The plans-folder carve-out still clamps under the config permission ceiling (#172) like any other rule; the scratch-dir carve-out lives inside escape-root containment, which the ceiling's `clamp_to_base` never touches, so a ceiling can still deny the tool outright but can't re-impose the escape-root prompt for it. Rejected: a config-level trusted-directories list (broader surface than the two built-ins need for v1), a new `AgentProfile` mask-exception field for the plans folder (duplicates what argument-scoped permission rules already do), widening the per-tool escape-root grant itself to directory-prefix matching (would change `is_durably_allowed_under`'s narrower, search-only semantics for every grant) | Accepted |
| [0143](0143-ask-user-draft-until-submit.md) | `ask_user` answers are drafts until an explicit Submit (#518, amends [0127](0127-ask-user-v2-multi-question-envelope.md)): each question's `Enter`/number-pick used to commit *and send* immediately once it was the batch's last question, with no way to revise an earlier answer short of aborting the turn. `PendingQuestion.answers` becomes `Vec<Option<Vec<String>>>`, written in place (not pushed) so revisiting a question overwrites its draft; `current == questions.len()` is a new terminal review/submit step rendering every question paired with its draft, whose `Enter` is the one explicit Submit and whose `Esc`/`←`/`Backspace` steps back to revise instead of sending; `Left`/`Backspace` step back a question outside free-text entry, restoring the on-screen selection from that question's draft. A single-question call gets the same review step for consistency. No wire/protocol change — `AnswerQuestion` is unwrapped from the drafts only at Submit. Rejected: auto-submit-last-question plus an undo key (only revises the most recent answer), skip the review step for single-question calls (breaks the issue's own consistency bar), post-submit revision via rewind/fork (out of scope; the pragmatic recourse is a corrective mid-turn `Prompt`, ADR-0058) | Accepted |
| [0144](0144-pause-resume-a-hold-between-cancel-and-hibernate.md) | `PauseSession`/`ResumeSession` — a hold between cancel and hibernate (#516, amends [0017](0017-stop-cancels-turn-not-session.md)/[0077](0077-session-hibernation-evictable-resumable.md)/[0139](0139-visible-session-states-working-and-waitingagent.md)): neither `Stop` (destroys the in-flight round) nor `HibernateSession` (evicts memory) covers "hold this session's work without losing it or evicting it." New `AgentState::Paused` plus a `Session.paused: bool` (never persisted/replayed, like `Stop`'s cancel) gates two things: an **idle** session's next `Prompt`/`SetAgent`/`SetModel`/`SetGeneration`/`Oneshot` is deferred onto the existing turn-stash queue (gated by `s.turn.is_some() || s.paused` everywhere those already gate on the turn alone, with the stash-pop condition gaining a matching `&& !s.paused`); a **parked** batch's arriving `ToolResult`s still resolve and fold immediately (stashing them would deadlock — the stash only drains once the turn goes idle, which needs every pending result resolved first) but the drained batch's continuation into the next model round-trip is what's held back, resuming with no new prompt on `ResumeSession`. Mid-stream pause needed **zero changes to `stream.rs`**: `Pause`/`Unpause` are ordinary `SessionCmd`s, so a mid-stream arrival rides the existing generic stash-and-replay-at-next-safe-point mechanism `SetAgent`/`SetModel` already use — deferred-until-safe, not a true interrupt-and-checkpoint (the issue's scope note permits shipping pause-while-parked-only for v1; this ships a bit more than that without touching the hard mid-stream case). `Stop`/`Hibernate` always win regardless of `paused` and neither clears it — `Stop` reports `Paused` instead of `Done` if still held; `Hibernate` tears down unconditionally, and since `paused` isn't persisted, a resumed-after-hibernate session always comes back unpaused. Wire-allowed like `Stop` (no elevated capability). Rejected: per-turn pause (nothing coarser than the whole session's next round to scope a hold to), true mid-stream freeze via buffer-flush-hold or cancel-and-checkpoint (needs primitives ADR-0017 deliberately doesn't have), reusing `Stop` with a "don't discard" flag (conflates two orthogonal intents, mirroring ADR-0017's own rejection of a distinct `Cancel`), persisting `paused` (it's loop control, not conversation content), blocking `Stop`/`Hibernate` while paused (adds a precondition with no safety benefit, mirroring ADR-0077's rejection of the analogous hibernate-refusal case) | Accepted |
| [0144](0144-file-backed-shared-endpoint-state-across-instances.md) | Shared endpoint resilience state across `skutter` instances (#523): two processes talking to the same `(endpoint, API key)` used to each apply the full RPM/concurrency budget independently, together sending up to N× the configured rate. `EndpointState` now also consults a file-backed cross-process gate (`client::shared_state::SharedGate`, `${data_dir}/entanglement/endpoints/<sha256(pool_key)>.state`, `fd-lock`-guarded like the managed config files, #329/ADR-0084) covering the RPM ledger, a lease-based in-flight concurrency count (crash-safe via TTL — no daemon, no live cross-process semaphore), and the `Retry-After` cool-down; the AIMD pacing gate stays per-process (v1). Falls back to pure in-process gating when the state directory is unwritable or via the explicit `ENTANGLEMENT_NO_SHARED_ENDPOINT_STATE=1` opt-out. *(Note: this ADR and 0144-pause-resume above collided on the same number — both merged 2026-07-31 from independent PRs; left as-is rather than renumbering an already-Accepted ADR post hoc.)* | Accepted — Amended by [0156](0156-normalize-and-stabilize-the-endpoint-pool-key.md) |
| [0145](0145-one-plan-tool-file-backed-plans-and-blocking-review-loop.md) | One plan tool: `propose_plan(content \| path)` (#513, supersedes [0049](0049-plan-task-tools-as-runtime-state-tools.md)'s `update_plan` half, amends [0138](0138-sponsored-build-child-and-propose-plan-cycle.md)/[0042](0042-plan-acceptance-via-propose-plan-approval-roundtrip.md)): `update_plan` removed entirely; `content` materializes `.entanglement/plans/<short-id>.md`, `path` binds an existing one — either way resolved content rides `OutEvent::Plan`'s new `path` field, the `ToolRequest.input` JSON, and the eventual tool result before/around the unconditional `Ask` force-park. A content-hash-only staleness guard (`plan_files::PlanFileRegistry`, kept fresh by `propose_plan` itself plus a passive `OutEvent::FileChange`-broadcast listener attributing in-session edits) refuses a `path` resubmit the user touched out of band, while never tripping on the agent's own `write`/`edit` between build phases. The already-blocking sponsored-build wait (ADR-0138) is registered with `CancelRegistry` for the first time, so `Stop` on the plan session now actually detaches (child keeps running); cascading to the child is just a second ordinary `Stop`, no new protocol. TUI: the truncated "Plan Outline" side panel is dropped for a one-line `Plan: <path> (pending\|accepted)` indicator plus `/plan` opening the bound file in `$EDITOR`. Deferred (ledger): a TUI stop-cascade-vs-detach confirm modal (no general turn-interrupt keybinding exists yet to hang it off) and a watcher-driven "plan updated by user" transcript notice | Accepted |
| [0146](0146-ask-user-list-retract-replace.md) | `ask_user` gains list/retract/replace of open questions (#515, amends [0027](0027-ask-user-interactive-prompt.md)/[0127](0127-ask-user-v2-multi-question-envelope.md)): `InMsg::ListQuestions{correlation_id,session?}` → `OutEvent::QuestionList` mirrors `ListSessions`/`McpList` (session-less snapshot query, `session` a result filter not a routing target), answered by a new runtime-owned `OpenQuestions` registry (`questions.rs`) since the generic `PendingDecisions` map carries no question content. `InMsg::RetractQuestion{session,request_id}` withdraws one question without cancelling the turn — a first-class `seam::Decision::Retract` the `ask_user` orchestrator replies to with a withdrawal note, not a silent unwind like `Stop` (the re-register-drops-the-waiter side effect the issue floated as an alternative resolves to silence, which is unsound outside a whole-turn cancel). `InMsg::ReplaceQuestion{session,request_id,questions}` swaps content in place — `seam::Decision::Replace` is *not* terminal, so `run_ask_user` is restructured into a loop that re-emits `UserQuestion` under the same `request_id` and re-parks rather than replying, keeping the swap invisible to core (no second `ToolExec`/`ToolResult` round-trip). All four wire-allowed (list is read-only like `ListSessions`; retract/replace are no more privileged than `AnswerQuestion`, which already is); the `serve` head's per-connection approval-ownership gate (#402, ADR-0107) is extended to cover retract/replace alongside answer. Rejected: relying on the re-register side effect alone for retract (resolves to silence, unsound for a targeted retract since the turn keeps running), minting a new `request_id` for a replace (would force a pointless second `ToolExec` round-trip core has no reason to run), folding `OpenQuestions` into `PendingDecisions` (three of its four consumers have no question content to store) | Accepted |
| [0147](0147-multi-user-mode-embedder-api.md) | Multi-user mode — session-scoped identity + embedder-API per-user seams (#522): a `UserId` newtype (`entanglement-provider`, re-exported by core) rides `Session.user`/`InMsg::Spawn.user`/`OutEvent::SessionStarted.user`/`SessionInfo.user`, spawn-time-fixed exactly like `parent` — a child/compaction-successor inherits its parent's/predecessor's user from `session_meta` rather than being re-told, so every existing spawn call site needed only the new mandatory field, not new logic. `ModelResolver` widens to `Fn(Option<&UserId>, &str, &str) -> ...` (the three call sites in `session.rs` already had the session in scope; single-user callers ignore the parameter). v1 ships as the **embedder library API only** — `serve` stays exactly ADR-0048-scoped (local, single-user, unauthenticated); an authenticated multi-user wire head is deferred to its own future ADR. `entanglement-runtime::multi_user::provider` gives each user their own `Catalog` (so per-user RPM/concurrency is just per-user catalog data) + API keys held only in memory, **never** `std::env` — ADR-0050's pool (keyed by base URL + API-key hash) gives per-user rate-limit isolation for free once a per-user key reaches the request path. `entanglement-runtime::multi_user::permission` builds `PerUserPermissionResolver`/`PerUserGrantStore` on the existing #311 `PermissionResolver`/`GrantStore` seams with no core change: a per-user ceiling composes (least-privilege) on top of any process-global one via the same `clamp_to_base` #172 uses, and `Always`-scope grants are keyed by `UserId` so one user's grant structurally cannot leak to another's session. A `SessionUserRegistry` the embedder populates itself (it already knows the mapping from sending the session's `Spawn`) is the shared session→user directory both consult. Rejected: per-frame identity (session already is the continuous identity boundary for every other seam), one `Holly` per tenant (defeats "one running engine serving multiple users"), baking per-user state into `EngineConfig` directly (belongs behind a resolver seam, mirroring `ToolSpecResolver`/`SystemPromptResolver`), shipping the authenticated `serve` head in the same change (a distinct wire-trust design deserving independent review) | Accepted |
| [0148](0148-glob-patterns-in-the-agent-tool-mask.md) | Glob patterns in the agent tool mask (#537, supersedes [0038](0038-physical-per-agent-tool-restriction.md)'s "name-based, no globbing" consequence, composes with [0117](0117-mcp-tool-capability-fan-out.md)): `tools:`/`disallowed_tools:` entries become `*`/`?` wildcard patterns matched by the same `glob_match` the #173/#425 permission scopes use — a literal entry degenerates to exact equality, so every existing profile is unchanged, while `"mcp__*"` / `"mcp__docs__*"` finally let a masked profile hold MCP tools whose namespaced names are unknowable at authoring time and don't even exist at parse time (`load_registry` runs before `mcp::connect`; live `McpAdd` mints names mid-process). Matching stays dynamic at advertisement/dispatch time (never parse-time expansion), confined to `AgentProfile::advertises_tool` — now delegating to a public `AgentProfile::mask_allows` so the TUI tools-dialog seeds through the real predicate instead of a duplicated exact-match copy — so core's spec filter, the runtime's `tool_masked` ancestor clamp, and the rhai binding mirror all inherit it from one change, and core stays MCP-unaware. Carve-outs: `plan_tasks::explicitly_allowlists` stays literal-exact (`tools: ["*"]` never grants plan authorship, mirroring `tools: None`); skill `allowed_tools`, permission tool-name keys, and `spawnable_agents` stay exact; built-in `plan`/`explore` masks unchanged (an MCP tool can be arbitrarily write-capable) — the recipe is a user-layer override; the #330 dialog still saves the concrete checked set (a glob doesn't survive the checklist round-trip). Rejected: a dedicated `mcp:` frontmatter key (second mechanism, subsumed by server-scoped globs), parse-time capability-style fan-out (misses live adds), a reserved `mcp` pseudo-capability (MCP-aware core, no per-server granularity) | Accepted |
| [0149](0149-per-session-tool-overlay.md) | Per-session tool overlay — live injection past the agent mask (#539, builds on [0148](0148-glob-patterns-in-the-agent-tool-mask.md)): `InMsg::SetToolOverlay { session, entries: Vec<ToolOverlayEntry { pattern, allow, deny }> }` (full replacement, empty clears; `SetGeneration`-shaped — always succeeds, stash-deferred during a live turn, **trusted-only/wire-refused** per the `BashEnable` rationale since `allow: true` grants no-prompt tools) makes matching tools *exist* for one session regardless of the active profile's allowlist and denylist — the "enable this MCP server in this session, now" gap ADR-0148's static per-profile globs left open. Confirmed by `OutEvent::ToolOverlayChanged` (no-`seq`, full effective list, persisted + replay-folded by overwrite like `GenerationChanged`); `Session.tool_overlay` survives `SetAgent` by design and dies with the session. Enforced on both seam sides from one predicate: core's advertisement filter ORs the overlay with `advertises_tool`; the runtime's `tool_masked` admits **per link** (a parent's overlay covers its spawn sub-tree, a masked child stays masked without its own entry); the generic dispatch route replaces the chain's grade with the entry's Ask (default)/Allow — applied in the executor ladder after the pluggable `PermissionResolver` (seam untouched for embedders), still ceiling-clamped (#172), grants/escape-root composing unchanged. Plan authority, skill mask, spawn gating, sandbox clamp untouched. TUI: `/enable mcp <server>` \| `/enable tool <name>` [--allow] (upsert enable), `/disable` upserts a deny (bare = reset), bare `/enable` opens the session-tools checklist dialog (overlay = diff against the profile mask), `/mcp` panel `e`/`d` toggles the highlighted server per session. A `deny: true` entry withdraws even a profile-advertised tool (deny > enable > profile), making per-session disable symmetric with enable. Deferred (ledger): rhai binding *grades* (mask half covered), child-session grade override. Rejected: ADR-0076 resolver composition (deliberately never bypasses masking), mutating the folded `AgentProfile` (reverted by the #156 self-heal), wire merge ops (full snapshot keeps replay = overwrite), wire-allowing for `serve` (fails ADR-0124's bar) | Accepted — Amended by [0163](0163-live-bash-enablement-is-a-tool-overlay-entry.md) |
| [0150](0150-search-tool-cli-ergonomics.md) | Search-tool CLI ergonomics (#540, amends [0016](0016-host-tool-empty-result-contract.md)): session-log analysis showed 57% of `grep` calls returning silent empty strings, driven by CLI/Claude-Code-trained expectations the tools mismatched. All runtime-`host`-side, no wire change: a directory-named `glob`/`grep` pattern auto-expands to `dir/**/*` (containment-gated, since a bare directory matched zero files by construction ADR-0016's rejected-rewrite concern doesn't apply); hand-rolled brace-set `{a,b}` expansion (cap 64 alternatives) for `glob.pattern`/`grep.path`/`exclude`; zero matches return an explanatory hint line instead of the empty string; walk/match caps annotate truncation; unknown input fields and out-of-root drops are called out instead of ignored; `grep` gains case-insensitivity. Permission grading (#173 argument scopes) grades the *pre-expansion* pattern the user-visible string (#540/its own note in the brief) | Accepted |
| [0151](0151-settable-session-metadata.md) | Settable session display metadata (name + action): `InMsg::SetSessionMeta { session, name?, action? }` merges a display title and a live "what the agent is doing" line onto `Session.name`/`.action` (`None` leaves a field, `Some("")` clears), always acked by `OutEvent::SessionMetaChanged` with the full merged state (seq-less, persisted, replay-folded by overwrite — the `GenerationChanged` shape). Applied **immediately, never stashed** (the `ChildSpawned` path, not `SetGeneration`'s stash gate) since `action` exists to change mid-turn. Wire-allowed (cosmetic); deliberately not mirrored into `SessionInfo` (creation-time directory); TUI `/name <text>`, sidebar/modal prefer name over short id and action over the first-prompt snippet. No LLM auto-namer in-tree — an external namer is the intended writer (since amended: 0154's aux-model session-title generator ships in-tree) | Amended by [0154](0154-per-purpose-auxiliary-models.md) |
| [0152](0152-provider-bundled-mcp-servers-three-state-enablement.md) | Provider-bundled MCP servers + three-state enablement (#542): `ProviderEntry.mcp_servers` makes first-party tool servers **catalog data** (z.ai `web_search_prime`/`web_reader`/`zread` in the embedded defaults, streamable HTTP, `Bearer ${ZAI_API_KEY}`, #426 `read` hints), key-gated **live** (a `/key` save unlocks with no restart; keyless ⇒ silently absent). Every server gains `McpServerState` — `enabled` (startup-connect, the user-entry/legacy default), `allowed` (*available*: lazily connected + session-scoped on demand via TUI `/enable mcp <name>`, the `/mcp` panel `e` key, or the new profile-graded `mcp_enable` tool whose schema `enum`s the live roster; the bundled default), `disabled` (invisible) — resolved by `effective_state()` over the legacy `disabled` bool. Enablement is session-ephemeral: `available::enable_for_session` is `mcp_add` minus persistence (bundled servers never touch `ServerConfigs`/`save_mcp`; a user `mcp:` entry field-merges over a same-name bundle), visibility enforced in the runtime's `tool_spec_resolver` (specs of a lazily-connected server filter to enabling sessions — no core change); `McpServerStatus` gains optional `state`, `McpList` appends available-unconnected entries, `McpRemove` of a bundled name disconnects instead of erroring. Rejected: hardcoded runtime table (violates #118), auto-connect on key presence (surprise tool-surface change), persisting enablement (a session decision, not config), #539-overlay scoping (an overlay admits past the mask, it can't hide from other sessions) | Accepted |
| [0153](0153-mcp-server-oauth.md) | MCP server OAuth + the MCP client mechanism moves to `entanglement-provider` (tui-ux-batch Issue 3): an optional `oauth:` block on an MCP server entry — present *even empty* — switches it from static-header auth to a browser-obtained bearer token, because most remote MCP servers are OAuth-protected and issue **no** pre-registered `client_id`, so a hand-configured endpoint block would have worked for almost none of them. Endpoints resolve by **RFC 9728** protected-resource metadata (off the `401` `WWW-Authenticate: resource_metadata` pointer, else the well-known path) chained into **RFC 8414** AS metadata; **RFC 7591** dynamic client registration mints a *public* client (`token_endpoint_auth_method: none` — no secret is confidential in a local CLI, which is what OAuth 2.1 + PKCE is for); **PKCE S256** mandatory, `plain` never offered, an AS advertising neither is refused not downgraded. Every field is an override; `authorization_url` + `token_url` short-circuits discovery. RFC 8252 loopback redirect caught by a one-request listener hand-rolled on `tokio::net` (bound `127.0.0.1`, mismatched `state` refused outright — not "keep waiting", which would defeat the CSRF check), bound *before* registration since the ephemeral port is part of the declared `redirect_uri`. `InMsg::McpAuth { name, action: Connect\|Check\|Disconnect }` → `OutEvent::McpAuthChanged` is engine-global and **wire-refused**, sharpening [0124](0124-wire-refused-mcp-mutation-and-stdio-key-scrub.md): a forged connect opens a browser and mints a durable credential, a forged disconnect destroys one, and even `Check` mutates state by refreshing — so unlike `McpList` none of the three is wire-allowed; a `Connect` emits twice (interim `authorize_url`, then outcome) and runs detached from the responder loop. Credentials persist in managed `mcp-tokens.yml` (`0600`, atomic + `fd-lock`, `ENTANGLEMENT_MCP_TOKENS_FILE`) **with the resolved endpoints + client id**, so startup connects skip discovery/registration entirely; `Debug` redacts every secret. Startup never opens a browser — an unauthenticated OAuth server is skipped non-fatally and reported `needs auth` in `/mcp list`. Browser launch is the runtime's job (keeps process spawning out of the leaf crate) and is deliberately **not** opt-in — the user typed `/mcp connect`, and the URL is always reported anyway so a failed launch degrades to "copy this link". Structurally, the streamable-HTTP transport + shared JSON-RPC helpers move to `entanglement-provider::mcp` (mechanism) while config/registry/permissions/token file/browser stay runtime-side (policy); stdio stays in the runtime (it needs the #164 key scrub); the runtime drops its direct `reqwest` dep and `mcp-http` becomes a pure compile gate. Rejected: hand-configured endpoints only (works for almost no real server), auto-launching a browser at startup (intrusive, breaks headless/CI), a separate `/mcp register` command (DCR belongs inside `connect`), pulling `axum` into the provider crate for the redirect. Deferred: device-code flow, per-user credentials under [0147](0147-multi-user-mode-embedder-api.md) (the store is process-global, keyed by server name), OAuth for LLM provider endpoints, cross-process refresh leasing (the file lock serializes the write, not the exchange) | Amended by [0157](0157-mcp-http-transport-shares-the-endpoint-pool.md) |
| [0154](0154-per-purpose-auxiliary-models.md) | Per-purpose auxiliary models for side transformations (tui-ux-batch Issue 5): a managed `aux-models.yml` (`ENTANGLEMENT_AUX_MODELS_FILE`, sibling of `agent-models.yml`) maps `purpose → {provider, model}` for a closed enum (`summarize`, `session_title`), so a cheap/fast model can serve compaction summaries and session titles while the turn loop keeps the strong one. Keyed by **purpose, not profile** — neither [0081](0081-per-profile-model-pinning-and-rebind-on-set-agent.md)'s per-profile pin nor [0094](0094-reasoning-effort-and-per-profile-generation-persistence.md)'s per-profile generation can express it, since a side transformation happens *within* whatever profile is active. A runtime-owned `AuxLlmRegistry` resolves a pin through the same catalog `ModelResolver` closure `SetModel` drives, inheriting the warm per-endpoint pool ([0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)). **Two consumers with deliberately different fallbacks** (the asymmetry is the point, not an inconsistency): the runtime-side session-title generator has no session backend in hand so it falls back to the *primary* model, while session compaction — which runs inside core — falls back to the *session's own* `llm`/`model`/`generation`, so a live `/model` switch ([0063](0063-realtime-model-provider-switch.md)) keeps applying to it. Compaction reaches the pin via a new `EngineConfig::aux_llm_resolver` (`Arc<dyn Fn(&str) -> Option<ResolvedModel>>`, shaped like `GenerationResolver` but carrying `ResolvedModel` so the one-shot client is built from the same `llm_factory`); core knows only the purpose *string* (`AUX_PURPOSE_SUMMARIZE`), never the `Purpose` enum or the pin file, and an unrecognized key is inert rather than fatal. Covers both the `/compact` oneshot op and the ADR-0103 auto-summarize overflow path (an overflow recovery is a side transformation too). Also ships the auto session-title generator (best-effort, detached; `/name`/[0151](0151-settable-session-metadata.md) always overrides) and the `/aux-model` command. Rejected: threading an aux `Llm` through the protocol (core would carry a second per-session backend for a rare call), intercepting compaction runtime-side (would duplicate the transcript render, context-budget guard, and keep-tail clamp outside core), extending `AgentProfile` with the pin (not a profile trait). Deferred: a `narrate` purpose (stream-render concern, not an LLM call), per-user pins under [0147](0147-multi-user-mode-embedder-api.md) | Amended by [0158](0158-defer-session-title-aux-call-under-contended-primary-concurrency.md) |
| [0155](0155-errored-subagent-turn-parks-for-steering.md) | An errored sub-agent turn parks the parent for steering, not unblocks it (#562): `collect_child_answer` (`entanglement-runtime/src/subagent.rs`, shared by the blocking `agent` tool, `agent_spawn`'s detached watcher feeding `agent_poll`, and the [0138](0138-sponsored-build-child-and-propose-plan-cycle.md) sponsored build wait) no longer treats a child turn's `Done` as final by itself — `emit_turn_error` fires `Error` *then* `Done` for a failed turn (#434's deliberate pairing), so a bare `Done` break let a transiently-erroring child (e.g. an exhausted [0111](0111-adaptive-endpoint-pacing-and-429-retry-until-clear.md) 429 budget) unblock its whole ancestor chain with `"sub-agent ended with error: …"` folded in as if it were a real answer. Now: a `Done` with no accumulated text *and* an `Error` on that turn clears the per-turn state and keeps watching — the child session is still alive and steerable (prompting "continue" starts a new turn) — emitting an explanatory `OutEvent::Error` on the *parent* (no new wire type) so the user knows to steer it. The wait ends only on a `Done` carrying a usable answer, or the child's `SessionEnded`/`SessionHibernated` (a lagging/closed watcher still breaks defensively). No new `AgentState`; `WaitingAgent` ([0139](0139-visible-session-states-working-and-waitingagent.md)) still covers the whole park. Narrows 0111's "fail the sub-agent's turn rather than hang its parent" to the provider layer only — the runtime layer above it has a third option 0111 didn't have: steer, don't fail. Rejected: a new wire field/state for this (touches every exhaustive match for a signal the existing `Error` event already carries), a retry cap on the re-wait (no natural bound on how long a human takes to steer) | Accepted |
| [0156](0156-normalize-and-stabilize-the-endpoint-pool-key.md) | Normalize and stabilize the endpoint pool key (#551, amends [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)/[0144](0144-file-backed-shared-endpoint-state-across-instances.md)): `pool_key` gains a central `normalize_endpoint` (trim trailing `/`, lowercase the host — path stays case-sensitive) so every wire client's pool identity collapses onto one `EndpointState`/shared-state file regardless of spelling, instead of each client having to pre-trim its own URL (and Anthropic doing neither); the API-key hash switches from `DefaultHasher` (documented unspecified, silently varying by Rust toolchain) to `sha2::Sha256`, making the pool key — and the shared-state file name it feeds — genuinely stable cross-process. `AnthropicLlm` gains a `base_url` field (new `ANTHROPIC_BASE` default) threaded through `anthropic_factory`/`resolve_for_user`, mirroring `OpenAiLlm`/`GeminiLlm` — a catalog `wire: anthropic` entry's `base_url` (a proxy/gateway) is finally honored instead of silently ignored. New `client::prune_stale(max_idle)` sweeps `.state`/`.lock` pairs that are both idle and empty (never a live budget) — run at startup (1h) and, with a short 5s threshold, from `/key`'s submit handler (`spawn_blocking`, off the keypress path) so a rotated-away key's file doesn't linger indefinitely. `entanglement-provider/tests/streaming.rs` gains a `Once`-guarded `ENTANGLEMENT_NO_SHARED_ENDPOINT_STATE=1` so its mock-server tests stop writing real state files into the developer's data dir. Rejected: normalizing per-client instead of centrally (the exact bug being fixed), forcing an already-bound session to rebind on `/key` rotation (materially larger change than the issue's actual complaint, the orphaned file), evicting the in-process `EndpointState` map entry too (bounded, not the unbounded on-disk artifact), a recurring background prune task (more machinery than the evidence calls for; startup + rotation triggers suffice for v1) | Accepted |
| [0157](0157-mcp-http-transport-shares-the-endpoint-pool.md) | The MCP HTTP transport shares the LLM endpoint pool (#559, amends [0050](0050-per-endpoint-connection-pool-retry-rate-limit.md)/[0153](0153-mcp-server-oauth.md)): `McpHttpClient` no longer builds its own bare `reqwest::Client` — `connect`/`connect_authenticated` take the caller's `HttpClient` (the pool) and an optional `api_key`, and every request routes through `execute_with_retry` like the LLM clients, `StreamGuard` held for the whole `request`/`notify` call. Pool-key identity is `(self.url, self.api_key)` — the MCP server's *own* URL, its *own* `EndpointState` bucket, isolated from its provider's LLM endpoint (ADR-0050's "host alone isn't the right grain" reasoning applies here too) while sharing key-hash conventions with it. Fixes the concrete gap: z.ai's bundled `web_search_prime`/`web_reader`/`zread` (ADR-0152) shared `ZAI_API_KEY` with the LLM endpoint but issued completely unmetered requests against that key's real provider limit. The provider-key linkage (`AvailableServer.key_env`, ADR-0152) is threaded through `AvailableMcp::partition`'s startup set (widened from a bare `HashMap<String, McpServerConfig>`) and was already in hand at the lazy `/enable mcp` connect point (the *default* bundled-server activation state); a live `/mcp add` or an OAuth reconnect always passes `api_key: None` (still pooled, unkeyed bucket). The old flat 60s whole-request timeout is gone — `execute_with_retry`'s own bounded failure/429-patience behavior governs, the same tradeoff LLM traffic already accepts. `entanglement-core` re-exports `HttpClient` (alongside its existing `McpHttpClient` re-export) so the `mcp-http`-only build keeps naming no direct `entanglement-provider` dependency. `mcp/mod.rs`'s connect machinery splits into a sibling `connect.rs` (400-line file cap). Rejected: a bespoke MCP-only RPM gate (duplicates the pool's machinery, still opens a separate connection pool), sharing the exact `EndpointState` bucket with the LLM endpoint (different rate-limit domains), also pooling `mcp/auth`'s OAuth bookkeeping client (out of scope — low-volume, not the metered traffic the issue is about), re-scanning the catalog at connect time instead of widening `partition`'s return type (two lookups that can drift) | Accepted — "no special-cased shorter fuse" narrowed by [0169](0169-startup-mcp-connect-is-concurrent-and-fast-fail.md) for the startup handshake only |
| [0158](0158-defer-session-title-aux-call-under-contended-primary-concurrency.md) | Defer the session-title aux call when the primary model's concurrency cap is contended (#589, amends [0154](0154-per-purpose-auxiliary-models.md)): the session-title generator's aux call fires *concurrently* with the main turn by design, but when the aux purpose has no pin (falls back to the primary model) and that model carries an [0140](0140-per-model-concurrency-cap-layered-on-endpoint-cap.md) cap of 1 (z.ai's `glm-4.7-flash` tier), the main turn's own request holds the model's only permit for the whole turn, so the concurrently-fired aux call is *guaranteed* to block behind it — every time, with zero user-set config, reported as the engine "waiting for something" on a session's first prompt. `Catalog::effective_concurrency(provider, model)` (model's own cap, else its provider's, else `None`) backs a new `AuxLlmRegistry::concurrency_cap(purpose)` that mirrors `resolve`'s own pin/primary fallback exactly (a `catalog: Catalog` + `primary_concurrency: Option<usize>` snapshotted onto the registry at construction). `spawn_session_title_generator` checks this cap on each `Prompt`: above 1, contention is merely possible and firing stays concurrent unchanged; at 1, it's certain, so the generator instead subscribes to the outbound stream immediately (before spawning, closing the race against an already-in-flight turn) and waits for that session's first `Done`/`Error` — bounded by a 300s safety-net timeout — before making its aux call, sequencing the two instead of racing for a permit neither call can win early. Rejected: a dedicated `aux_concurrency` catalog reservation (only helps when the cap is already > 1; a cap-of-1 model has nothing to carve out from without starving the primary turn), a separate semaphore bypassing the per-model cap entirely (would admit 2 concurrent requests against a documented 1-in-flight provider limit — a real 429 traded for a perceived stall), skipping title generation outright on a contended model (throws away a working feature for a common config shape), lowering `glm-4.7-flash`'s cap (it's z.ai's own documented ceiling, not an invented default) | Accepted |
| [0159](0159-plan-mask-widened-for-explore-delegation.md) | `plan`'s own mask now carries `call`/`bash` (#597, related [0038](0038-physical-per-agent-tool-restriction.md)/[0114](0114-capability-level-permission-keys.md)/[0138](0138-sponsored-build-child-and-propose-plan-cycle.md)): `plan.md`'s system prompt tells it to delegate research to `explore`, and `explore.md` grants itself `call`/`bash` at `Ask` — but the ancestor-chain mask intersection (ADR-0038) erased both from any `explore` child because `plan.md`'s own allowlist lacked them, so a delegated `gh issue view`/`git log` was hard-refused before any approval round-trip. Adding `call`/`bash` to `plan.md`'s `tools:` allowlist is only half the fix (the narrowest of the two directions considered) — the ancestor *permission* ceiling needed a second, less obvious fix: `call` is a `MULTI_GROUP` tool (ADR-0114) whose bare (no-argument) grade is always the least-privileged of every bare capability grade in the profile, so `plan.md`'s pre-existing bare `write: deny` silently discards any bare `call: ask` and keeps `call`'s coarse grade `Deny` — which would still clamp an explore child's own `Ask` down to `Deny` on real dispatch even after the mask fix. `plan.md` instead uses the arg-scoped `call(*): ask` (ADR-0114 lets an arg-scoped capability key refine the multi-group floor), which fans to both `call(*)`/`bash(*)` and matches any real command — the coarse no-argument view stays `Deny` (an accurate floor), but every real dispatch, which always carries the command as its argument, resolves `Ask`. `write`/`edit` stay denied outside the plans-folder carve-out. Rejected extending the ADR-0138 sponsored-child exemption to this delegation — sponsorship's authorization is a specific user-approval event (`propose_plan` accept), not the weaker basis an ordinary `agent_spawn` call would need, and reusing it here would widen a deliberately narrow mechanism for a bigger blast radius than the actual mask mismatch. Also: `tool_masked` becomes a thin wrapper over a new `tool_mask_source` (`Option<SessionId>`, mirroring `resolve_with_source`'s split for the privilege ceiling) so `tool_runner`'s mask-deny path can name *which* link's mask refused the call ("restricted by its own profile" vs "restricted by ancestor agent `<name>`'s profile") instead of an undifferentiated "restricted by profile" | Accepted |
| [0160](0160-extended-thinking-round-trip.md) | Extended thinking round-trips as a persisted `ContentPart`, replay gated per model (amends [0007](0007-streaming-llm-and-provider-crate.md)'s "reasoning is display-only" consequence, reuses [0075](0075-provider-side-web-search-mvp.md)/#481's opaque round-trip rail): reasoning was streamed, rendered and persisted but deliberately dropped from `Context`, which is right for two wires and wrong for the third — with thinking enabled Anthropic requires the unmodified `thinking`/`redacted_thinking` block, signature intact, on the final assistant message whenever tool results come back, i.e. exactly a parked turn ([0061](0061-parked-turn-state-batch-tool-resolution.md)), so thinking + tools could not work at all (the client discarded `signature_delta` and captured no block). New `ContentPart::Reasoning { provider, text, data }` rides the existing `LlmEvent::ContentBlock` → `content_blocks` → committed `Message` → persisted event → replay-fold rail (no new `LlmEvent`); `signature`/`redacted` live inside opaque `data`, keeping Anthropic's wire shape out of a core type shared by three wires (the `ProviderSearch` precedent). New `OutEvent::ReasoningBlock` rather than reusing `SearchResult` — renaming would need a serde alias to keep pre-existing logs (`"type":"search_result"`) replayable. **Capture is unconditional; replay is a catalog decision**: `ModelEntry::replay_thinking: Option<bool>` gates only whether a block is *sent back*, so toggling it never rewrites a log or changes what a head renders; `None` derives from the wire (Anthropic on whenever thinking is enabled — the API requires it and a silent `false` is an undiagnosable 400; others off), an explicit value always wins. Hard rules regardless of the flag: provider match (a foreign block renders *nothing*, stricter than `ProviderSearch`'s summary fallback — reasoning is not answer content); `as_text` returns `None` so it stays out of `content_text`/compaction/token estimation; an unsigned block is display-only. Ordering (thinking first) is restored in `anthropic_blocks`, lifetime (last assistant message only — Anthropic strips the rest server-side) in `convert_messages`, so core stays wire-agnostic. Also closes the `pause_turn` mid-thinking-block gap `sse.rs` had acknowledged. Rejected: blocks on the parked `TurnState` (invisible to compaction/inspect), flat `{text,signature,redacted}` fields, renaming `SearchResult`, replaying on every assistant turn (pure token cost), degrading a foreign block to text, a hardcoded per-wire rule with no catalog knob | Accepted |
| [0161](0161-unified-async-work-background-flag-and-one-poll.md) | Unified async work — a `background` flag on every launcher, one `poll` to join (supersedes [0026](0026-async-subagent-spawn-and-poll.md)'s launch/join *tool split* and [0033](0033-agent-tool-family-and-blocking-agent.md)'s separate-tools rationale; amends [0123](0123-agent-poll-zero-timeout-waits-for-notification.md)/[0008](0008-host-tools-workdir-and-bounded-output.md)/[0045](0045-call-host-tool-argv-exec-tailed-output.md)): four launchers each invented their own waiting rule — `call`/`rhai` always block, `bash` blocks unless backgrounded, `bash_output` **cannot wait at all** (`JobRegistry::poll` is a synchronous drain: lock, `mem::take`, return — so the model busy-waits across turns, every spin a full LLM round-trip), `agent` blocks unboundedly, `agent_poll` blocks 60s unless you know the `0` sentinel. Now every launcher blocks by default and takes `background: bool`; `agent_spawn` is removed (`agent { background: true }` replaces it); `poll { handle?, timeout_secs?, kill? }` replaces `bash_output` **and** `agent_poll`, waiting on a `watch` channel for agents and on a **new** `tokio::sync::Notify` in `JobRegistry` for jobs (the substantive new machinery — there is no wakeup mechanism today to extend). Delta-vs-final rides a `running`/`complete` **status**, not the tool name; `kill` SIGKILLs a job's process group but is **refused** on an agent handle (0033 deferred cancellation and this doesn't open it); unknown handle is an *error*, adopting `agent_poll`'s convention over `bash_output`'s return-as-text. `poll` bypasses permission like `agent_poll` (it starts nothing and touches no host resource — the *launch* stays the graded decision), which dissolves two warts: the embedded `explore` mask (`[read, glob, grep, call, bash, rhai]`) could start background jobs it could **never read**, and 0133's `bash_output`→`Ask` degradation. **Handle omitted ⇒ lists the session's pending operations**, closing the gap 0026 opened and left open ("a launched-but-never-polled child runs to completion unobserved") — plus `InMsg::ListOperations`/`OutEvent::OperationList` for heads on the 0072/[0146](0146-ask-user-list-retract-replace.md) correlation-id pattern; per-session ownership reuses the same bookkeeping as the descendant check and rides [0088](0088-session-aware-tool-execution.md)'s `run_for_session` seam. Lifetimes deliberately differ by kind (agents survive resume per [0112](0112-resume-cascades-over-the-spawn-subtree.md), jobs are OS processes that don't outlive the engine) and the list says so. One result shape for all four, and **a cap never loses data — `poll` serves the remainder, replacing the temp-file dance entirely**: the operation registry already holds the operation, so `poll { handle, offset?, tail? }` pages the retained text (`tail` **defaults to 30 lines**, matching `call`'s existing default and for the same reason — value concentrates at the end, and an unbounded page would reintroduce the blow-up the cap prevents; `tail: 0` = full page, still byte-capped) instead of `call`'s scratch-dir artifact (which cost a scratch directory, per-call naming, `.stderr` siblings, degraded-write notices, an absolute path spent in context, and files nothing ever cleaned up). This generalizes the handle rule from "backgrounded work has a handle" to "**work you might still have questions about has a handle**" — a blocking `call` that overflows the cap returns its tail *and* a handle. `call`'s **explicit** `output_file` is untouched (a user asking for a file, not a truncation workaround) and **`poll` names it when one exists** — the one place a path still appears, because there genuinely *is* a file rather than one truncation manufactured; only the implicit artifact goes. Cost: retention moves from disk to memory, so it needs a size cap + eviction, and past that cap the text is genuinely gone where a file would have held it — accepted only because overflow is *reported*, never silent. Closes the largest hole in the bounded-output story: sub-agent answers folded into a parent with *no truncation at all*. `rhai` gets the uniform output shape but **not** `background` in v1: `spawn_blocking` can't be aborted (its 30s cap is enforced *inside* the engine by `on_progress`) and the cooperative `stop: Arc<AtomicBool>` can't reach a binding already blocked in `exec`/`bash`, so backgrounding it means an unkillable detached task — a different decision that would let the weakest case set the family's terms. Rejected: timeout-on-`bash_output` only (leaves the divergence that caused the `explore` bug), always-return-a-handle (a 200ms command would cost two round-trips), a separate `operations` tool (same question at a different scope; an absent argument is cheaper than a sixth tool), auto-appending the pending list to every poll (doesn't help the post-compaction no-handle case), keeping `call`'s scratch artifact for the remainder (the filesystem *is* a retention policy, but it spends a path in context, routes a tool's own output back through the permission-graded file tools, and litters files outside the project), general parallel tool execution in core (still deferred — larger turn-loop/[0018](0018-turn-loop-stash-discipline.md) change that wouldn't unify the *waiting* vocabulary anyway) | Accepted |
| [0162](0162-agent-send-supervising-a-sub-agent.md) | `agent_send` — supervising a sub-agent instead of replacing it (amends [0138](0138-sponsored-build-child-and-propose-plan-cycle.md)): a sub-agent could be talked to exactly once, so a stuck child's only options were guess, finish early, or error — and [0155](0155-errored-subagent-turn-parks-for-steering.md) *tells the parent* the child "is still alive — steer it," a verb the parent didn't have. The obvious fix (an escalation tool + event + answerer-routing rule) turned out to be **unnecessary**, verified against the code rather than assumed: `collect_child_answer` already ends its wait on **any `Done` carrying text**, so a child concluding "I'm blocked, advise" *already* unparks its parent today; `InMsg::Prompt` is session-addressed with no parent/child restriction; `Holly::send` is already held by the executor; a child session task stays alive after its turn ends (it exits only when its `SessionCmd` channel drops). The child→parent channel was complete — only the **reply** was missing. So: one tool, **no protocol change**. `agent_send { agent_id, prompt, background? }` is structurally "a launcher against an existing session," carrying [0161](0161-unified-async-work-background-flag-and-one-poll.md)'s flag and reusing `collect_child_answer` blocking / `poll` backgrounded. Escalation is a *message, not a mechanism*: it routes to the **parent agent**, which reaches the human via its own `ask_user` if it can't decide — composing one level at a time up the spawn chain, so no race rule between agent and human answerers is ever needed. The parent **re-parks explicitly** (the child's answer ends the tool call; the parent investigates, then calls `agent_send` again) rather than answering blind in-place. Refusals are load-bearing: a closed/tombstoned child ([0056](0056-closesession-cascades-over-spawn-subtree.md)) refuses clearly, and a **hibernated** child must resume via [0112](0112-resume-cascades-over-the-spawn-subtree.md) or refuse loudly — never silently lazy-respawn a **blank** session wearing the right id, which [0090](0090-idle-ttl-auto-hibernation.md)'s sweep makes the *likely* failure (a plan agent reviewing a build report is exactly the shape that lets its child hibernate underneath it). Also surfaces the sponsored build child's `agent_id` in `propose_plan`'s result — a one-line gap with outsized effect, since the child is never closed and is already `AgentRegistry`-registered, so the 0138 cycle can reuse the **same** child with its accumulated context instead of spawning a fresh one per phase. Mid-flight steering is reachable only on the `background` path (a parent parked in a blocking `agent` call can't call anything), which is *why* the parked-parent race never arises. Non-termination (parent steers → child stuck → repeat) is bounded only indirectly by the turn budget in v1, recorded as a known-thin guarantee. **Left open, deliberately**: whether 0155's errored-turn park should now unpark the parent to decide, rather than waiting for a human — a change to 0155's trade-off wanting its own ADR once `agent_send` has usage behind it. Rejected: a dedicated escalation protocol (duplicates a working path), routing a child's `ask_user` to its parent (silently changes what `ask_user` means in a sub-agent and creates the answerer race), automatic re-park (forces answering without investigating), scoping the reuse to `propose_plan` only (all three consumers share `collect_child_answer`), a round cap (reintroduces 0155's original bug at a different timescale) | Accepted |
| [0163](0163-live-bash-enablement-is-a-tool-overlay-entry.md) | Live bash enablement is a tool-overlay entry, not its own message pair (**supersedes [0133](0133-live-bash-enablement-graded-by-permission.md)**, amends [0149](0149-per-session-tool-overlay.md)): 0133 (#498) shipped `InMsg::BashEnable { grade }`/`BashDisable` + `OutEvent::BashChanged` + a `BashGrade` enum because nothing generic existed yet; 0149 (#539) then built exactly that — and built it *on the 0133 precedent*, its own doc-comment calling `allow: false` "mirroring `BashGrade::Ask`" and `allow: true` grants "**exactly like a live bash grade (#498)**." The duplication costs a hardcoded `matches!(tool, "bash" \| "bash_output")` in `policy.rs`'s permission path and a second registration path that mints a **fresh `JobRegistry`** per enable — so `/bash off` + `/bash on` orphans every outstanding background job and restarts the `bg-N` counter at 0. But the overlay is **not yet a strict superset**, and the near-collision of two fields named `pattern` hid it: `ToolOverlayEntry.pattern` is a **tool-name** glob ([0148](0148-glob-patterns-in-the-agent-tool-mask.md) semantics), while `BashGrade::Allow { pattern: Some("git *") }` is a **command-argument** pattern materializing into an argument-scoped `bash(git *): allow` rule ([0114](0114-capability-level-permission-keys.md)/#173). The flat `allow: bool` can say "bash exists, Allow" or "…, Ask" but *not* "Ask by default, Allow only for `git *`" — 0133's most useful grade. So `ToolOverlayEntry` gains `arg_pattern: Option<String>` (`#[serde(default)]`, absent on every existing entry ⇒ wire shape and replay unchanged), materializing the same flat-`Ask`-plus-scoped-rule shape `grade_profile` does, fanned over the name-glob's matches — which generalizes past bash and closes 0149's own deferred "rhai binding *grades*" gap. Second, an enable entry matching a **closed, explicit table** of lazily-registrable built-ins triggers registration into the `SharedRegistry` (the overlay is mask-level; `bash` isn't in the registry at all without `ENTANGLEMENT_ENABLE_BASH=1`, so an entry alone can't conjure it) — a deliberately bounded table, since teaching a trusted frame to *instantiate* tools is the one genuinely new power here. Third, **one long-lived `JobRegistry`**, which dissolves the orphaning bug rather than fixing it and is a prerequisite for [0161](0161-unified-async-work-background-flag-and-one-poll.md)'s shared handle namespace. Removes two `InMsg` variants, one `OutEvent` variant, an enum, the `policy.rs` special-case and most of `bash_live.rs`; TUI `/bash on|off` becomes `/enable tool bash [--allow [<pattern>]]`. `ENTANGLEMENT_ENABLE_BASH` unchanged — [0010](0010-single-head-crate-and-bash-opt-in.md)'s gate/dispatch orthogonality still holds. Rejected: sharing the `JobRegistry` while keeping `BashEnable` (leaves the duplication that *is* the problem), dropping pattern narrowing for config rules (a session-scoped in-the-moment decision pushed into persistent config = capability regression), replacing `allow: bool` with a `grade: Permission` field (cleaner but rewrites 0149's wire shape for existing senders and replayed logs, where an additive optional field doesn't), letting any tool name be lazily instantiable (a general tool-construction facility with no review surface) | Accepted |
| [0164](0164-short-sortable-kind-tagged-ids.md) | Short, sortable, kind-tagged ids behind a pluggable generator (amends [0002](0002-session-multiplexed-protocol.md)'s minting only): sessions were 36-char unordered UUIDs; background jobs were `bg-{n}` from a per-registry `AtomicU64` that **restarts at 0** whenever a fresh `JobRegistry` is built, so job ids weren't unique even within a session — which [0161](0161-unified-async-work-background-flag-and-one-poll.md) turns from a wart into a correctness bug, since one `poll` must dispatch on a handle. Cheap because `SessionId` is `pub struct SessionId(pub String)` — a newtype already stringly-typed on the wire, with `new_uuid()` merely *one* constructor: this changes a **minting function**, not a type, serde shape, or stored format. Format `<kind>-<epoch-seconds hex><2-hex process salt><3-hex counter>` = **15 chars** (`s-6a708af0a1002`, `j-…`, `r-…`). **The generator never returns the same id twice**: the tail is a *monotonic counter*, not randomness — a process-global `(last_second, counter)` advances within a second and resets on rollover, and on exhaustion (4096/s) **waits for the next second rather than repeating**, making "never twice" structural rather than probabilistic while also restoring intra-second ordering a random tail couldn't. Sized against the **≤30-day object lifetime** (`ENTANGLEMENT_SESSION_RETENTION_DAYS`), since ids need uniqueness and order only among *live* objects: epoch-**seconds** in hex is exactly 8 chars and doesn't wrap until **2106**, and the key reasoning is *reduce resolution, don't truncate range* — keeping ms and cutting to 8 hex wraps every **49.7 days**, so against a 30-day window a wrap boundary can sit *inside* the live set and silently break sort order for exactly the objects that still matter. The 2-hex salt separates concurrent engines sharing a project dir ([0144](0144-file-backed-shared-endpoint-state-across-instances.md)) and is **probabilistic where the counter is absolute** — stated, not glossed. Scope is sessions + jobs + **runtime-minted** request ids: a `ToolCall.id` is provider-supplied on the Anthropic/OpenAI wires and passes through verbatim (reformatting it is precisely the bug #444 fixed for Gemini). Legacy UUID ids coexist indefinitely — the shapes can't collide and every consumer treats a `SessionId` as opaque; the sole migration point is a *test* (`session_id_new_uuid_generates_unique_ids` asserts `Uuid::parse_str`), production code makes no such assumption. An `IdGen` trait behind `Arc<dyn IdGen>` on `EngineConfig` follows the established seam pattern ([0079](0079-pluggable-permission-resolver-and-grant-store.md)/[0154](0154-per-purpose-auxiliary-models.md)/[0134](0134-per-profile-sandbox-scoping-and-spawn-chain-clamp.md)) so an embedder needing fleet-coordinated ids swaps one object. Rejected: git-style short prefixes over UUIDs (needs an ambiguity resolver, gives jobs no shared scheme, leaves the stored form 36 chars), a random tail (probabilistic even intra-process, can't order within a second), ms truncated to 8 hex (the 49.7-day wrap), a custom epoch (arbitrary constant every reader must know), a shared cross-process sequence file (a lock per id to convert unlikely into impossible, when the seam already covers anyone who needs that), making `SessionId` a real typed id (turns a minting change into a protocol change and kills the pluggability) | Accepted |
| [0165](0165-background-bash-jobs-are-bounded-by-timeout.md) | Background `bash` jobs are bounded by `timeout`, not exempt from it (#617, part of #604; related [0161](0161-unified-async-work-background-flag-and-one-poll.md), whose context table names this exact gap): the schema documented `timeout` as "ignored when run_in_background=true" — a stated gap, not a silent bug, but one that left a backgrounded job with no deadline at all, running until the engine exited or a human polled `bash_output` with `kill: true`. `JobRegistry::spawn` now takes the same `timeout: Duration` a foreground call already computes (`min(timeout.unwrap_or(120), 600)`) and starts a deadline task alongside the existing drain/reaper tasks; if the job is still running when the deadline elapses, the task `kill_process_group`s it — the same path an explicit `kill: true` poll uses — and sets a new `timed_out` flag, tracked separately from the resulting `Exited(None)` so `bash_output`'s poll message can say *why* (`[killed: timed out after Ns]`) instead of leaving the model to guess between "exited on its own", "timed out" and "explicitly killed". No default changes: 120s default, 600s cap, identical to a foreground call — a job that must outlive that needs an explicit larger `timeout` up to the cap; there is still no unbounded option. Fixed now, on the current `run_in_background` surface, rather than waiting on 0161's larger `background`-flag-family rework, because the gap is a live, independently-actionable bug. Rejected: waiting for 0161's rename (a much larger, uncommitted-to-a-timeline change; this fix carries over unchanged once the field renames), a separate longer default timeout for background jobs (a second bound to reason about for no clear benefit — the `timeout` argument already covers it), leaving the terminal status as bare `Exited(None)` and letting the model infer timeout from elapsed time (the registry already knows why it killed the process) | Accepted |
| [0166](0166-migration-note-and-startup-warning-for-stale-tool-names.md) | Migration note + startup warning for stale tool names in masks/permission rules (#623, part of #604; amends [0161](0161-unified-async-work-background-flag-and-one-poll.md)'s "Config churn" consequence): removing `bash_output`/`agent_poll`/`agent_spawn` invalidates any `tools`/`disallowed_tools` mask entry or `permission:` rule key naming them — a mask entry that matches nothing silently narrows the advertised tool set, a permission rule that matches nothing silently never fires, and neither errors. `tool_names::KNOWN_TOOL_NAMES` is a compile-time vocabulary of every literal tool name in the codebase, deliberately independent of what's registered *this run* (`bash`/`rhai` are env/feature-gated, MCP tools connect after profiles load — a registry-driven check would false-positive `plan.md`'s own `bash` entry under an ordinary bash-disabled run); `is_recognized_mask_entry` additionally admits capability keys, `*`/`?` globs ([0148](0148-glob-patterns-in-the-agent-tool-mask.md)), and `mcp__*` names. `agents::warn_unrecognized_mask_entries` runs on every parsed profile (built-in, user, project alike) and `tracing::warn!`s each unrecognized `tools`/`disallowed_tools`/`permission` entry — visible by default (`warn` is the fallback log level with no `--verbose`/`RUST_LOG`) but never a load error, since "unrecognized" is a heuristic (the vocabulary itself can lag a brand-new tool) and bricking startup over one stale mask line is a worse failure than degrading that one entry loudly. `grants.yml` is deliberately left unvalidated: a stale grant key is inert by construction (exact-match against live calls, so nothing "degrades quietly" the way a mask/rule does) — the migration note covers pruning it as file hygiene instead. Rejected: deriving "known" from the live `ToolRegistry` (false-positives on conditionally-registered tools), making an unrecognized entry a load error, validating `grants.yml`, aliasing the removed names (reopens the "no aliases" decision [0161](0161-unified-async-work-background-flag-and-one-poll.md) already made) | Accepted |
| [0167](0167-embedded-research-agent-profile.md) | Embedded `research` agent profile — a global read-only Q&A entry agent (related [0137](0137-explore-ask-grade-shell-access.md)/[0159](0159-plan-mask-widened-for-explore-delegation.md)): a fifth built-in for *thinking a problem through* without any write path — `mode: all` (entry-capable via the `/agent` picker **and** a legal spawn target, kept out of the Tab ring), read triad `Allow`, exec (`call`/`bash`/`rhai`) advertised at `Ask` (the ADR-0137 posture — per-command escalation, no dead ends), `write: deny` with **no** carve-out and the write tools also unmasked, no `propose_plan` (authorship stays `plan`'s job). `spawnable_agents: [research]` self-closes the spawn subtree: the per-spawner allowlist (ADR-0040) isn't transitive, but a self-only list is — every descendant is `research` again, so delegation can never widen into a write-capable profile (the omitted-`agent` `explore` default is refused; the prompt body says name `agent: research` explicitly). Grading uses the ADR-0159 pattern verbatim (`write: deny` drags the multi-group `call`/`rhai` floor to `Deny`; arg-scoped `call(*): ask` + a later literal `rhai: ask` govern real dispatch). "Global" = embedded built-in + the standard ADR-0034 layer shadowing as the tweak path (pinned by an override test). Rejected: user-layer-only file (no shared baseline), a primary + subagent twin pair (`mode: all` does both), hard-denied exec (re-opens the pre-0137 dead-end), widening the allowlist to `explore` (default-target spawns would silently succeed and the posture would rest on two profiles) | Accepted |
| [0168](0168-lazy-prompt-refuses-a-known-hibernated-child.md) | The lazy-`Prompt` path refuses a known hibernated sub-agent child instead of blank-respawning it (amends [0077](0077-session-hibernation-evictable-resumable.md)): the supervisor's lazy-`Prompt` path (an unknown-but-not-closed session id auto-creates a blank root under `build` — the single-user convenience) decided the profile via `resolve(DEFAULT_PROFILE)` before consulting `parent_links`, and `HibernateSession`/the idle-TTL sweep tore `parent_links` down alongside `session_meta` on eviction — so a hibernated **sub-agent child**, prompted without an intervening `Resume`, was silently blank-respawned running under `build`'s unrestricted permission profile and tool mask, discarding its history with no signal on the event stream distinguishing it from a legitimate resume: the exact escalation the `Spawn` handler's own unknown-target case (ADR-0040/#119) already refuses for a typo'd agent name. Fix: `hibernate_subtree` keeps the `parent_links` edge (still tearing down the live `sessions` channel and `session_meta`, so a hibernated id is still gone from `ListSessions`) — it's the cheapest possible record of "this id is a spawned child," and it's still cleaned up on `CloseSession`. The lazy-`Prompt` path now checks `parent_links` *before* deciding a profile: `Some(parent)` refuses with a supervisor `Error` naming `Resume` as the fix (mirroring the closed-id refusal, issue #105), and only a genuinely fresh id — no entry, or an entry with no parent (e.g. a hibernated `/compact` successor root) — keeps the old blank-`build` convenience. Rejected: carrying the child's recorded profile forward from `session_meta` instead of refusing (would need `session_meta` to survive hibernation too, defeating eviction), a separate ever-spawned-child registry (duplicates data `parent_links` already holds), refusing every non-live lazy-`Prompt` id regardless of parentage (breaks the documented convenience for a root, which carries no escalation hazard) (#639) | Accepted |
| [0169](0169-startup-mcp-connect-is-concurrent-and-fast-fail.md) | Startup MCP connect is concurrent, and the startup handshake fails fast (narrows [0157](0157-mcp-http-transport-shares-the-endpoint-pool.md)'s "no special-cased shorter fuse" to exclude just the one-time startup handshake): the startup connect loop walked configured servers sequentially, and each HTTP server's `initialize` rode the shared endpoint pool's LLM-tuned `RetryConfig` default (5 attempts, ~10s backoff) even when the server simply refused the connection outright — one dead server cost ~10s, N dead servers cost N times that, all ahead of any head becoming interactive. `HttpClient::execute_with_retry` gains a per-call `retry: Option<RetryConfig>` override (`None` everywhere unchanged — every LLM client, every non-startup MCP call) that swaps only the failure-path knobs (`max_attempts`/backoff/`response_header_timeout`), never the RPM/concurrency/429 admission gates ADR-0157 actually cares about; threaded from the runtime's startup `mcp::connect` down through `McpClient::connect`/`connect_with_auth` into `McpHttpClient::handshake` only — `tools/list`/`tools/call` and every live `/mcp add`/`/mcp connect`/`mcp_enable` connect keep the patient default. Startup's per-server connect+handshake+`tools/list` also now fans out concurrently via `tokio::task::JoinSet` instead of a sequential `for` loop, with the registry mutation (`register_tools`) still done synchronously on the main task as each result lands, so `registry` is never touched from more than one place. Rejected: backgrounding the whole startup connect and starting heads before any MCP tool registers (the original issue's larger option 3 — touches the roster-snapshot ordering in `main.rs`, a separately-scoped win) (#660) | Accepted |
| [0171](0171-zai-streaming-web-search-placement-confirmed.md) | z.ai streaming `web_search` placement confirmed (amends [0131](0131-web-search-post-mvp-follow-ups.md) §3, itself amending [0075](0075-provider-side-web-search-mvp.md); closes the last of the web-search MVP's four accepted limitations): live-verified against a working Coding Plan key (`https://api.z.ai/api/coding/paas/v4/chat/completions`, the catalog default) that `web_search` is a top-level sibling of `choices`, delivered exactly once on the final chunk alongside `finish_reason`/`usage` — never nested under `choices[0].delta`. `openai::sse::handle_chunk` drops the never-matching delta-nested scan site, keeping only the confirmed top-level one; a regression test locks in the removal. Also found: tool invocation is model-decided, not guaranteed by `enable: true` — several live attempts streamed a full response with no `web_search` key at all, the model's own reasoning showing it deciding not to call the tool, which is an *observed* instance of the "cited-text-only floor" both 0075 and 0131 already accepted as the worst case. `docs/deferred-work-ledger.md` row 3 moves to Resolved (#625, orig. part of #481) | Accepted |
